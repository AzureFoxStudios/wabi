[package]
name = "wabi-payments-eu"
version = "0.1.0"
edition = "2021"
description = "Wabi payments-eu addon: SEPA Instant via EPC QR-067 (GiroCode) builder + IBAN validation"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"