//! Lore addon integration — re-exports from the `wabi-lore` crate.
//!
//! This module wraps the `wabi-lore` crate so the server can
//! uniformly refer to `crate::lore::LoreService` etc.

pub use wabi_lore::*;
