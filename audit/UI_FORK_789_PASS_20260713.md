# Wabi Polish Fork — Items 7 / 8 / 9 Pass

**Date:** 2026-07-13
**Workdir:** /var/home/Ronin/wabi/frontend
**Brief:** `POLISH_FORK_789_BRIEF.md` (separate fork session; Phases A–D assumed complete)
**Verification:** `bun run check` → **0 errors, 75 warnings** (warning count unchanged from baseline). `bun run build` → **success** (adapter-node, ~20s).

---

## Precondition note
Phases A/B/C pass files exist. **`UI_PHASE_D_PASS_2026-07-13.md` is absent.** Item 9 below assumes any Phase D work landed in `frontend/src`; if Phase D did not run, treat its surfaces as still-unaudited. No code in this fork depends on Phase D.

---

## Item 7 — Admin "Server Policy" section (real data only)

Added a real, data-backed `Server Policy` admin section reusing `AdminWorkspace` plumbing. Scope per decision: **curated subset** (only keys not already owned by another section) and **top-level nav** (owner/admin only). **No new backend routes, no mock/fabricated data.**

### Files changed
- `src/lib/components/admin/ServerPolicyPanel.svelte` **(NEW)** — owns loading/saving for:
  - **Upload limits** via `getAdminUploadLimits` / `saveAdminUploadLimits` (per-role MB grid + global cap).
  - **Download limits** via generic `getAdminPolicy<DownloadLimitConfig>` / `saveAdminPolicy` over `download_limits`.
  - **Community node announcements** via `community_node_announcements` (enabled, channelId, online/offline templates).
  - **Community node access** via `community_node_access` (mode select + allow-list add/remove by userId).
  - Self-contained scoped `<style>` (token-driven) so it does not depend on `settings-core-part1.css`, which is **not** in the global `styles.css` aggregator.
- `src/lib/components/AdminWorkspace.svelte` — added `'settings'` to the `section` prop union; new `{:else if section === 'settings'}` branch rendering `<ServerPolicyPanel canManageAdmin={canManageRoles} />` (owner/admin gated).
- `src/lib/components/AdminCenterStage.svelte` — added `'settings'` to the `Section` union, `sectionMeta` (`Server Policy`, sliders icon), and `sectionAccess` (`owner`,`admin`); the existing `{:else}` main branch already routes non-overview sections to `<AdminWorkspace>`, so the center renders it automatically.

### Decisions taken
- **Curated subset** (not full `AdminPolicyKey` browser): `runtime_tuning`/`payments_access`/`frontend_app_metadata` are already surfaced in their own sections, so they are intentionally excluded to avoid duplicate UI.
- **Top-level nav item** labeled **"Server Policy"** (the Phase A-dropped `settings` slot is reborn as a real section).
- Generic policy browser uses the existing `getAdminPolicy`/`saveAdminPolicy` generic key→value API; upload limits use the dedicated `getAdminUploadLimits`/`saveAdminUploadLimits` per the brief.

---

## Item 8 — App-wide motion cohesion (OS-only reduced motion)

Created one shared `styles/motion.css` as the single source of truth for motion primitives, and consolidated the duplicated one-off keyframes that were scattered across component CSS.

### Files changed
- `src/styles/motion.css` **(NEW)** — canonical definitions: `.live-dot` (token green pulse), `.reveal` + `.reveal-stagger` (fade-up, token-driven distance/duration/stagger), and the shared keyframes `wabi-live-pulse`, `wabi-status-pulse-online`, `wabi-admin-pulse-glow`, `wabi-admin-card-reveal`, `wabi-admin-slide-in`, `wabi-admin-seg-in`, `wabi-admin-fade-slide-in`, `wabi-reveal`. Includes a single `@media (prefers-reduced-motion: reduce)` block (OS-only, per decision — no in-app motion toggle added).
- `src/styles/styles.css` — added `@import './motion.css';` after `animations.css`.
- `src/styles/components/sidebar-channels.css` — `.live-dot` now references `wabi-live-pulse`; removed the local `@keyframes live-pulse` and its local reduced-motion block.
- `src/styles/components/status-system.css` — online/hero status dots now reference `wabi-status-pulse-online`; removed the local `@keyframes status-pulse-online` and its reduced-motion block.
- `src/styles/components/sidebar-core-part3.css` — status-indicator online now references `wabi-status-pulse-online`; removed the **duplicate** local `@keyframes status-pulse-online`.
- `src/styles/components/admin-center-stage.css` — pulse/reveal usages now reference the shared `wabi-*` keyframes; removed the local keyframe definitions (`adminCardReveal`, `adminSlideIn`, `adminPulseGlow`, `adminSegIn`, `adminFadeSlideIn`) and the local reduced-motion block. The admin-specific **shine** effects (`adminShimmer`, `adminShine`) remain here as they are not duplicated anywhere.

### Notes
- The universal reduced-motion guard in `animations.css` (`* { animation/transition-duration: 0.01ms }`) already covers all components, so removing the per-file local reduced-motion blocks is safe and reduces duplication.
- Whiteboard / Notes / DM motion is predominantly **transition-based** (hover-lift, etc.); those are already governed by the universal reduced-motion guard, and `.live-dot`/`.reveal` are now available as shared tokens for any future keyframe-driven work there. No layout or density changed.

---

## Item 9 — Audit re-sweep of remaining surfaces (PLAN ONLY — no implementation)

Re-read `DESIGN_AUDIT_2026-07-13.md` §5 + §11.3–§11.5. The following surfaces were **not** touched in Phases A–D. Each is a **separate sign-off** entry; none were implemented in this fork.

Priority is ranked by user-facing awkwardness vs effort; all entries honor the guardrails: frontend-only, never regress cozy chat density, token-driven, no commits, no fabricated data.

### P0 (high impact, low risk)
1. **Members panel** (`MemberList.svelte` / right-panel members) — verify row density matches the cozy/accepted DM-row language (32px avatar, status dot, unread/preview, time-ago). Confirm hover/focus-visible parity with `DmListPanel`. Sign-off: density + a11y pass.
2. **Channel-sidebar density** (`ChannelSidebar.svelte`, `sidebar-channels.css`, `TextChannelList.svelte`) — Phase C added the LIVE pill; audit overall channel-row rhythm, active-state wash, and section header micro-labels against the mock's left-nav recipe (§11.1). Ensure no regression to cozy chat. Sign-off: density + token check.
3. **Server rail** (`ServerRail.svelte`, `ServerRail.css`, `ServerSwitcherPanel.svelte`) — far-left 72px identity rail; audit icon sizing, active server indicator, add-server affordance, and tooltip language. Sign-off: visual parity.

### P1 (medium)
4. **Login screen** (`Login.svelte`, `login/LoginConnectionPrompt.svelte`, `login/LoginQRModal.svelte`) — Phase A/B noted Discord blurple still in `LoginConnectionPrompt` shadow and `LineDm.css`. Audit brand drift (blurple `#5865f2`, hard grays) and replace with accent tokens. Sign-off: token-only cleanup, no layout change.
5. **Map** (`MapWorkspace.svelte`, `map/`, `map-workspace.css`) — right-panel "Map" is in the calm default strip; audit empty/loading states, marker/chip density, and ambient vs primary weight. Sign-off: polish only.
6. **Media / Transfers** (`TransferCenter.svelte`, `TransferCard.svelte`, `MediaAlbumsTab.svelte`, `chat-upload.css`) — audit transfer card chrome, progress affordances, and token compliance; confirm no cozy-density impact. Sign-off: polish only.

### P2 (defer / deeper)
7. **Reader** (`ReaderTab.svelte`, `ReaderTabImpl.svelte`, `ReaderImportSheet.svelte`, `reader-tab.css`) — audit reading-column typography, max-width (mock ~1200px content rule), and toolbar hierarchy. Sign-off: typography pass.
8. **QuickResources** (`QuickResourcesPanel.svelte`) — multiple "mode" UIs (notes/DM) create a muddied mental model (per DESIGN_AUDIT §1); decide whether to simplify to a single calm mode or align with the right-panel default strip. Sign-off: IA decision before any code.

---

## Forbidden items respected
No backend writes, no cozy chat density changes, no right-panel width clamps, no git commits, no fabricated/mock-only data.

## Remaining risks
- **Phase D uncertainty:** if Phase D did not run, its surfaces are not yet audited; Item 9 P0–P2 assumes A–C only.
- **Server Policy live data:** all four panels call real endpoints; if the backend does not serve `download_limits` / `community_node_*` policies, those panels show their error state (graceful, by design). Verified only via `bun run check`/`build`; not exercised against a live backend in this session.
- **Login/brand drift (Item 9 P1):** listed but not implemented this fork (plan only).
- **Visual regression:** Item 8 reorganized CSS keyframe locations only (no value changes to animations); behavior is preserved. A browser probe of the admin "Server Policy" section and a live LIVE-pill/status-dot check are recommended but the server was not running here.
