# Superseded

**2026-07-17:** This checklist is STDB / wabi-node era and is **not** the current deploy model.

**Canonical install/deploy:** [`INSTALL.md`](./INSTALL.md)

**Summary:** one process `wabi-server` (WabiDB + SPA embedded) + `.env` + optional Caddy. Cargo path preferred; Docker/compose optional packaging only. No SpacetimeDB.
