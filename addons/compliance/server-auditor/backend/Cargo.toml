[package]
name = "server-auditor"
version = "0.1.0"
edition = "2021"
description = "Server compliance auditor addon for Wabi"
authors = ["Community"]
license = "MIT"

[workspace]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }