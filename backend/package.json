{
  "name": "wabi-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node build",
    "build": "esbuild src/server.ts --bundle --platform=node --target=node20 --outfile=build --external:socket.io"
  },
  "dependencies": {
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
