You are working in /var/home/Ronin/wabi. Implement ONLY the password-change session lifecycle fix from the Wabi Hermes regression backlog.

User symptom: changing password kicks the user out and keeps them kicked out.

Verified source facts:
- frontend/src/lib/api/auth.ts sends the current bearer token to POST /api/auth/change-password.
- core/crates/wabi-server/src/api/auth.rs handle_change_password verifies and updates the password, then calls state.revoke_user(auth.user_id), which revokes the current token as well as other sessions.
- core/crates/wabi-server/src/state.rs RevocationStore currently has users: HashSet<i64> and is_token_revoked checks that set.
- core/crates/wabi-server/src/auth_extractor.rs rejects revoked tokens.
- frontend ProfileSettingsTab currently assumes the current session remains valid after a successful response.

Goal:
Change the contract so a password change succeeds without trapping the current session. Preserve the current authenticated session and revoke other sessions, or return and install a replacement token if that is demonstrably safer in the existing architecture. Prefer the smallest root-cause fix consistent with current token/revocation design. Do not weaken authentication or remove revocation entirely.

Required work:
1. Read the relevant auth, state, extractor, and existing tests before editing.
2. Add a focused regression test that proves the token used for password change remains usable while another pre-existing token for the same user is rejected. Use existing test conventions and real auth/state code where practical.
3. Implement the minimal backend change. If adding current-jti-aware revocation, preserve persisted revocation compatibility and concurrency behavior.
4. Update frontend only if the actual chosen contract requires it.
5. Run the narrowest relevant Rust/frontend tests or checks available through the repository conventions. Do not run destructive commands.

Strict scope:
Allowed to modify only auth-related Rust source/tests and, if required, frontend/src/lib/api/auth.ts or frontend/src/lib/components/settings/ProfileSettingsTab.svelte plus focused tests. Do NOT touch channel, lore, docs, payments, uploads, or unrelated files. Do NOT git add, commit, stash, reset, checkout, or rewrite history. Do NOT install dependencies.

Before finishing, report:
- exact files changed;
- the test added and exact command/result;
- any baseline failure unrelated to this task;
- whether the current token and an older token were each verified semantically.
