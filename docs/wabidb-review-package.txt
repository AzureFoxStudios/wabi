
============================================================
WABIDB: ARCHITECTURE REVIEW PACKAGE (CONSOLIDATED)
============================================================

Date: 2026-06-19
For: Frontier AI architecture review (GPT / Claude / Gemini / etc.)
Status: Design complete. Take-off ready pending review. First vertical slice is the gate, not this document.
Current product status (2026-07-29): Wabi DMs are server-readable. Encryption sections describe a proposed end state, not shipped behavior or a product guarantee.

============================================================

------------------------------------------------------------
CONTEXT FOR THE REVIEWER
------------------------------------------------------------

This is one package. It contains:

* **PART 1** — The WabiDB end-state design. The main artifact. A custom Rust database engine intended to replace SpacetimeDB inside the Wabi app (a self-hosted Discord alternative for small teams).
* **PART 2** — The SpacetimeDB architecture study. The research foundation that informed the design. What we kept from STDB, what we dropped, and why.

Read PART 1 if you are short on time. PART 2 is reference material for understanding the "why" behind specific design choices.

BACKGROUND

Wabi is a self-hosted chat/collaboration platform. Today it uses SpacetimeDB (BSL 1.1) as its state engine. The user (Ronin) is evaluating whether to build a Wabi-native replacement to:
* Eliminate the BSL license exposure
* Reduce scope (STDB is a generic database; Wabi only needs collaborative room state)
* Add privacy/retention primitives STDB doesn't have
* Own the engine rather than depend on a third-party product

KEY QUESTIONS FOR REVIEW

1. **Is the design sound?** Every architectural choice is derived either from STDB patterns that work (and were kept) or from a deliberate "Wabi is not that" boundary (and were replaced). The Wabi-specific extensions (addon object primitive, helper-node protocol, range-read blob store, two-tier data lifecycle) are all there with rationale.

2. **Is the boundary correct?** Section 13 of PART 1 lists what WabiDB is NOT. If a reviewer thinks a boundary should be different (e.g., "WabiDB should be a real SQL database" or "WabiDB should support federation"), that's a fundamental redesign, not a tweak.

3. **Is the data model grounded in the actual frontend?** Section 12.4 of PART 1 has a "Feature Preservation" table mapping each real Wabi frontend file to the corresponding WabiDB table. Cross-reference against the actual files if you have access; the mappings were verified field-by-field by an independent research pass.

4. **Is the addon system flexible enough?** Section 9.9 introduces the addon object primitive. The CAD-as-addon worked example shows how a complex feature fits without bloating the engine. Apply the same pattern to a different complex feature (music sequencer, video editor, etc.) and see if it holds.

5. **Is the migration path realistic?** Section 12.5 is brief. The full migration plan is NOT yet written. This is the one real follow-up item: STDB's data export is the bottleneck (no clean dump tool exists in STDB; the migration has to read STDB while it's still live and transform).

WHAT CONVERGED ACROSS THREE INDEPENDENT RESEARCH PASSES

* SQLite WAL handles Wabi's expected scale (1k-10k users) easily with proven PRAGMAs
* Tokio + tokio-tungstenite scales to 100k+ WebSocket connections; 1k is trivial
* Per-recipient DM encryption (Signal's Sesame pattern) has negligible storage cost
* BLAKE3 is overkill in the right way (3-15 GB/s)
* Figma's production architecture validates the in-memory-hot + durable-committed + WAL pattern
* JWT/PASETO/Macaroons are all valid for the helper-node signed-token approach
* The frontend's data shapes are covered field-by-field in the WabiDB schema

The single MEDIUM risk item: STDB data export for the migration.

WHAT IS NOT IN THIS PACKAGE

* The WabiDB source code (not yet written — the design is the contract)
* Migration scripts (separate doc to be written)
* The first vertical slice (a weekend build, not yet started)
* A specific build-order doc (also separate)

The end-state design is the work. The implementation is the next step.

LEGAL / ETHICAL NOTE ON THE STDB STUDY

The SpacetimeDB source was studied locally at /var/home/Ronin/vendor/SpacetimeDB (v2.6.0, commit 1e51e32). No significant portions of STDB source code are reproduced in this document — only short, file:line-cited excerpts (≤10 lines) where load-bearing. All architectural derivations from STDB are paraphrased in original wording. The STDB study respects the project's license (BSL 1.1) and the standard "clean room" practice for architectural inspiration.

============================================================

============================================================
PART 1: WABIDB END-STATE DESIGN
============================================================

The main artifact. ~18,000 words. 15 main sections.

============================================================

============================================================
WABIDB: END-STATE DESIGN DOCUMENT
============================================================

  | **Status:** Proposal
  | **Date:** 2026-06-18
  | **Audience:** Senior Rust engineer implementing the Wabi database engine
  | **Scope:** The complete, production-ready WabiDB. Not the migration path. Not phased rollouts. Just what it is.

============================================================

------------------------------------------------------------
1. WHAT WABIDB IS
------------------------------------------------------------

WabiDB is a purpose-built, single-machine database engine and live-subscription service that Wabi-server talks to over a Rust API — not HTTP, not WASM, not RPC — to persist the entire state of a self-hosted Discord alternative and push real-time changes to browser clients over a WebSocket topic-subscription protocol.

The problem it solves is specific. Wabi currently depends on SpacetimeDB (BSL 1.1 licensed) for state management, but STDB's architecture imposes hard constraints that are misaligned with Wabi's product: the "one instance per app" BSL restriction prohibits multi-database privacy compartmentalization; the generic database scope (SQL, ad-hoc queries, system tables, migration planner) adds complexity Wabi does not need; the full WASM/V8 module runtime (tens of thousands of lines) exists to run user-supplied code that Wabi will never run; there is no helper-node awareness built-in, so retooling for media/transcode/search nodes must be retrofitted; there is no encryption-at-rest or per-DM encryption model; and the commitlog retains all event-table rows forever with no retention mechanism. WabiDB solves all of these at once by being a focused engine that does exactly what Wabi needs and nothing else.

Non-goals, explicitly:

* **Not a SQL database.** WabiDB has no SQL parser, no query planner, no ad-hoc query interface. State is read through typed Rust accessors, not `SELECT * FROM`.
* **Not a game engine.** WabiDB does not run game logic, physics, or spatial queries.
* **Not a programmable app platform.** No WASM runtime, no V8 isolates, no user-supplied modules. Wabi-server itself is compiled into the binary alongside the engine; it calls Rust functions directly.
* **Not federation.** WabiDB is a single-server engine. Cross-server federation is handled at a higher layer (Wabi-server's existing Socket.IO relay and mesh infrastructure).
* **Not multi-tenant SaaS.** WabiDB is deployed one-per-server. Each instance holds exactly one logical database for one community.

How it is shaped: WabiDB is a Rust library crate (wabidb) that wabi-server imports and calls. The engine can be embedded directly in the wabi-node binary (the canonical deployment) or run as a sibling process that wabi-server connects to over Unix domain sockets (for operational isolation). It is single-machine v1. Helper-node awareness is designed in from v1, not retrofitted. The engine talks to the frontend exclusively over WebSocket, using a topic-subscription grammar with BSATN or JSON framing. There is no WASM, no V8, no module lifecycle, no energy metering, no SQL interface, no generic "database server" posture. It is a custom state engine for a specific application.

============================================================

------------------------------------------------------------
2. THE ARCHITECTURE OVERVIEW
------------------------------------------------------------

The system is layered into seven distinct strata. Each layer lives in a named Rust module and exposes a small set of public types. Moving top-to-bottom:

2.1 THE STORAGE LAYER

Crate: wabidb::storage  
Public types: StorageEngine, Transaction, CommittedState, WalWriter

The storage layer wraps a single SQLite database file opened in WAL mode (PRAGMA journalmode=WAL). This is the committed durable state — not an in-memory BTree collection like STDB (crates/table/src/table.rs), but a real on-disk database with ACID transactions. SQLite WAL mode provides concurrent readers without blocking writers, and the WAL file provides crash recovery without a separate commitlog. There is no page pool, no object pool, no custom page layout — SQLite's own B-tree implementation handles storage. The StorageEngine opens one file per Wabi server at {datadir}/wabidb/wabidb.sqlite. The Transaction type wraps an rusqlite::Transaction with a typed API. Schema migrations use refinery (or sqlx-migrate) with versioned migrations in wabidb::storage::migrations. The committed state is what survives a crash. No in-memory-only tables exist at this layer.

2.2 THE TRANSACTIONAL OUTBOX

Crate: wabidb::outbox  
Public types: Outbox, OutboxRow, Topic, Payload

Every transaction that mutates state writes atomically into an outbox table within the same SQLite transaction. The outbox schema is:

    CREATE TABLE outbox (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        topic TEXT NOT NULL,
        payload BLOB NOT NULL,
        created_at INTEGER NOT NULL,
        broadcast_at INTEGER
    );
    CREATE INDEX idx_outbox_unsent ON outbox(broadcast_at) WHERE broadcast_at IS NULL;

The pattern is identical in spirit to the STDB commitlog + subscription delta pipeline, but simplified: instead of a separate commitlog process and page-level delta computation, WabiDB commands write outbox rows directly. "What changed" is computed by the command, not by a generic diff engine. If no rows are inserted into the outbox, no notification is sent. The consumer task polls outbox WHERE broadcastat IS NULL, reads pending rows, broadcasts them to subscribers, and sets broadcastat in a batch UPDATE.

2.3 THE LIVE SUBSCRIPTION ENGINE

Crate: wabidb::subscription  
Public types: SubscriptionManager, Subscriber, TopicFilter, SendWorker

The subscription engine is an in-memory actor (a tokio task) that manages per-WebSocket-connection subscribers. Each subscriber holds a set of topic filters (see Section 5 for the grammar). When the outbox consumer produces a row, the subscription manager matches it against every subscriber's filter set and forwards matching payloads to each subscriber's send channel. The SendWorker (conceptually derived from STDB's SendWorker at crates/core/src/subscription/modulesubscriptionmanager.rs:1714) batches messages per-client: linger 5--25ms depending on throughput, then serialize and send. If a subscriber's send buffer exceeds 256 messages or 4 MB, backpressure activates: the connection is sent a {"type":"backpressure"} frame and may be disconnected if it does not drain.

The connection to the frontend is a raw WebSocket (no Socket.IO framing — that is the current approach and will be replaced). On upgrade, the client authenticates via a JWT token in the URL query string (?token=...). Once authenticated, the client sends a Subscribe message with a list of topic filters. The server responds with an initial snapshot for each matching topic (the current state), then pushes incremental updates as outbox rows arrive.

2.4 THE COMMAND/REDUCER LAYER

Crate: wabidb::commands  
Public types: Command, CommandCtx, CommandError, CommandRegistry

Commands are typed Rust functions. Each command receives a CommandCtx that provides:

    pub struct CommandCtx<'a> {
        pub tx: &'a Transaction<'a>,          // the current SQLite transaction
        pub caller: CallerIdentity,            // who invoked this command
        pub timestamp: i64,                    // monotonic timestamp (micros since epoch)
        pub capabilities: CapabilitySet,       // what the caller is allowed to do
        pub outbox: &'a mut Vec<OutboxRow>,    // collected outbox rows for this command
    }

Each command returns Result<T, CommandError>. The transaction wrapper opens a SQLite transaction, runs the command, and on success commits both the state mutations and the outbox rows atomically. On failure, the transaction is rolled back and no outbox rows are emitted. This is the same atomicity model as STDB reducers (crates/core/src/host/module_host.rs), but without the WASM bridge overhead. The entire command registry is a HashMap<&'static str, Box<dyn Fn(CommandCtx) -> Result<Box<dyn Any>, CommandError>>> populated at startup.

2.5 THE ROOM/OBJECT LAYER

Crate: wabidb::objects  
Public types: ObjectType, ObjectInstance, Room, RoomMember

The room/object layer defines a bounded set of object types (see Section 7 for the full catalog). Each object type has: a schema (which SQLite table(s) store its data), a set of valid operations (which commands can mutate it), a retention class, a member list, and a default set of subscription topics. This layer is a thin metadata registry that maps "whiteboard" to its backing tables, indexes, and commands. It exists so that generic operations like getinstance, listinstances, deleteinstance can be implemented once and reused across types, while per-type operations (like addwhiteboardpatch or movekanban_task) are registered individually.

2.6 THE ENCRYPTION LAYER

Crate: wabidb::crypto  
Public types: KeyRegistry, EncryptedPayload

The proposed encryption layer would store only public keys, never private keys. The target design performs per-DM-conversation key derivation client-side and limits the server to public identity keys, ciphertext/nonces, wrapped recovery blobs, and key-registration commands. Current DM text and uploads are server-readable.

2.7 THE RETENTION ENGINE

Crate: wabidb::retention  
Public types: RetentionPolicy, RetentionClass, TtlReaper

The retention engine runs a periodic tokio task (default interval: 60 seconds) that scans tables for expired rows and hard-deletes them. The RetentionClass enum (Ephemeral, Session, Durable, Archive) is per-object-type, not per-row. The TTL reaper logs deletion counts, and after every 1000 deletions it runs PRAGMA incremental_vacuum(N) to reclaim space without a full VACUUM.

2.8 THE HELPER-NODE PROTOCOL

Crate: wabidb::helpers  
Public types: PairToken, SignedRouteToken, CapabilityManifest, HelperRegistry

Helper nodes (media, search, transcode, cache, regional anchor) pair with the WabiDB authority through a token exchange. The authority issues a one-time pair token; the helper redeems it over a mutually-authenticated TLS connection and receives a long-lived signed route token. The route token is a JWT-like signed payload containing: capability list (which operations the helper is authorized to perform), expiry, audience (the helper's identity), and node identity. Helpers send heartbeats every 30 seconds; if three heartbeats are missed the helper is marked offline. The authority can revoke a route token at any time via a revocation command; the helper discovers revocation on its next heartbeat and stops accepting work.

2.9 THE CONNECTION TO WABI-SERVER

Crate: wabidb::server_api  
Public types: WabiState trait (see Section 10), ServerConnection

Wabi-server embeds the engine as a crate dependency. It calls the WabiState trait methods directly — no HTTP, no RPC, no IPC in the embedded mode. In sibling-process mode, the same trait is exposed over a Unix domain socket using a simple request/response protocol (bsatn-framed messages over a Unix socket). The frontend never talks to the engine directly; it always goes through wabi-server's Socket.IO layer, which translates WebSocket topic events into the engine's protocol.

============================================================

------------------------------------------------------------
3. THE DATA MODEL
------------------------------------------------------------

This section enumerates every table in the durable committed state. The schema is driven by the existing Wabi frontend code — if the frontend has a field, the table must have it. Each entry lists: table name, columns, indexes, relationships, retention class, encryption posture, and access control.

3.1 USERS, SESSIONS, DEVICES, KEYS

Table: users

    id              INTEGER PRIMARY KEY,
    username        TEXT NOT NULL UNIQUE,
    username_lc     TEXT NOT NULL UNIQUE,       -- lowercase for case-insensitive lookup
    handle          TEXT,                        -- optional @handle
    handle_lc       TEXT UNIQUE,
    display_name    TEXT,
    avatar_url      TEXT,
    banner_url      TEXT,
    status          TEXT NOT NULL DEFAULT 'offline',  -- online, idle, dnd, offline
    active          INTEGER NOT NULL DEFAULT 1,
    deleted         INTEGER NOT NULL DEFAULT 0,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL

Indexes: idxusersusernamelc UNIQUE, idxusershandlelc UNIQUE, idxusersstatus.

Retention: Durable. Encryption: none (usernames and handles are public within the server). Access: server admin creates; all members read; user self-updates.

Prior art: StateUser at wabistatebridge/src/lib.rs:141-152 carries the same fields (username, usernamelc, handle, handlelc, active, deleted) plus a generic row_json blob. WabiDB replaces the blob with fixed columns.

Table: sessions

    session_id      TEXT PRIMARY KEY,
    user_id         INTEGER NOT NULL REFERENCES users(id),
    expires_at      INTEGER,
    is_temporary    INTEGER NOT NULL DEFAULT 0,
    deleted         INTEGER NOT NULL DEFAULT 0,
    created_at      INTEGER NOT NULL

Indexes: idxsessionsuserid on userid. Retention: Session (rows are cleaned up on expiry or explicit deletion). Access: session owner reads/writes; server reads for auth.

Prior art: StateSession at wabistatebridge/src/lib.rs:183-192.

Table: devices

    device_id       TEXT PRIMARY KEY,
    user_id         INTEGER NOT NULL REFERENCES users(id),
    device_name     TEXT,
    device_type     TEXT,                        -- desktop, mobile, web
    public_key      TEXT NOT NULL,               -- X25519 public key (base64)
    created_at      INTEGER NOT NULL,
    last_seen_at    INTEGER

Indexes: idxdevicesuser_id. Retention: Durable. Encryption: none (public key only). Access: user creates/reads/deletes.

Table: identity_keys

    user_id         INTEGER PRIMARY KEY REFERENCES users(id),
    signing_key     TEXT NOT NULL,               -- Ed25519 public key (base64)
    key_fingerprint TEXT NOT NULL,
    rotated_at      INTEGER NOT NULL

Indexes: idxidentitykeys_fingerprint UNIQUE. Retention: Durable (even past user deletion for message verification). Access: user writes; anyone reads.

3.2 CHANNELS AND CHANNEL MEMBERS

Table: channels

    channel_id      TEXT PRIMARY KEY,            -- "text:general", "voice:lobby", etc.
    channel_type    TEXT NOT NULL,               -- text, voice, forum, gallery, wiki, stage, announcement
    name            TEXT NOT NULL,
    description     TEXT,
    created_at      INTEGER NOT NULL,
    created_by      TEXT,                        -- user id
    archived        INTEGER NOT NULL DEFAULT 0,
    auto_delete_ms  INTEGER,                     -- auto-delete messages older than this
    min_role        TEXT,                        -- minimum role to read
    is_private      INTEGER NOT NULL DEFAULT 0,
    parent_id       TEXT,                        -- for threads / breakout rooms
    sort_order      INTEGER NOT NULL DEFAULT 0

Indexes: idxchannelstype on channeltype, idxchannels_parent. Retention: Durable. Encryption: none. Access: server admin creates/archives; members read.

Prior art: StateChannel at wabistatebridge/src/lib.rs:112-123.

Table: channel_members

    channel_id      TEXT NOT NULL REFERENCES channels(channel_id),
    user_id         INTEGER NOT NULL REFERENCES users(id),
    role            TEXT NOT NULL DEFAULT 'member',  -- owner, admin, moderator, member
    joined_at       INTEGER NOT NULL,
    active          INTEGER NOT NULL DEFAULT 1,
    PRIMARY KEY (channel_id, user_id)

Indexes: idxcmuserid on userid. Retention: Durable. Access: server admin manages; members read.

Prior art: StateChannelMember at wabistatebridge/src/lib.rs:126-137.

3.3 MESSAGES

Table: messages

    message_id      TEXT PRIMARY KEY,
    channel_id      TEXT NOT NULL REFERENCES channels(channel_id),
    sender_id       TEXT NOT NULL,               -- user id (string form)
    content         TEXT NOT NULL,               -- the message body (plain text or JSON for rich)
    created_at      INTEGER NOT NULL,
    edited_at       INTEGER,
    deleted         INTEGER NOT NULL DEFAULT 0,
    deleted_at      INTEGER,
    reply_to        TEXT,                        -- parent message id
    is_pinned       INTEGER NOT NULL DEFAULT 0,
    is_spoiler      INTEGER NOT NULL DEFAULT 0,
    embed_url       TEXT,
    gif_url         TEXT,
    file_url        TEXT,
    file_name       TEXT,
    file_size       INTEGER,
    mime_type       TEXT,
    entities_json   TEXT,                        -- message entities (mentions, places, etc.)
    reactions_json  TEXT,                        -- JSON object of emoji_id -> [user_id, ...]

Indexes: idxmessageschannelcreated on (channelid, createdat), idxmessagessender on senderid, idxmessagesdeleted on deleted (partial index). Retention: Durable or configurable per-channel auto-delete. Encryption: none (channel messages are server-readable; DM messages use separate tables). Access: channel members read; sender edits/deletes; server admin may delete any.

Full-text search: a FTS5 virtual table messagesfts is created on (content) with the unicode61 tokenizer, linked to messages(messageid, content).

Prior art: StateMessage at wabistatebridge/src/lib.rs:98-109, which includes senderid, channelid, createdat, deleted, deletedat, and a row_json blob. WabiDB replaces the blob with typed columns.

Table: reactions

    reaction_id     TEXT PRIMARY KEY,
    message_id      TEXT NOT NULL REFERENCES messages(message_id),
    channel_id      TEXT NOT NULL,
    user_id         INTEGER NOT NULL,
    emoji_id        TEXT NOT NULL,
    created_at      INTEGER NOT NULL

Indexes: idxreactionsmessage on messageid, idxreactionsuser on userid. Retention: Durable. Access: any member reads; user creates/deletes own.

Prior art: StateReaction at wabistatebridge/src/lib.rs:506-515.

3.4 DM CONVERSATIONS AND DM MESSAGES

Table: dm_conversations

    conversation_id TEXT PRIMARY KEY,            -- canonical: "{user_a}:{user_b}" where a < b
    user_a          INTEGER NOT NULL REFERENCES users(id),
    user_b          INTEGER NOT NULL REFERENCES users(id),
    created_at      INTEGER NOT NULL,
    key_id          TEXT,                        -- opaque conversation key identifier (not the key)
    active          INTEGER NOT NULL DEFAULT 1,
    UNIQUE(user_a, user_b)

Indexes: idxdmusera, idxdmuserb. Retention: Durable. Encryption: conversation key id stored on server, keys never stored. Access: only the two participants read/write.

Table: dm_messages

    message_id      TEXT PRIMARY KEY,
    conversation_id TEXT NOT NULL REFERENCES dm_conversations(conversation_id),
    sender_id       INTEGER NOT NULL,
    recipient_id    INTEGER NOT NULL,
    created_at      INTEGER NOT NULL,
    ciphertext_a    BLOB NOT NULL,              -- encrypted for user_a
    nonce_a         BLOB NOT NULL,
    ciphertext_b    BLOB NOT NULL,              -- encrypted for user_b (same content, different key)
    nonce_b         BLOB NOT NULL,
    deleted         INTEGER NOT NULL DEFAULT 0,
    deleted_at      INTEGER

Indexes: idxdmmsgsconversation on conversationid. Retention: Durable (optionally with per-user auto-delete policy). Encryption: each message is encrypted once per recipient using AES-GCM with per-recipient derived keys. The server stores both ciphertexts. It cannot decrypt either. Access: only the two participants access (filtered by senderid/recipientid match on read).

Table: friends

    friendship_id   INTEGER PRIMARY KEY AUTOINCREMENT,
    user_a          INTEGER NOT NULL REFERENCES users(id),
    user_b          INTEGER NOT NULL REFERENCES users(id),
    tier            TEXT NOT NULL DEFAULT 'friend',   -- friend, close_friend, family, acquaintance
    status          TEXT NOT NULL DEFAULT 'pending',  -- pending, accepted, blocked
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    UNIQUE(user_a, user_b)

Retention: Durable. Access: both users read/write.

Table: friend_requests

    request_id      INTEGER PRIMARY KEY AUTOINCREMENT,
    from_user       INTEGER NOT NULL REFERENCES users(id),
    to_user         INTEGER NOT NULL REFERENCES users(id),
    message         TEXT,
    created_at      INTEGER NOT NULL,
    status          TEXT NOT NULL DEFAULT 'pending'

Retention: Durable until accepted/declined. Access: fromuser creates; touser accepts/declines.

3.5 CALLS

Table: call_sessions

    session_id      TEXT PRIMARY KEY,
    channel_id      TEXT NOT NULL,
    call_type       TEXT NOT NULL,              -- voice, video, screen_share
    host_user_id    INTEGER,
    started_at      INTEGER NOT NULL,
    ended_at        INTEGER,
    transport       TEXT NOT NULL DEFAULT 'webrtc',  -- webrtc, livekit, sfu
    max_participants INTEGER NOT NULL DEFAULT 0,     -- 0 = unlimited
    active          INTEGER NOT NULL DEFAULT 1

Indexes: idxcallsessionschannel on channelid. Retention: Durable (for history/records). Encryption: metadata only. Access: channel members read; host manages.

Prior art: StateCallSession at wabicallstate_bridge/src/lib.rs:4-17.

Table: call_participants

    participant_key TEXT PRIMARY KEY,            -- "{session_id}:{user_id}"
    session_id      TEXT NOT NULL REFERENCES call_sessions(session_id),
    user_id         INTEGER NOT NULL,
    stable_user_id  TEXT NOT NULL,
    joined_at       INTEGER NOT NULL,
    left_at         INTEGER,
    is_host         INTEGER NOT NULL DEFAULT 0,
    muted           INTEGER NOT NULL DEFAULT 0,
    video_enabled   INTEGER NOT NULL DEFAULT 0

Indexes: idxcpsession on session_id. Retention: Session-scoped. Access: channel members read; self joins/leaves.

Prior art: StateCallParticipant at wabicallstate_bridge/src/lib.rs:20-34.

Table: call_signals (event table — ephemeral, not persisted)

This table uses a special WabiDB mechanism (the ephemeral tier): rows are broadcast in the current transaction but never written to SQLite. Schema:

    session_id       TEXT NOT NULL,
    signal_type      TEXT NOT NULL,              -- offer, answer, ice-candidate
    target_user_id   INTEGER,
    payload          TEXT NOT NULL,              -- JSON signal payload
    created_at       INTEGER NOT NULL

Retention: Ephemeral (never committed). Access: all session participants receive during the transaction; no historical access.

Prior art: StateCallSignal at wabicallstate_bridge/src/lib.rs:36-43, declared as an STDB event table.

3.6 WHITEBOARDS

Table: whiteboards

    board_id        TEXT PRIMARY KEY,
    scope_type      TEXT NOT NULL,               -- channel
    scope_id        TEXT NOT NULL,               -- channel_id
    version         INTEGER NOT NULL DEFAULT 0,
    is_private      INTEGER NOT NULL DEFAULT 0,
    created_by      TEXT,
    updated_by      TEXT,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL

Indexes: idxwhiteboardsscope on (scopetype, scopeid). Retention: Durable. Access: channel members read; all members create/update.

Prior art: StateWhiteboard at wabistatebridge/src/lib.rs:441-456.

Table: whiteboard_layers

    layer_id        TEXT NOT NULL,
    board_id        TEXT NOT NULL REFERENCES whiteboards(board_id),
    name            TEXT NOT NULL,
    kind            TEXT NOT NULL,               -- content, reference, background
    visible         INTEGER NOT NULL DEFAULT 1,
    locked          INTEGER NOT NULL DEFAULT 0,
    opacity         REAL NOT NULL DEFAULT 1.0,
    sort_order      INTEGER NOT NULL DEFAULT 0,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    PRIMARY KEY (board_id, layer_id)

Retention: Durable. Access: same as whiteboard.

The frontend defines WhiteboardLayer with fields id, name, kind, visible, locked, opacity, order, createdAt, updatedAt at whiteboard/boardTypes.ts:11-21. The kind is WhiteboardLayerKind = 'content' | 'reference' | 'background' (boardTypes.ts:3). Every field maps directly to a column.

Table: whiteboard_elements

    element_id      TEXT NOT NULL,
    board_id        TEXT NOT NULL REFERENCES whiteboards(board_id),
    layer_id        TEXT NOT NULL,
    type            TEXT NOT NULL,               -- stroke, line, rect, ellipse, arrow, text, image
    x               REAL NOT NULL,
    y               REAL NOT NULL,
    width           REAL NOT NULL DEFAULT 0,
    height          REAL NOT NULL DEFAULT 0,
    rotation        REAL NOT NULL DEFAULT 0,
    z_index         INTEGER NOT NULL DEFAULT 0,
    opacity         REAL NOT NULL DEFAULT 1.0,
    stroke_color    TEXT,
    stroke_width    REAL NOT NULL DEFAULT 2,
    fill_color      TEXT,
    created_by      TEXT,
    updated_at      INTEGER NOT NULL,
    locked          INTEGER NOT NULL DEFAULT 0,
    points_json     TEXT,                        -- for stroke elements (JSON array of {x,y,pressure})
    text            TEXT,                        -- for text elements
    font_size       REAL,                        -- for text elements
    font_family     TEXT,
    text_align      TEXT,
    border_radius   REAL,                        -- for rect elements
    arrow_head      TEXT,                        -- end, both, none
    image_src       TEXT,                        -- for image elements
    image_asset_id  TEXT,
    image_mime      TEXT,
    image_natural_w INTEGER,
    image_natural_h INTEGER,
    PRIMARY KEY (board_id, element_id)

Retention: Durable. Access: same as whiteboard.

The element shape types and their fields derive directly from elementTypes.ts:36-76 (StrokeElement, LineElement, RectElement, EllipseElement, ArrowElement, TextElement, ImageElement), each with ElementBase fields id, type, x, y, width, height, rotation, zIndex, layerId, opacity, strokeColor, strokeWidth, fillColor, createdBy, updatedAt, locked (elementTypes.ts:17-34).

Table: whiteboard_patches (append-only change log)

    patch_id        INTEGER PRIMARY KEY AUTOINCREMENT,
    board_id        TEXT NOT NULL REFERENCES whiteboards(board_id),
    user_id         TEXT NOT NULL,
    op              TEXT NOT NULL,               -- create, update, delete, reorder, layer:*, replace
    payload_json    TEXT NOT NULL,               -- the patch body
    created_at      INTEGER NOT NULL

Indexes: idxwbpatchesboardcreated on (boardid, createdat). Retention: Durable (or configurable trim window). This is the append-only log that enables collaborative editing: remote clients apply patches to stay in sync. The frontend patch operations at boardSync.ts:19-53 (emitCreatePatch, emitUpdatePatch, emitDeletePatch, emitLayerCreatePatch, etc.) correspond directly to rows in this table.

3.7 MAPS AND PLACES

Table: places

    place_id        TEXT PRIMARY KEY,
    slug            TEXT NOT NULL UNIQUE,
    name            TEXT NOT NULL,
    aliases_json    TEXT,                        -- JSON array of alias strings
    building        TEXT,
    floor           TEXT,
    lat             REAL,
    lon             REAL,
    description     TEXT,
    model_url       TEXT,
    poi_theme_preset TEXT DEFAULT 'classic',     -- classic, campus, quest, terminal
    tags_json       TEXT,                        -- JSON array
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL

Indexes: idxplacesslug. Retention: Durable. Access: all members read; server admin creates/edits/deletes.

The frontend PlaceRecord at placeRegistry.ts:17-34 carries all these fields plus mapLayers, pois, mapImageUrl, mapRotation.

Table: place_layers

    layer_id        TEXT NOT NULL,
    place_id        TEXT NOT NULL REFERENCES places(place_id),
    name            TEXT NOT NULL,
    floor           TEXT,
    image_url       TEXT NOT NULL,
    rotation        REAL DEFAULT 0,
    PRIMARY KEY (place_id, layer_id)

Retention: Durable. Access: same as place.

Frontend PlaceMapLayerRecord at placeRegistry.ts:40-46 has id, name, floor, imageUrl, rotation.

Table: place_pois

    poi_id          TEXT NOT NULL,
    place_id        TEXT NOT NULL REFERENCES places(place_id),
    name            TEXT NOT NULL,
    x               REAL NOT NULL,
    y               REAL NOT NULL,
    layer_id        TEXT,
    description     TEXT,
    render_mode     TEXT NOT NULL DEFAULT 'both',    -- label, pin, both
    theme_preset    TEXT,
    icon_preset     TEXT DEFAULT 'pin',
    icon_glyph      TEXT,
    icon_color      TEXT,
    PRIMARY KEY (place_id, poi_id)

Retention: Durable. Access: same as place.

Frontend PlacePoiRecord at placeRegistry.ts:48-60 has id, name, x, y, layerId, description, renderMode, themePreset, iconPreset, iconGlyph, iconColor.

Table: place_drafts

    draft_id        TEXT PRIMARY KEY,
    place_id        TEXT,                        -- null if new place
    draft_json      TEXT NOT NULL,               -- serialized PlaceDraft
    created_by      INTEGER NOT NULL,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL

Retention: Session-scoped (auto-deleted after 7 days). Access: owner reads/writes; server admin may delete.

Table: place_assets

    asset_id        TEXT PRIMARY KEY,
    place_id        TEXT NOT NULL REFERENCES places(place_id),
    url             TEXT NOT NULL,
    mime_type       TEXT,
    file_size       INTEGER,
    uploaded_by     INTEGER NOT NULL,
    uploaded_at     INTEGER NOT NULL

Retention: Durable. Access: all members read; server admin/uploads.

3.8 NOTES

Table: notes

    note_id         TEXT PRIMARY KEY,
    owner_id        INTEGER NOT NULL REFERENCES users(id),
    scope_type      TEXT NOT NULL,               -- keep, dm, scratchpad
    scope_id        TEXT,                        -- dm channel id or null for keep
    text            TEXT NOT NULL DEFAULT '',
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL

Indexes: idxnotesownerscope on (ownerid, scopetype, scopeid). Retention: Durable. Access: owner reads/writes.

The frontend notes system at notesStore.ts:3-8 defines LocalNote with id, text, createdAt, updatedAt — all replicated here. Scope types correspond to KEEPNOTESPREFIX, DMNOTESPREFIX, QUICKSCRATCHPADPREFIX at notesStore.ts:10-12 — but stored server-side rather than in localStorage.

Table: note_versions

    version_id      INTEGER PRIMARY KEY AUTOINCREMENT,
    note_id         TEXT NOT NULL REFERENCES notes(note_id),
    text            TEXT NOT NULL,
    saved_at        INTEGER NOT NULL

Indexes: idxnvnote on note_id. Retention: Durable (keep last 50 versions per note). Access: owner reads.

3.9 BUSINESS: KANBAN, TASKS, PROJECTS, SPRINTS, CALENDAR, DIARY

Table: kanban_boards

    board_id        TEXT PRIMARY KEY,
    name            TEXT NOT NULL,
    scope_type      TEXT NOT NULL,
    scope_id        TEXT NOT NULL,
    created_by      INTEGER NOT NULL,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL

Retention: Durable. Access: members read; owner manages.

Table: kanban_tasks

    task_id         TEXT PRIMARY KEY,
    board_id        TEXT NOT NULL REFERENCES kanban_boards(board_id),
    title           TEXT NOT NULL,
    description     TEXT,
    status          TEXT NOT NULL DEFAULT 'todo',    -- ideas, todo, in_progress, done, scrapped, archived
    priority        TEXT NOT NULL DEFAULT 'medium',  -- low, medium, high, urgent
    estimated_min   INTEGER,
    due_date        INTEGER,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    created_by      TEXT NOT NULL,
    assigned_to     TEXT,
    project_id      TEXT,
    completed_at    INTEGER,
    sort_order      INTEGER

Indexes: idxktboardstatus on (boardid, status), idxktassigned on assigned_to. Retention: Durable. Access: board members read; assigned user and creator update.

The frontend Todo type at shared/businessContracts.ts:27-44 has all these fields: id, title, description, status, priority, estimatedMinutes, dueDate, createdAt, updatedAt, createdBy, assignedTo, tags, projectId, completedAt.

Table: kanban_columns

    board_id        TEXT NOT NULL REFERENCES kanban_boards(board_id),
    column_id       TEXT NOT NULL,               -- ideas, todo, in_progress, done, scrapped, archived
    label           TEXT NOT NULL,
    color           TEXT NOT NULL,
    visible         INTEGER NOT NULL DEFAULT 1,
    sort_order      INTEGER NOT NULL DEFAULT 0,
    PRIMARY KEY (board_id, column_id)

Frontend KanbanColumn at business/types.ts:33-38: id, label, color, visible.

Table: projects

    project_id      TEXT PRIMARY KEY,
    name            TEXT NOT NULL,
    description     TEXT,
    color           TEXT NOT NULL DEFAULT '#6366f1',
    created_by      TEXT NOT NULL,
    created_at      INTEGER NOT NULL,
    start_date      INTEGER,
    target_end_date INTEGER,
    status          TEXT NOT NULL DEFAULT 'planning',  -- planning, active, paused, completed, cancelled
    parent_id       TEXT,
    visibility      TEXT DEFAULT 'public'

Frontend Project at shared/businessContracts.ts:82-95.

Table: sprints

    sprint_id       TEXT PRIMARY KEY,
    project_id      TEXT NOT NULL REFERENCES projects(project_id),
    name            TEXT NOT NULL,
    start_date      INTEGER NOT NULL,
    end_date        INTEGER NOT NULL,
    goals_json      TEXT,                        -- JSON array of goal strings
    status          TEXT NOT NULL DEFAULT 'planned'  -- planned, active, completed

Frontend Sprint at shared/businessContracts.ts:97-108.

Table: calendar_events

    event_id        TEXT PRIMARY KEY,
    title           TEXT NOT NULL,
    description     TEXT,
    start_date      INTEGER NOT NULL,
    end_date        INTEGER,
    all_day         INTEGER NOT NULL DEFAULT 0,
    color           TEXT,
    created_by      TEXT NOT NULL,
    recurring_json  TEXT,                        -- JSON recurring rule (frequency, interval, endDate)
    reminders_json  TEXT,                        -- JSON array of reminder offsets
    cancelled_dates_json TEXT,
    visibility      TEXT DEFAULT 'public'

Frontend CalendarEvent at shared/businessContracts.ts:52-66.

Table: diary_entries

    entry_id        TEXT PRIMARY KEY,
    date            INTEGER NOT NULL,
    content         TEXT NOT NULL,
    mood            TEXT,
    images_json     TEXT,
    tags_json       TEXT,
    created_by      TEXT NOT NULL,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    is_private      INTEGER NOT NULL DEFAULT 0

Frontend DiaryEntry at shared/businessContracts.ts:68-80.

Table: business_resources

    resource_id     TEXT PRIMARY KEY,
    type            TEXT NOT NULL,               -- brush, code, image, url, note, file, youtube, video, pdf, audio, ebook, document, archive
    name            TEXT NOT NULL,
    description     TEXT,
    storage_type    TEXT NOT NULL,               -- inline, upload, external
    content         TEXT,
    file_url        TEXT,
    external_url    TEXT,
    file_size       INTEGER,
    mime_type       TEXT,
    preview         TEXT,
    tags_json       TEXT,
    created_by      TEXT NOT NULL,
    created_at      INTEGER NOT NULL,
    updated_at      INTEGER NOT NULL,
    is_encrypted    INTEGER NOT NULL DEFAULT 0,
    visibility_type TEXT DEFAULT 'public',
    min_role        TEXT,
    workspace_id    TEXT

Frontend Resource at shared/businessContracts.ts:110-131.

Table: business_tags

    tag_id          TEXT PRIMARY KEY,
    name            TEXT NOT NULL,
    color           TEXT NOT NULL,
    created_at      INTEGER NOT NULL

Table: businessgraphedges

    edge_id         TEXT PRIMARY KEY,
    source_id       TEXT NOT NULL,
    target_id       TEXT NOT NULL,
    edge_type       TEXT NOT NULL,               -- contains, depends_on, references, tagged_with, related_to, inspired_by
    label           TEXT,
    weight          REAL,
    created_at      INTEGER NOT NULL,
    created_by      TEXT NOT NULL

Indexes: idxbgesource, idxbgetarget. Retention: Durable.

3.10 USER SETTINGS

Table: user_settings

    user_id             INTEGER PRIMARY KEY REFERENCES users(id),
    offline_retention   TEXT NOT NULL DEFAULT 'forever',
    allow_temp_messages INTEGER NOT NULL DEFAULT 0,
    business_private    INTEGER NOT NULL DEFAULT 0,
    home_experience     TEXT DEFAULT 'default'

Table: theme_preferences

    user_id             INTEGER PRIMARY KEY REFERENCES users(id),
    theme_id            TEXT NOT NULL DEFAULT 'dark',
    custom_theme_json   TEXT,
    uniform_font        INTEGER NOT NULL DEFAULT 0,
    font_family         TEXT DEFAULT 'Inter',
    font_size           TEXT DEFAULT '16',
    font_weight         TEXT DEFAULT '400',
    font_style          TEXT DEFAULT 'normal'

Table: layout_preferences

    user_id             INTEGER PRIMARY KEY REFERENCES users(id),
    layout_json         TEXT NOT NULL,           -- serialized layout state
    updated_at          INTEGER NOT NULL

Prior art: StateThemePreferences at wabistatebridge/src/lib.rs:240-253 and StateLayoutPreferences at lib.rs:256-262.

3.11 SERVER SETTINGS

Table: app_settings

    setting_key         TEXT PRIMARY KEY,
    value               TEXT NOT NULL,
    updated_at          INTEGER NOT NULL

Prior art: StateAppSetting at wabistatebridge/src/lib.rs:341-349.

3.12 ENCRYPTION KEY REGISTRY

Table: encryption_keys

    user_id             INTEGER PRIMARY KEY REFERENCES users(id),
    public_key          TEXT NOT NULL,           -- base64-encoded X25519 public key
    created_at          INTEGER NOT NULL,
    updated_at          INTEGER NOT NULL

No private key is stored. The prior-art StateUserEncryptionKey at wabistatebridge/src/lib.rs:429-438 stored both publickey and privatekey_encrypted — WabiDB drops the encrypted private key from the server and moves key escrow to client-side.

3.13 HELPER NODES

Table: helper_nodes

    node_id             TEXT PRIMARY KEY,
    node_type           TEXT NOT NULL,           -- media, search, transcode, cache, anchor
    name                TEXT,
    region              TEXT,
    status              TEXT NOT NULL DEFAULT 'offline',
    api_url             TEXT,
    public_key          TEXT,
    bandwidth_mbps      INTEGER,
    storage_gb          INTEGER,
    metadata_json       TEXT,
    paired_at           INTEGER,
    last_heartbeat      INTEGER,
    approved            INTEGER NOT NULL DEFAULT 0

Prior art: StateRelay at wabistatebridge/src/lib.rs:301-320.

Table: pair_tokens

    token               TEXT PRIMARY KEY,
    node_type           TEXT NOT NULL,
    capabilities_json   TEXT NOT NULL,
    expires_at          INTEGER NOT NULL,
    redeemed            INTEGER NOT NULL DEFAULT 0,
    issued_at           INTEGER NOT NULL

Table: route_tokens

    token_id            TEXT PRIMARY KEY,
    node_id             TEXT NOT NULL REFERENCES helper_nodes(node_id),
    token               TEXT NOT NULL,           -- signed JWT-like token
    capabilities_json   TEXT NOT NULL,
    issued_at           INTEGER NOT NULL,
    expires_at          INTEGER NOT NULL,
    revoked             INTEGER NOT NULL DEFAULT 0

3.14 RETENTION CLASSES

The retention_classes table maps object type to retention policy:

    object_type         TEXT PRIMARY KEY,
    retention_class     TEXT NOT NULL,           -- ephemeral, session, durable, archive
    ttl_seconds         INTEGER,                 -- null if no TTL
    created_at          INTEGER NOT NULL,
    updated_at          INTEGER NOT NULL

3.15 RBAC

Table: role_definitions

    role_key            TEXT PRIMARY KEY,        -- "{workspace_id}:{role_name}"
    workspace_id        TEXT NOT NULL,
    role_name           TEXT NOT NULL,
    display_name        TEXT,
    priority            INTEGER NOT NULL DEFAULT 0,
    color               TEXT,
    is_hoisted          INTEGER NOT NULL DEFAULT 0,
    active              INTEGER NOT NULL DEFAULT 1

Prior art: StateRoleDefinition at wabistatebridge/src/lib.rs:209-222.

Table: rbac_assignments

    assignment_key      TEXT PRIMARY KEY,        -- "{workspace_id}:{user_id}:{role}"
    workspace_id        TEXT NOT NULL,
    user_id             INTEGER NOT NULL,
    role                TEXT NOT NULL,
    assigned_by         INTEGER,
    active              INTEGER NOT NULL DEFAULT 1

Prior art: StateRbacAssignment at wabistatebridge/src/lib.rs:195-206.

============================================================

------------------------------------------------------------
4. THE COMMAND/REDUCER MODEL
------------------------------------------------------------

4.1 HOW COMMANDS ARE DEFINED

Every state mutation in WabiDB goes through a typed Rust function. The shape is:

    pub struct CommandCtx<'a> {
        pub tx: &'a Transaction<'a>,
        pub caller: CallerIdentity,
        pub timestamp: i64,
        pub capabilities: CapabilitySet,
        pub outbox: &'a mut Vec<OutboxRow>,
    }
    
    pub enum CommandError {
        NotFound(String),
        Forbidden(String),
        Validation(String),
        Internal(String),
    }
    
    pub trait Command<T>: Send + Sync {
        fn name(&self) -> &'static str;
        fn execute(&self, ctx: CommandCtx) -> Result<T, CommandError>;
    }

The transaction wrapper does:

    fn run_command<T>(cmd: &dyn Command<T>, caller: CallerIdentity, ...) -> Result<T, CommandError> {
        let tx = storage.begin()?;
        let mut outbox = vec![];
        let result = cmd.execute(CommandCtx { tx: &tx, caller, timestamp, capabilities, outbox: &mut outbox })?;
        // write outbox rows to the outbox table within the same transaction
        for row in &outbox {
            tx.execute("INSERT INTO outbox (topic, payload, created_at) VALUES (?1, ?2, ?3)", ...)?;
        }
        tx.commit()?;
        // notify the subscription manager (non-blocking channel send)
        subscription_manager.notify(outbox);
        Ok(result)
    }

This is the same atomicity guarantee as STDB reducers (crates/core/src/host/module_host.rs): the command either commits everything or rolls back everything, and outbox rows are part of the same transaction.

4.2 COMPLETE COMMAND LIST

Below is every command the Wabi frontend requires, organized by domain. For each: name, input type, output type, who can call it, description, and what it emits to the outbox.

MESSAGING

Command            Input                                  Output       Caller           What it does                             Outbox topic            
-----------------  -------------------------------------  -----------  ---------------  ---------------------------------------  ------------------------
`send_message`     `channel_id, content, reply_to?, ...`  `MessageId`  channel members  Inserts a message row, notifies channel  `channel:{id}:messages` 
`edit_message`     `message_id, content`                  `()`         message sender   Updates message content, sets edited_at  `channel:{id}:messages` 
`delete_message`   `message_id, hard_delete?`             `()`         sender or admin  Soft-deletes (or hard-deletes) message   `channel:{id}:messages` 
`pin_message`      `message_id, channel_id`               `()`         channel mod+     Toggles `is_pinned`                      `channel:{id}:messages` 
`add_reaction`     `message_id, emoji_id`                 `()`         channel members  Inserts reaction row                     `channel:{id}:reactions`
`remove_reaction`  `message_id, emoji_id`                 `()`         reaction owner   Deletes reaction row                     `channel:{id}:reactions`

CHANNELS

Command                    Input                            Output       Caller        What it does                          Outbox topic          
-------------------------  -------------------------------  -----------  ------------  ------------------------------------  ----------------------
`create_channel`           `name, type, description?, ...`  `ChannelId`  server admin  Creates channel + initial member row  `server:announcements`
`archive_channel`          `channel_id`                     `()`         server admin  Sets `archived=true`                  `channel:{id}:meta`   
`delete_channel`           `channel_id`                     `()`         server admin  Hard-deletes channel + all messages   `server:announcements`
`update_channel_settings`  `channel_id, settings`           `()`         server admin  Updates channel metadata              `channel:{id}:meta`   
`add_channel_member`       `channel_id, user_id, role`      `()`         server admin  Inserts channel member                `channel:{id}:members`
`remove_channel_member`    `channel_id, user_id`            `()`         server admin  Sets member active=false              `channel:{id}:members`

DM

Command              Input                                                            Output            Caller                What it does                 Outbox topic                      
-------------------  ---------------------------------------------------------------  ----------------  --------------------  ---------------------------  ----------------------------------
`create_dm`          `target_user_id`                                                 `ConversationId`  any user              Creates dm_conversation row  `user:{a}:inbox`, `user:{b}:inbox`
`send_dm_message`    `conversation_id, ciphertext_a, nonce_a, ciphertext_b, nonce_b`  `MessageId`       conversation members  Inserts dm_message row       `dm:{a}:{b}`                      
`delete_dm_message`  `message_id`                                                     `()`              sender                Soft-deletes                 `dm:{a}:{b}`                      

FRIENDS

Command                   Input                       Output       Caller     What it does                         Outbox topic          
------------------------  --------------------------  -----------  ---------  -----------------------------------  ----------------------
`send_friend_request`     `target_user_id, message?`  `RequestId`  any user   Creates friend_request row           `user:{target}:inbox` 
`accept_friend_request`   `request_id`                `()`         recipient  Creates friendship, deletes request  `user:{sender}:inbox` 
`decline_friend_request`  `request_id`                `()`         recipient  Deletes request                      `user:{sender}:inbox` 
`set_friend_tier`         `user_id, tier`             `()`         friend     Updates friendship.tier              `user:{user_id}:inbox`
`remove_friend`           `user_id`                   `()`         either     Deletes friendship row               `user:{user_id}:inbox`

CALLS

Command                Input                                                Output       Caller           What it does                      Outbox topic       
---------------------  ---------------------------------------------------  -----------  ---------------  --------------------------------  -------------------
`create_call_session`  `channel_id, call_type, max_participants`            `SessionId`  channel members  Inserts call_session row          `channel:{id}:call`
`join_call`            `session_id`                                         `()`         channel members  Inserts/updates call_participant  `channel:{id}:call`
`leave_call`           `session_id`                                         `()`         participant      Sets left_at                      `channel:{id}:call`
`end_call_session`     `session_id`                                         `()`         host             Sets active=false, ended_at       `channel:{id}:call`
`publish_call_signal`  `session_id, signal_type, target_user_id?, payload`  `()`         participants     Writes ephemeral signal row       `call:{session_id}`

WHITEBOARDS

Command                      Input                            Output       Caller           What it does                        Outbox topic             
---------------------------  -------------------------------  -----------  ---------------  ----------------------------------  -------------------------
`create_whiteboard`          `channel_id`                     `BoardId`    channel members  Inserts whiteboard + default layer  `channel:{id}:whiteboard`
`add_whiteboard_element`     `board_id, element`              `ElementId`  channel members  Inserts element row                 `whiteboard:{board_id}`  
`update_whiteboard_element`  `board_id, element_id, changes`  `()`         element creator  Updates element columns             `whiteboard:{board_id}`  
`delete_whiteboard_element`  `board_id, element_ids[]`        `()`         element creator  Deletes element rows                `whiteboard:{board_id}`  
`add_whiteboard_layer`       `board_id, layer`                `LayerId`    channel members  Inserts layer row                   `whiteboard:{board_id}`  
`update_whiteboard_layer`    `board_id, layer_id, changes`    `()`         channel members  Updates layer columns               `whiteboard:{board_id}`  
`delete_whiteboard_layer`    `board_id, layer_id`             `()`         channel members  Deletes layer, reassigns elements   `whiteboard:{board_id}`  
`reorder_whiteboard_layer`   `board_id, layer_id, direction`  `()`         channel members  Reorders layers                     `whiteboard:{board_id}`  
`add_whiteboard_patch`       `board_id, op, payload`          `()`         channel members  Appends to whiteboard_patches       `whiteboard:{board_id}`  
`save_whiteboard_snapshot`   `board_id, document_json`        `()`         channel members  Saves full document snapshot        `whiteboard:{board_id}`  

The frontend patch operations at boardSync.ts:19-53 — emitCreatePatch, emitUpdatePatch, emitDeletePatch, emitLayerCreatePatch, emitLayerUpdatePatch, emitLayerDeletePatch, emitLayerReorderPatch, emitLayerSelectPatch — all map to addwhiteboardpatch with an op field.

PLACES

Command             Input                Output     Caller        What it does                   Outbox topic          
------------------  -------------------  ---------  ------------  -----------------------------  ----------------------
`create_place`      `place_draft`        `PlaceId`  server admin  Inserts place, layers, POIs    `server:announcements`
`update_place`      `place_id, changes`  `()`       server admin  Updates place                  `place:{id}`          
`delete_place`      `place_id`           `()`       server admin  Deletes place + layers + POIs  `server:announcements`
`save_place_draft`  `draft_json`         `()`       any user      Upserts place_draft            `user:{caller}:inbox` 

KANBAN/BUSINESS

Command                  Input                              Output       Caller             What it does                            Outbox topic         
-----------------------  ---------------------------------  -----------  -----------------  --------------------------------------  ---------------------
`create_kanban_board`    `name, scope`                      `BoardId`    workspace members  Inserts kanban_board + default columns  `kanban:{board_id}`  
`add_kanban_task`        `board_id, task`                   `TaskId`     board members      Inserts kanban_task                     `kanban:{board_id}`  
`update_kanban_task`     `task_id, changes`                 `()`         creator/assignee   Updates kanban_task                     `kanban:{board_id}`  
`delete_kanban_task`     `task_id`                          `()`         creator/admin      Deletes kanban_task                     `kanban:{board_id}`  
`move_kanban_task`       `task_id, new_status, sort_order`  `()`         board members      Moves task between columns              `kanban:{board_id}`  
`create_project`         `project`                          `ProjectId`  workspace members  Inserts project                         `kanban:{board_id}`  
`update_project`         `project_id, changes`              `()`         creator            Updates project                         `kanban:{board_id}`  
`create_calendar_event`  `event`                            `EventId`    workspace members  Inserts calendar_event                  `user:{caller}:inbox`
`update_calendar_event`  `event_id, changes`                `()`         creator            Updates calendar_event                  `user:{caller}:inbox`
`create_diary_entry`     `entry`                            `EntryId`    workspace members  Inserts diary_entry                     `user:{caller}:inbox`
`update_diary_entry`     `entry_id, changes`                `()`         creator            Updates diary_entry                     `user:{caller}:inbox`

USERS & AUTH

Command            Input                                   Output      Caller           What it does                       Outbox topic          
-----------------  --------------------------------------  ----------  ---------------  ---------------------------------  ----------------------
`register_user`    `username, password_hash`               `UserId`    unauthenticated  Creates user row, initial session  `server:announcements`
`update_profile`   `user_id, changes`                      `()`        user             Updates user row                   `user:{id}:inbox`     
`delete_user`      `user_id`                               `()`        user or admin    Sets deleted=true                  `server:announcements`
`register_device`  `device_name, device_type, public_key`  `DeviceId`  user             Inserts device row                 `user:{id}:inbox`     
`rotate_key`       `public_key`                            `()`        user             Updates identity_keys              `user:{id}:inbox`     
`set_ingest_key`   `key_sha256_hex, previous_key?`         `()`        server admin     Updates/creates ingest auth        `server:announcements`

SETTINGS

Command                 Input          Output  Caller        What it does                Outbox topic          
----------------------  -------------  ------  ------------  --------------------------  ----------------------
`update_user_settings`  `settings`     `()`    user          Upserts user_settings       `user:{id}:inbox`     
`update_theme`          `preferences`  `()`    user          Upserts theme_preferences   `user:{id}:inbox`     
`update_layout`         `layout_json`  `()`    user          Upserts layout_preferences  `user:{id}:inbox`     
`set_app_setting`       `key, value`   `()`    server admin  Upserts app_settings        `server:announcements`

RBAC

Command        Input                              Output  Caller        What it does             Outbox topic          
-------------  ---------------------------------  ------  ------------  -----------------------  ----------------------
`assign_role`  `workspace_id, user_id, role`      `()`    server admin  Upserts rbac_assignment  `server:announcements`
`remove_role`  `workspace_id, user_id, role`      `()`    server admin  Sets active=false        `server:announcements`
`define_role`  `role_key, name, color, priority`  `()`    server admin  Upserts role_definition  `server:announcements`

MODERATION

Command        Input                               Output  Caller        What it does                       Outbox topic          
-------------  ----------------------------------  ------  ------------  ---------------------------------  ----------------------
`ban_user`     `user_id, reason`                   `()`    server admin  Creates ban row, deactivates user  `server:announcements`
`unban_user`   `user_id`                           `()`    server admin  Deactivates ban                    `server:announcements`
`mute_user`    `user_id, channel_id, expires_at?`  `()`    server admin  Creates mute row                   `server:announcements`
`unmute_user`  `user_id, channel_id`               `()`    server admin  Deactivates mute                   `server:announcements`

RETENTION

Command                 Input                                         Output          Caller        What it does                        Outbox topic          
----------------------  --------------------------------------------  --------------  ------------  ----------------------------------  ----------------------
`set_retention_policy`  `object_type, retention_class, ttl_seconds?`  `()`            server admin  Upserts retention_classes           `server:announcements`
`purge_expired`         (none; scheduled)                             `DeletedCount`  internal      Hard-deletes expired rows, VACUUMs  none                  

HELPER NODES

Command                    Input                                   Output        Caller        What it does                       Outbox topic          
-------------------------  --------------------------------------  ------------  ------------  ---------------------------------  ----------------------
`issue_pair_token`         `node_type, capabilities, ttl_seconds`  `Token`       server admin  Inserts pair_token                 `server:announcements`
`redeem_pair_token`        `token, node_id, public_key`            `RouteToken`  helper node   Creates helper_node + route_token  `server:announcements`
`update_helper_heartbeat`  `node_id, status, metrics`              `()`          helper node   Updates helper_node heartbeat      `server:announcements`
`revoke_route_token`       `node_id`                               `()`          server admin  Revokes route_token                `server:announcements`

4.3 EXAMPLE COMMANDS IN RUST

The code below is illustrative, showing the shape that the implementing engineer would fill in.

    // wabidb/src/commands/messages/send_message.rs
    pub struct SendMessageCommand;
    
    impl Command<MessageId> for SendMessageCommand {
        fn name(&self) -> &'static str { "send_message" }
    
        fn execute(&self, ctx: CommandCtx) -> Result<MessageId, CommandError> {
            let args: SendMessageArgs = ctx.args()?;
            // validate channel membership
            let member_count: i64 = ctx.tx.query_row(
                "SELECT COUNT(*) FROM channel_members WHERE channel_id = ?1 AND user_id = ?2 AND active = 1",
                params![args.channel_id, ctx.caller.user_id()], |row| row.get(0)
            )?;
            if member_count == 0 {
                return Err(CommandError::Forbidden("not a member of this channel".into()));
            }
            // insert message
            let message_id = generate_id();
            ctx.tx.execute(
                "INSERT INTO messages (message_id, channel_id, sender_id, content, created_at)
                 VALUES (?1, ?2, ?3, ?4, ?5)",
                params![message_id, args.channel_id, ctx.caller.user_id(), args.content, ctx.timestamp],
            )?;
            // emit outbox row
            ctx.outbox.push(OutboxRow {
                topic: format!("channel:{}:messages", args.channel_id),
                payload: encode_to_bsatn(&MessageCreated { message_id, channel_id, ... }),
                created_at: ctx.timestamp,
            });
            Ok(message_id)
        }
    }

    // wabidb/src/commands/whiteboards/add_whiteboard_patch.rs
    pub struct AddWhiteboardPatchCommand;
    
    impl Command<()> for AddWhiteboardPatchCommand {
        fn name(&self) -> &'static str { "add_whiteboard_patch" }
    
        fn execute(&self, ctx: CommandCtx) -> Result<(), CommandError> {
            let args: WhiteboardPatchArgs = ctx.args()?;
            let board_id = &args.board_id;
            // verify board exists
            let exists: bool = ctx.tx.query_row(
                "SELECT 1 FROM whiteboards WHERE board_id = ?1",
                params![board_id], |_| true
            )?.unwrap_or(false);
            if !exists {
                return Err(CommandError::NotFound("whiteboard not found".into()));
            }
            // insert patch row
            ctx.tx.execute(
                "INSERT INTO whiteboard_patches (board_id, user_id, op, payload_json, created_at)
                 VALUES (?1, ?2, ?3, ?4, ?5)",
                params![board_id, ctx.caller.user_id(), args.op, args.payload_json, ctx.timestamp],
            )?;
            ctx.outbox.push(OutboxRow {
                topic: format!("whiteboard:{}", board_id),
                payload: encode_to_bsatn(&WhiteboardPatchBroadcast { board_id, ... }),
                created_at: ctx.timestamp,
            });
            Ok(())
        }
    }

    // wabidb/src/commands/dm/send_dm_message.rs
    pub struct SendDmMessageCommand;
    
    impl Command<MessageId> for SendDmMessageCommand {
        fn name(&self) -> &'static str { "send_dm_message" }
    
        fn execute(&self, ctx: CommandCtx) -> Result<MessageId, CommandError> {
            let args: SendDmArgs = ctx.args()?;
            let user_id = ctx.caller.user_id();
            // verify caller is part of the conversation
            let conv = ctx.tx.query_row(
                "SELECT user_a, user_b FROM dm_conversations WHERE conversation_id = ?1",
                params![args.conversation_id], |row| Ok((row.get::<_, i64>(0)?, row.get::<_, i64>(1)?))
            )?.ok_or(CommandError::NotFound("conversation not found".into()))?;
            if user_id != conv.0 && user_id != conv.1 {
                return Err(CommandError::Forbidden("not a participant".into()));
            }
            let recipient = if user_id == conv.0 { conv.1 } else { conv.0 };
            let message_id = generate_id();
            ctx.tx.execute(
                "INSERT INTO dm_messages (message_id, conversation_id, sender_id, recipient_id,
                 ciphertext_a, nonce_a, ciphertext_b, nonce_b, created_at)
                 VALUES (?1, ?2, ?3, ?4, ?5, ?6, ?7, ?8, ?9)",
                params![message_id, args.conversation_id, user_id, recipient,
                        args.ciphertext_a, args.nonce_a, args.ciphertext_b, args.nonce_b, ctx.timestamp],
            )?;
            ctx.outbox.push(OutboxRow {
                topic: format!("dm:{}", args.conversation_id),
                payload: encode_to_bsatn(&DmMessageBroadcast { message_id, ... }),
                created_at: ctx.timestamp,
            });
            Ok(message_id)
        }
    }

4.4 TRANSACTION WRAPPER

The run_command function shown in 4.1 is the single entry point for all mutations. It guarantees: (1) the SQLite transaction is open, (2) the command receives exclusive access, (3) outbox rows are written in the same transaction, (4) on success, the transaction commits and the outbox consumer is notified, (5) on failure, the transaction rolls back and no outbox rows are emitted.

============================================================

------------------------------------------------------------
5. THE LIVE SUBSCRIPTION MODEL
------------------------------------------------------------

5.1 TOPIC GRAMMAR

The subscription grammar is a closed set of topic strings and filters. There is no SQL subset, no query compilation, no query hash indexing. Topics are defined as a Rust enum:

    #[derive(Debug, Clone, Hash, Eq, PartialEq)]
    pub enum Topic {
        /// All messages in a specific channel
        ChannelMessages(String),         // "channel:{channel_id}:messages"
        /// Channel metadata changes
        ChannelMeta(String),             // "channel:{channel_id}:meta"
        /// Channel member changes
        ChannelMembers(String),          // "channel:{channel_id}:members"
        /// Reactions in a channel
        ChannelReactions(String),        // "channel:{channel_id}:reactions"
        /// DM conversation
        DmConversation(String),          // "dm:{canonical_conversation_id}"
        /// User's personal inbox
        UserInbox(i64),                  // "user:{user_id}:inbox"
        /// All events in a whiteboard
        Whiteboard(String),              // "whiteboard:{board_id}"
        /// All changes to a place
        Place(String),                   // "place:{place_id}"
        /// All changes in a kanban board
        KanbanBoard(String),             // "kanban:{board_id}"
        /// Call signaling
        Call(String),                    // "call:{session_id}"
        /// Server-wide announcements
        ServerAnnouncements,             // "server:announcements"
    }

In BNF form:

    topic         = channel-topic | dm-topic | user-topic | whiteboard-topic
                  | place-topic | kanban-topic | call-topic | server-topic
    channel-topic = "channel:" channel-id ":" ("messages" | "meta" | "members" | "reactions")
    dm-topic      = "dm:" conversation-id
    user-topic    = "user:" user-id ":" "inbox"
    whiteboard-topic = "whiteboard:" board-id
    place-topic   = "place:" place-id
    kanban-topic  = "kanban:" board-id
    call-topic    = "call:" session-id
    server-topic  = "server:announcements"

5.2 FILTER GRAMMAR

Filters are a closed set of operations, not arbitrary expressions:

    filter          = include-clause | exclude-clause
    include-clause  = "+" topic-pattern
    exclude-clause  = "-" topic-pattern
    topic-pattern   = topic (where topic-id may be "*" for wildcard)

Examples:
* `+channel:general:messages` — subscribe to messages in channel "general"
* `+channel:*:messages` — subscribe to messages in all channels
* `+whiteboard:board-abc123` — subscribe to whiteboard changes
* `-channel:noisy:messages` — unsubscribe from a specific noisy channel

5.3 THE OUTBOX TABLE SCHEMA

    CREATE TABLE outbox (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        topic TEXT NOT NULL,           -- the topic string (e.g. "channel:general:messages")
        payload BLOB NOT NULL,         -- BSATN-encoded payload
        created_at INTEGER NOT NULL,   -- micros since epoch
        broadcast_at INTEGER           -- set when the consumer has sent it to subscribers
    );
    CREATE INDEX idx_outbox_unsent ON outbox(broadcast_at) WHERE broadcast_at IS NULL;

5.4 THE CONSUMER TASK

A background tokio task (the "outbox consumer") runs a loop:

    loop {
        // poll for unsent rows (batched)
        let rows = tx.query("SELECT * FROM outbox WHERE broadcast_at IS NULL ORDER BY id LIMIT 100");
        for row in rows {
            // deliver to matching subscribers
            subscription_manager.deliver(row.topic, row.payload, row.created_at);
            // mark as broadcast
            tx.execute("UPDATE outbox SET broadcast_at = ?1 WHERE id = ?2", params![now, row.id]);
        }
        // adaptive sleep: linger 5ms if batch was full, 25ms if batch was small
        sleep(if rows.len() >= 100 { 5 } else { 25 });
    }

This replaces STDB's delta evaluation pipeline (evalupdatessequential at modulesubscriptionmanager.rs:1346) with the simpler approach of: commands write outbox rows, subscriber patterns match, consumer delivers. No query hashing, no join edge index, no delta computation. Command authors are responsible for writing the right outbox rows.

5.5 WEBSOCKET FRAME FORMAT

The primary frame format is BSATN (borrowing the STDB binary encoding concept but using a simpler bincode or postcard encoding). A debugging JSON format is also supported when the connection negotiates ?format=json.

Frame envelope:

    enum ClientMessage {
        Subscribe { filters: Vec<String> },
        Unsubscribe { filters: Vec<String> },
        Command { name: String, args: Vec<u8>, id: u64 },      // BSATN-encoded args
    }
    
    enum ServerMessage {
        Subscribed { filters: Vec<String>, snapshot: Vec<(Topic, Vec<u8>)> },
        Unsubscribed { filters: Vec<String> },
        Update { topic: String, payload: Vec<u8>, created_at: i64 },
        CommandResult { id: u64, result: Result<Vec<u8>, CommandError> },
        Backpressure { level: u8 },
        Error { code: String, message: String },
    }

Example JSON frames (for debugging):

    // client -> server
    {"type":"subscribe","filters":["+channel:general:messages","+whiteboard:w-abc"]}
    
    // server -> client
    {"type":"update","topic":"channel:general:messages","payload":{"message_id":"msg-123","content":"hello"},"created_at":1750000000000000}
    
    // server -> client (initial snapshot)
    {"type":"subscribed","filters":["+whiteboard:w-abc"],"snapshot":[{"topic":"whiteboard:w-abc","payload":[{...all elements...}]}]}

5.6 PER-CONNECTION TASK AND FILTERING

When a WebSocket connects:

1. Authentication (JWT token from query string).
2. The connection handler spawns a per-connection task that reads `ClientMessage` frames from the WebSocket.
3. Subscribe/Unsubscribe messages update the connection's `TopicFilter` set in the `SubscriptionManager`.
4. The `SubscriptionManager.deliver()` method iterates every connected subscriber, tests `topic_matches_filter(topic, filter)`, and if matched enqueues the payload on the subscriber's mpsc channel.
5. The per-connection task reads from its receiver and writes frames to the WebSocket.

5.7 ADAPTIVE BATCHING AND BACKPRESSURE

The SendWorker (owned by the SubscriptionManager, not per-connection) aggregates updates per-client from the delivery pipeline. It implements:

* **Adaptive batching**: if messages arrive faster than 200/s, linger 5ms to batch; if slower, linger 25ms to reduce latency.
* **Backpressure**: each subscriber's send channel is a bounded mpsc (capacity 256). When full, a `Backpressure` frame is sent. If the channel stays full for 5 seconds, the connection is dropped with `"backpressure_timeout"`.

5.8 COMPARED TO STDB

STDB's subscription system at crates/core/src/subscription/modulesubscriptionmanager.rs is a sophisticated delta-evaluation engine: query compilation, query hash caching, SearchArguments indexing by (TableId, ColId, Value), join edges, a SendWorker actor. WabiDB's system replaces all of this with the topic-outbox pattern. The cost is that WabiDB commands must explicitly write outbox rows — they cannot do a "generic SELECT * subscription" on any table. The benefit is the elimination of ~15,000 lines of query engine code and the entire SQL compiler. For a known set of subscription patterns (channel messages, whiteboard patches, call signals), the topic-outbox pattern is sufficient and far simpler.

5.9 FTS5 AND SEARCH HELPER INTEGRATION

WabiDB uses SQLite FTS5 for built-in full-text search on messages. The FTS5 virtual table is kept in sync with the base table by triggers. The FTS5 index lives in the same SQLite file as the committed state.

For more sophisticated search (typo tolerance, semantic search, federated search across many channels, search-as-you-type with suggestions), a search helper node may be deployed. The integration with a search helper is defined as follows:

* **Outbox topic**: the engine exposes a `search:updates` outbox topic. Every state change that affects a searchable table (messages, notes, kanban tasks, calendar events, etc.) writes a row to this topic with the diff and the originating table.
* **Helper subscription**: the search helper node subscribes to `search:updates` via the same subscription mechanism as any client, with a special token that grants server-side read access.
* **Helper index**: the search helper maintains its own index (e.g., Meilisearch, Tantivy, or a custom Rust index). It applies the diff from each `search:updates` row to its index.
* **Search queries**: the frontend's search UI sends a query to the helper (via the helper's API, not via WabiDB). The helper returns ranked results. If the helper is down or the query is simple, the frontend falls back to the built-in FTS5 query against the engine.

This pattern means the search helper is a true read-replica of the search-relevant state. It does not need to read the SQLite file directly. It does not need its own STDB-style subscription query engine. It just consumes the diff stream and applies it to its own index.

The tradeoff: there is a brief window (typically <500ms) where the helper's index lags behind the engine's committed state. For "search as I type" this is invisible. For "find me the message I just sent" the frontend can use the built-in FTS5 query against the engine as a fallback.

For addons that need search integration (e.g., the CAD addon's comment search), the addon declares a search:addon:{addon_id}:updates topic in its manifest. The engine routes changes to that topic. The search helper subscribes to all search:addon:* topics and indexes them.

============================================================

------------------------------------------------------------
6. THE ENCRYPTION MODEL
------------------------------------------------------------

6.1 IDENTITY KEYPAIRS

Every Wabi user generates an identity keypair on their first device. The keypair is:

* **X25519** for key exchange (or ECDH P-256, matching the existing `encryption.ts:10-39` which uses `ECDH P-256`).
* **Ed25519** for message signing (not implemented yet in the frontend, but provisioned in the schema).

The public key is uploaded to WabiDB via the registerdevice or rotatekey command. The private key never leaves the device. The frontend already implements this pattern at encryption.ts:10-39 with generateKeyPair() using Web Crypto API's ECDH P-256.

6.2 PER-DM-CONVERSATION KEY DERIVATION

When user A wants to send a DM to user B:

1. A fetches B's public key from WabiDB.
2. A derives a shared secret using ECDH: `shared = ecdh(A.private, B.public)`.
3. A derives an AES-256-GCM key from the shared secret via HKDF.
4. A encrypts the message content: `ciphertext, iv = AES-256-GCM.encrypt(content, derived_key)`.
5. A sends the `ciphertext` and `iv` to WabiDB.

When B wants to read the message:

1. B downloads the `ciphertext` and `iv` from WabiDB.
2. B derives the same shared secret: `shared = ecdh(B.private, A.public)`.
3. B derives the same AES key and decrypts.

This is already exactly how the frontend works at encryption.ts:44-91 (deriveSharedKey) and encryption.ts:96-127 (encryptContent/decryptContent).

6.3 PER-RECIPIENT ENCRYPTION

For each DM message, the sender encrypts the content once per recipient and sends both ciphertexts to the server. The server stores both in the dmmessages table (ciphertexta/noncea and ciphertextb/nonce_b). This is the approach already used by Signal and WhatsApp. The server cannot decrypt either ciphertext.

6.4 MULTI-DEVICE KEY ESCROW

For users with multiple devices, a key escrow mechanism is needed so that device 2 can derive the same conversation keys that device 1 used.

The design: when the user sets a passphrase on their account, a wrapped copy of the private key is stored as an encrypted blob on the server. The wrapping key is derived from the passphrase using Argon2. On a new device, the user enters the passphrase, the server returns the wrapped blob, the client unwraps it, and the client now has the full identity key. The frontend's existing encryptForUsers at encryption.ts:164-182 already supports per-recipient key derivation — this is the pattern extended to cover the identity key itself.

The server stores only: wrappedprivatekeyblob (opaque bytes), salt (for Argon2), and keyid (opaque identifier). It cannot unwrap the blob.

The UI must display an honest tradeoff: "Your passphrase is the only way to recover your encryption keys. Wabi cannot reset it. If you forget your passphrase, your existing DMs become unreadable on new devices."

6.5 WHAT THE SERVER STORES

* **Public keys** in `identity_keys` and `devices` tables (always public).
* **Key identifiers** in `dm_conversations.key_id` (just a label, not a key).
* **Ciphertext and nonce** in `dm_messages` (server cannot decrypt).
* **Wrapped key escrow blob** (server cannot unwrap).
* **Expiry timestamps** for retention (server uses these to delete ciphertext, not to decrypt).

6.6 TARGET CLIENT-ONLY MATERIAL (UNSHIPPED)

* Plaintext DM message content.
* Private identity keys.
* Derived AES-256-GCM conversation keys.

6.7 ENCRYPTION TEST PLAN

The test suite for the encryption module (wabidb::crypto) must include:

1. **Roundtrip test**: generate keypair for user A, generate keypair for user B, derive shared key on both sides, encrypt on A, decrypt on B, assert plaintext matches.
2. **Key rotation test**: user A rotates key, user A sends message to B using old key for existing DMs and new key for new DMs, verify B can decrypt both with appropriate key lookup.
3. **Multi-device test**: user A generates identity key on device 1, escrows it with passphrase, recovers it on device 2, device 2 derives the same conversation key as device 1 for a conversation with user B.
4. **Ciphertext integrity test**: server cannot decrypt stored ciphertext (verify by checking that decryption with any server-side key fails).
5. **Retention test**: ciphertext is deleted after retention policy expiry; deleted ciphertext cannot be decrypted (because it is gone).

6.8 CALL METADATA ENCRYPTION (PRODUCT DECISION)

By default, the engine stores call metadata (callsessions, callparticipants, call_signals) unencrypted. This is a product decision, not a technical constraint. The rationale:

* Call metadata is needed for live call signaling. The server routes audio through the media helper and must know who is in the call, who is muted, and so on.
* Audio bytes are never stored in WabiDB at all — they flow through the media helper (or STDB's media relay) and are not persisted. So the audio content is already outside the engine's reach.
* The metadata (who called whom, when, how long) is similar to server logs and is treated similarly: stored durably, not encrypted at the engine level.
* A small server admin with a small group of trusted friends does not need to encrypt "Alice called Bob for 5 minutes at 3pm." That is normal metadata.

For maximum-privacy servers, the engine supports an "encrypted at rest" mode where callsessions and callparticipants rows are encrypted with a server-side key derived from the server's passphrase. In this mode, the server can still route live calls (the key is in memory while the engine is running) but a disk dump of the SQLite file is unreadable without the passphrase. This is a product decision the server admin makes at deployment time, not a per-call decision.

Note: even in encrypted-at-rest mode, the live call signaling (call_signals) rows are NOT encrypted. They must be readable in real time by the routing layer. The cost of encrypting signaling would be an in-memory decryption step on every signal, which is achievable but adds latency. The product decision is that signaling metadata (mute, unmute, speaking) is acceptable in plaintext; only the historical call records (who, when, how long) are encrypted at rest.

6.9 CLIENT-SIDE KEY VERIFICATION (FUTURE CAPABILITY)

A future capability (not v1) is the Signal-style "safety number" or "fingerprint" comparison. Each user has a short fingerprint derived from their identity public key (e.g., 60 decimal digits or a QR code). To verify that the public key on the server is actually controlled by the person they think it is, two users compare fingerprints in person or over a trusted channel.

This is not in v1 because:
* It requires a stable key (the fingerprint changes on every key rotation, and users tend to rotate when devices are lost).
* It requires a UI flow for "compare fingerprints" (not just storage).
* The threat model it addresses (MITM on the public key registry) is partially mitigated by the existing trust-on-first-use pattern in the frontend.
* It is a quality-of-life feature, not a security primitive.

The schema and command registry are designed to accommodate it without breaking changes: a devicefingerprint field can be added to the devices table, and a getmy_fingerprint command can be added to the registry, when the team is ready to build the UX.

============================================================

------------------------------------------------------------
7. THE ROOM/OBJECT MODEL
------------------------------------------------------------

7.1 BOUNDED SET OF OBJECT TYPES

WabiDB defines exactly 16 object types. This is a closed set — adding a new type requires engine code changes. No dynamic type registration.

Object Type           Backing Table(s)                                                                 Members            Permissions                            Retention  Children                 
--------------------  -------------------------------------------------------------------------------  -----------------  -------------------------------------  ---------  -------------------------
`text_channel`        `channels` (type=text), `messages`, `reactions`                                  `channel_members`  read: member; write: member            Durable    messages, reactions      
`voice_channel`       `channels` (type=voice), `call_sessions`                                         `channel_members`  read: member; join: member             Session    call_sessions            
`dm_conversation`     `dm_conversations`, `dm_messages`                                                exactly 2 users    read: participant; write: participant  Durable    dm_messages              
`call_session`        `call_sessions`, `call_participants`                                             channel members    join: member                           Session    participants, signals    
`whiteboard_session`  `whiteboards`, `whiteboard_layers`, `whiteboard_elements`, `whiteboard_patches`  channel members    read: member; edit: member             Durable    layers, elements, patches
`map_session`         (Higher-level: frontend uses OSM tiles; `places` table holds metadata)           all members        read: all                              Durable    —                        
`note`                `notes`, `note_versions`                                                         owner only         read/write: owner                      Durable    versions                 
`poll`                (Future table)                                                                   depends on scope   vote: member                           Session    votes                    
`timer`               (Future table)                                                                   depends on scope   read: member                           Session    —                        
`turn_tracker`        (Future table)                                                                   depends on scope   move: member                           Session    turn log                 
`dice_log`            (Future table)                                                                   depends on scope   roll: member                           Session    rolls                    
`album`               `state_album`, `state_album_item` (from bridge)                                  channel members    read: member; upload: member           Durable    album items              
`place`               `places`, `place_layers`, `place_pois`, `place_assets`                           all members        read: all; edit: admin                 Durable    layers, pois, assets     
`kanban_board`        `kanban_boards`, `kanban_tasks`, `kanban_columns`                                workspace members  read: member; edit: member             Durable    tasks, columns           
`project`             `projects`, `sprints`, `calendar_events`                                         workspace members  read: member; edit: member             Durable    sprints, events          
`task`                `kanban_tasks` (shared with kanban)                                              board members      read: member; edit: assignee/creator   Durable    —                        

7.2 GENERIC OPERATIONS

Every object type supports these operations automatically:

    fn create_instance(ctx: CommandCtx, object_type: ObjectType, args: CreateArgs) -> Result<InstanceId, CommandError>;
    fn list_instances(ctx: CommandCtx, object_type: ObjectType, scope: Scope) -> Result<Vec<InstanceSummary>, CommandError>;
    fn get_instance(ctx: CommandCtx, object_type: ObjectType, instance_id: &str) -> Result<InstanceDetail, CommandError>;
    fn update_instance(ctx: CommandCtx, object_type: ObjectType, instance_id: &str, changes: Value) -> Result<(), CommandError>;
    fn delete_instance(ctx: CommandCtx, object_type: ObjectType, instance_id: &str) -> Result<(), CommandError>;

Each object type registers its schema (table name, column list, permissions) in a OBJECT_TYPES static HashMap. The generic operations use runtime SQL construction (parameterized, no string concatenation of user input) to build INSERT/UPDATE/SELECT/DELETE queries for the known schema. This avoids repeating CRUD for each type.

7.3 PER-TYPE OPERATIONS

Beyond generic CRUD, each object type has type-specific operations:

* **whiteboard_session**: `add_layer`, `update_layer`, `delete_layer`, `reorder_layer`, `add_element`, `update_element`, `delete_element`, `add_patch`
* **kanban_board**: `add_task`, `update_task`, `delete_task`, `move_task`, `reorder_columns`, `update_column`
* **call_session**: `join`, `leave`, `end`, `signal`
* **dm_conversation**: `send_message`, `delete_message`
* **place**: `create_poi`, `update_poi`, `delete_poi`, `add_asset`

These are individually registered in the CommandRegistry.

7.4 ROOM STATE SUBSCRIPTION MODEL

The subscription topic for an object instance is:

* `channel:{id}:messages` → all messages in the channel (auto-subscribed by channel members)
* `whiteboard:{id}` → all whiteboard patch events and snapshot events
* `kanban:{id}` → all task/column changes
* `call:{id}` → all call signaling events
* `place:{id}` → all place metadata and POI changes
* `dm:{canonical_id}` → all DM messages in the conversation
* `user:{id}:inbox` → friend requests, notifications, system messages

When a client connects, it typically subscribes to:
* `+server:announcements` (always)
* `+channel:{active_channel}:messages`
* `+channel:{active_channel}:meta`
* `+channel:{active_channel}:members`
* `+channel:*:call` (if in a voice channel)
* `+user:{my_id}:inbox`
* `+dm:{active_dm}:*`

Unlike STDB's approach of subscribing to SQL queries that could match any table, WabiDB's subscriptions are always scoped to a specific instance of a specific object type. This is a deliberate simplification.

============================================================

------------------------------------------------------------
8. THE PRIVACY AND RETENTION MODEL
------------------------------------------------------------

8.1 TWO-TIER DATA LIFECYCLE

WabiDB has exactly two tiers:

1. **Committed state** (durable): All data in SQLite WAL-mode tables. Survives restart. Backed up on schedule. This is the canonical source of truth.
2. **Ephemeral state** (in-memory only): Call signaling rows (the `call_signals` concept), typing indicators, cursor positions. Never written to SQLite. Survives only the current transaction. On a crash, ephemeral data is lost.

The boundary is explicit at the table level: tables marked with retentionclass = ephemeral in the retentionclasses registry are not backed by SQLite rows — they are in-memory only and written directly to the outbox.

8.2 PER-OBJECT RETENTION CLASSES

Class        Behavior                                                     Examples                                          
-----------  -----------------------------------------------------------  --------------------------------------------------
`ephemeral`  Never persisted to SQLite. Written only to outbox.           Call signals, typing indicators, cursor positions 
`session`    Deleted when the originating session ends (or TTL expires)   Call participants, temporary sessions, pair tokens
`durable`    Kept until explicitly deleted by a user or admin command     Messages, channels, whiteboards, user profiles    
`archive`    Kept until the operator purges (no user-triggered deletion)  Audit logs, deleted messages (retention hold)     

8.3 THE TTL REAPER

A scheduled command purge_expired runs on a configurable interval (default 60 seconds). It:

1. Queries `retention_classes` for all object types with `ttl_seconds IS NOT NULL`.
2. For each such type, queries the corresponding table for rows where `created_at < now - ttl_seconds`.
3. Hard-deletes expired rows.
4. After every 1000 deletions, runs `PRAGMA incremental_vacuum(100)` to reclaim pages.
5. Logs the count of deleted rows.

8.4 ENCRYPTION AT REST

WabiDB does not implement encryption at rest at the engine level. The data directory is expected to be on an encrypted volume (LUKS, ZFS encryption, or similar). This is documented in the deploy guide. The rationale: encryption at rest is an OS/filesystem concern, and implementing it in the database engine duplicates that work without providing additional security — the engine cannot protect against an attacker who has access to the running process's memory.

8.5 NO TELEMETRY THAT PHONES HOME

WabiDB has zero telemetry. No usage statistics, no crash reports, no analytics. Logs are written to local files only, controlled by the RUST_LOG environment variable.

8.6 NO CROSS-SNAPSHOT CONTENT-ADDRESSING

STDB's snapshot system (crates/snapshot/src/lib.rs) uses BLAKE3 content-addressed pages and hardlinks to deduplicate across snapshots. This creates a privacy concern: hash linkability means an attacker who controls a page can verify its presence in a snapshot by comparing hashes. WabiDB's SQLite backup mechanism (sqlite3 .backup) does not use content addressing or deduplication. Each backup is a complete, independent copy of the database file. This eliminates cross-snapshot hash linkability.

8.7 DM MESSAGE RETENTION

DM message retention is configurable per user via the usersettings.offlineretention field (values: forever, 30days, 7days, 1day). The TTL reaper checks each DM message's createdat against the retention policy of each participant and deletes the message if both participants' policies have expired. If one participant has forever and the other has 7_days, the message is retained (union of both policies: keep until both would delete).

8.8 PERFORMANCE AND LOAD-SHEDDING

WabiDB is built on SQLite, which has a single-writer-multiple-readers model in WAL mode. This has a real ceiling. The honest characterization:

* **Sustained write throughput**: ~1,000 transactions per second on a modern SSD with `synchronous=NORMAL` and a reasonable WAL size. Higher bursts (2,000-3,000/sec for short periods) are achievable but not sustained.
* **Read concurrency**: essentially unlimited for typical query patterns. Hundreds of concurrent readers do not contend with the writer.
* **Memory**: the entire SQLite file is memory-mapped by default. The OS handles paging. A 10 GB database works fine on a 32 GB machine.
* **Disk I/O**: the bottleneck is the WAL file's fsync. A separate disk for the WAL file (vs. the main database file) can double write throughput.

For Wabi's expected scale (a single Wabi server with maybe 100-1,000 concurrent users, ~10-50 active in any given room), this is comfortably within budget. For a hypothetical future Wabi with 10,000+ concurrent users, the limits start to matter.

Load-shedding strategy. When the outbox consumer falls behind the producer:

1. **Per-subscriber backpressure** (Section 5.7): a slow subscriber's channel fills, gets a backpressure frame, and is dropped after 5 seconds. The fast subscribers continue normally.
2. **Outbox backlog cap**: if the outbox row count exceeds a configured threshold (default 100,000), the engine enters degraded mode. Non-essential commands return `CommandError::EngineBusy` with a `retry_after_ms` hint. Essential commands (send_message, set_presence) continue to be accepted.
3. **Adaptive batching**: the outbox consumer's batch size grows with backlog to drain faster; with low backlog it shrinks to keep latency low.
4. **Circuit breaker for the search helper**: if the search helper is down, the engine stops enqueuing `search:updates` rows after a brief retry window. Search falls back to the built-in FTS5. When the helper comes back, the consumer replays any missed rows from a checkpoint.
5. **Per-object write rate limit**: each object instance has a configurable write rate limit (e.g., 100 writes/sec on a single whiteboard). Excess writes are dropped with a warning. This prevents a runaway client from saturating the engine.

Future scaling options (not v1):

* **Per-channel sharding**: split the database into multiple SQLite files, one per channel, with a router in front. This is a v2 architecture; v1 is one file. The shard key would be the room ID.
* **Move to libmdbx (LMDB)**: LMDB allows multiple writers with locking. It is not as feature-rich as SQLite (no FTS5, no SQL) but it is faster for write-heavy workloads. If the write ceiling becomes the bottleneck, an alternative storage backend is pluggable behind the `StorageEngine` trait. This is a v3 or v4 architecture.
* **Helper-node write absorption**: helper nodes can absorb write traffic for high-frequency subsystems (e.g., the media helper absorbs call signaling, leaving the engine to handle only committed state). This is a v2 design.

The honest answer: for a small community server, none of this is needed. For a large community server, sharding and libmdbx are the path. The engine's StorageEngine trait is the abstraction that makes either possible without rewriting commands.

8.9 WHITEBOARD AND CAD PATCH RETENTION

Whiteboards and CAD sessions produce append-only patch logs. Without a trim policy, these grow unboundedly: a whiteboard with 10 active users at 1 patch/sec per user produces 36,000 patches per hour. A CAD session can produce thousands of element-add patches per minute during initial modeling.

Per-object retention policy. Each whiteboard or CAD session has a patchretentiondays field (default 30 days) and a max_patches field (default 100,000). The TTL reaper (Section 8.3) applies these policies:

* Patches older than `patch_retention_days` are deleted.
* If the total patch count exceeds `max_patches`, the oldest patches are deleted down to `max_patches * 0.8` (leave headroom).

Periodic snapshot consolidation. A scheduled command consolidatewhiteboardpatches (runs every 6 hours by default) does the following:

1. Counts the patches in each whiteboard.
2. If a whiteboard has more than `max_patches` patches, the engine computes a fresh "snapshot row" (the current full state) and deletes all patches older than the most recent N (where N is chosen to keep the log at `max_patches * 0.5`).
3. The snapshot is the canonical state; the remaining patches are the diff from the snapshot.

Subscribers that join after a consolidation receive the snapshot first, then any subsequent patches. Late-joiners do not need to replay the entire history.

The trade-off. Consolidation loses fine-grained history (you cannot replay the whiteboard state from 3 weeks ago). For most use cases this is acceptable. For audit or rollback needs, the engine keeps a separate whiteboard_history table (configurable per object) that snapshots the full state every N hours. Default: daily history snapshot retained for 1 year. The history table is small (a snapshot is much smaller than a patch log) and the cost is bounded.

For CAD, the same pattern applies with a longer default retention (CAD sessions are typically larger and edited less frequently than whiteboards, so a default of 90 days is reasonable).

8.10 PATCH BOUNDED GROWTH FOR OTHER APPEND-ONLY LOGS

Other append-only logs (callsignals, dmmessagehistory, auditlog) have their own retention policies:

* `call_signals`: ephemeral. Not persisted beyond the call session. Cleared when the call ends.
* `dm_message_history`: durable. Subject to the per-user retention policy (Section 8.7).
* `audit_log`: archive class. Operator-purgeable only. Default retention 1 year. Operator can configure longer.

============================================================

------------------------------------------------------------
9. THE HELPER-NODE PROTOCOL
------------------------------------------------------------

9.1 PROTOCOL SURFACE

The helper-node protocol is not a full networking stack — it is a set of commands, data types, and expected behaviors that helper nodes use to pair, authenticate, and communicate with the WabiDB authority. The actual transport is TLS (mutual TLS for helper-to-authority communication, or a WebSocket upgrade if behind a load balancer).

9.2 NODE TYPES

Type         Purpose                                                      Capabilities                                           
-----------  -----------------------------------------------------------  -------------------------------------------------------
`media`      Relay audio/video streams (SFU/TURN)                         `relay:audio`, `relay:video`, `relay:screen`           
`search`     Full-text search index (external Elasticsearch/Meilisearch)  `search:messages`, `search:channels`                   
`transcode`  Convert media between formats                                `transcode:audio`, `transcode:video`, `transcode:image`
`cache`      CDN/caching layer for file attachments and avatars           `cache:static`, `cache:media`                          
`anchor`     Regional network anchor for latency reduction                `anchor:relay`, `anchor:turn`                          

9.3 PAIR-TOKEN ISSUANCE

The authority operator runs:

    wabi-node helper pair --type media --capabilities '["relay:audio","relay:video"]' --ttl 3600

This generates a one-time pairtoken (a random 32-byte hex string plus metadata) stored in the pairtokens table and printed to stdout. The operator conveys the token to the helper operator out-of-band (SSH, config management, etc.).

9.4 ROUTE TOKEN REDEMPTION

The helper connects to the authority and redeems the pair token:

    POST /v1/helpers/redeem
    {
      "pair_token": "...",
      "node_id": "media-01",
      "public_key": "..."
    }

The authority validates the pair token (not expired, not redeemed), creates a helpernodes row and a routetoken (a signed JWT-like payload), and returns the route token. The helper stores this token and presents it on every subsequent request.

9.5 ROUTE TOKEN SHAPE

    {
      "iss": "wabi-authority",
      "sub": "media-01",
      "aud": "wabi-media-node",
      "iat": 1750000000,
      "exp": 1781536000,
      "capabilities": ["relay:audio", "relay:video"],
      "node_identity": "<public_key_fingerprint>"
    }

Signed with the authority's Ed25519 key (stored in a config file, never in the database).

9.6 HEARTBEAT

Every 30 seconds, the helper sends:

    POST /v1/helpers/heartbeat
    {
      "node_id": "media-01",
      "status": "healthy",
      "current_connections": 42,
      "bandwidth_mbps": 850,
      "load_pct": 0.65
    }

The authority updates the helpernodes row's lastheartbeat and returns the current revocation list (empty unless there are revocations). If the authority receives no heartbeat for 90 seconds (3 intervals), the helper is marked offline.

9.7 REVOCATION

The authority operator runs:

    wabi-node helper revoke --node-id media-01

This sets route_tokens.revoked = 1 for the node's active token. On the next heartbeat, the authority includes this token ID in the revocation list. The helper sees the revocation and stops accepting work. The helper should also periodically check revocation independently (every 5 minutes, fetch the revocation list from the authority).

9.8 WHY THIS EXISTS IN V1

Retrofitting helper-node awareness into a database engine that assumed a single-process model is much harder than designing for it from the start. STDB has no helper concept — adding it would require modifying the core transaction pipeline, the WebSocket handler, and the binary interface. WabiDB's helper protocol is a thin layer of tables + commands + a clock-driven heartbeat checker, totaling fewer than 500 lines of engine code. The actual helper communication (media relay, search indexing, etc.) happens at the wabi-server level, not inside WabiDB.

9.9 THE ADDON OBJECT PRIMITIVE

WabiDB has a fixed set of core object types (textchannel, dmconversation, callsession, mapsession, whiteboardsession, note, poll, timer, turntracker, dicelog, album, place, kanbanboard, project, task, calendar_event — 16 types). But Wabi will want complex feature rooms that don't fit this fixed set: CAD, music sequencer, video editor, advanced D&D combat tracker, AI training room. The "Minecraft redstone, not arbitrary kernel modules" boundary forbids uploading arbitrary server code at runtime, so these need a different mechanism.

The mechanism is the addon object primitive: one new engine table, one manifest format, one build-time registration pattern. Addons compose engine primitives; they don't add new engine features.

THE `ADDON_OBJECT` TABLE

    CREATE TABLE addon_object (
        id              INTEGER PRIMARY KEY,
        addon_id        TEXT NOT NULL,                -- e.g. "wabi.cad"
        room_id         INTEGER NOT NULL,             -- the room this object belongs to
        data_blob_hash  BLAKE3,                       -- actual data in the blob store
        data_schema_id  TEXT NOT NULL,                -- schema version for migration
        created_at      INTEGER NOT NULL,
        updated_at      INTEGER NOT NULL,
        expires_at      INTEGER,                      -- optional TTL
        created_by      INTEGER NOT NULL,             -- user id
        deleted_at      INTEGER
    );
    
    CREATE INDEX idx_addon_object_room ON addon_object(addon_id, room_id)
        WHERE deleted_at IS NULL;
    CREATE INDEX idx_addon_object_expires ON addon_object(expires_at)
        WHERE expires_at IS NOT NULL AND deleted_at IS NULL;

The engine does not interpret datablobhash. The blob is the addon's data, in whatever format the addon defines. The engine stores it, indexes it by (addonid, roomid), and broadcasts changes through the outbox.

THE BUILD-TIME MANIFEST

Each addon is a Rust crate compiled into the engine binary at build time. The crate exposes a manifest:

    // cad_addon/src/lib.rs
    pub struct CadAddon;
    
    impl Addon for CadAddon {
        fn id() -> &'static str { "wabi.cad" }
        fn version() -> &'static str { "0.1.0" }
    
        fn manifest() -> AddonManifest {
            AddonManifest {
                id: "wabi.cad",
                version: "0.1.0",
                data_schemas: schemas![
                    cad_session_schema,
                    cad_part_schema,
                    cad_dimension_schema,
                    cad_constraint_schema,
                    cad_layer_schema,
                    cad_view_schema,
                    cad_user_viewport_schema,
                ],
                commands: commands![
                    create_cad_session,
                    add_part,
                    set_dimension,
                    add_constraint,
                    solve_constraints,
                    save_view,
                    set_viewport,
                    select,
                    add_comment,
                ],
                topics: topics![
                    "cad:{session_id}:state",
                    "cad:{session_id}:viewport:{user_id}",
                    "cad:{session_id}:presence",
                ],
                required_capabilities: vec![
                    "can_create_cad_session",
                    "can_edit_cad",
                    "can_view_cad",
                ],
            }
        }
    }

At engine startup, the manifest is registered. The engine validates the manifest (no duplicate command names, no conflicting topic grammars, all declared capabilities are valid). The addon's commands become part of the command registry. The addon's topics become part of the topic grammar. The addon's data schemas become known to the migration tool.

BUILD-TIME LINKING, NOT RUNTIME UPLOAD

Addons are linked at build time, not uploaded at runtime. There is no "install addon" operation on a running server. The server admin enables an addon by:

    # Add the addon to Cargo.toml
    [dependencies]
    wabidb-cad = { path = ".../cad_addon" }
    
    # Add the feature to wabi-server
    wabi-server = { features = ["cad", "music"] }

Or, if addons are statically compiled into a single binary, by building with --features=cad,music. The server admin's choice is a deployment decision, not a runtime decision.

This is more conservative than a runtime plugin system. It is also simpler, more secure, and easier to reason about. The server admin knows exactly what code is in their server because it is all compiled together. There is no class of attack "attacker uploads malicious addon to the server."

If a future Wabi needs runtime addons, that is a different architecture (STDB's WASM model). WabiDB explicitly chose the build-time model because it fits the self-hosted trust model: you trust the code you compiled, period.

CAPABILITY MODEL

Each command in an addon declares the capability required to invoke it. The engine checks the caller's capability set against the command's required capability before running the command. If the check fails, the command returns CommandError::PermissionDenied and no state mutation occurs.

Capabilities are mapped to roles by the server admin (via existing RBAC, see Section 3.15). A user with the caduser role has the cancreatecadsession and caneditcad capabilities. A user without the role has neither, and the engine refuses their add_part command.

The frontend also uses capabilities to decide which UI to show. If a user lacks caneditcad, the CAD component renders in read-only mode.

WHAT THIS GIVES ADDON AUTHORS

* **Typed commands**: addon commands have typed inputs and outputs, validated at compile time.
* **Transactional state**: addon commands participate in WabiDB's transaction model. If the command fails, the state mutation and the outbox rows roll back atomically.
* **Live subscriptions**: addon topics work the same as core topics. Subscribers receive initial snapshots and incremental updates.
* **Retention**: addon objects support all four retention classes (ephemeral, session, durable, archive).
* **Encryption at rest**: addon objects live in the same SQLite database, so operator-side disk encryption applies.
* **Migration: addon data schemas are versioned. When the addon's schema changes, the addon provides a migration file. The same migration tool (`refinery` or `sqlx-migrate`) handles it.
* **No engine features added**: the engine stays the same. The `addon_object` table is the only new core primitive. Everything else is addon code.

WHAT THIS DOES NOT GIVE ADDON AUTHORS

* **Hot-load addons**: you rebuild the binary to add an addon. The server does not load code at runtime.
* **Third-party addons from strangers**: you have to trust the source code you compile in. The engine does not sandbox addon code; it runs in the same process.
* **Custom SQL queries**: addons use the engine's typed accessors, not raw SQL. The data schemas are declared in Rust, validated at compile time.
* **Custom migration logic**: addon migrations are SQL files run by the same tool as core migrations.
* **Custom subscription topics beyond the declared grammar**: addons can declare new topics at build time, but cannot add new topic patterns at runtime.

THE CAD EXAMPLE AS A WORKED INSTANCE

A "wabi.cad" addon, as described above, would be roughly 3,000-5,000 lines of Rust in wabidb-cad plus 50,000-100,000 lines of frontend TypeScript. The Rust code provides: the data schemas (cadsession, cadpart, etc.), the commands (createcadsession, add_part, etc.), and the topic grammar. The frontend code provides: the CAD component (Three.js, OpenCascade.js, IFC.js), the dimension UI, the layer panel, the comment system, the per-user viewport state.

The engine is unchanged. The frontend gains a new "CAD" tab for users with the canviewcad capability. The data flows through the same outbox and subscription engine. The retention engine handles TTL. The encryption engine handles per-recipient DM messages (if CAD supports comments-as-DM).

Server admins who don't want CAD don't link the wabidb-cad crate. Their binary does not have the CAD commands, the CAD topics, or the CAD data schemas. The base Wabi experience is unchanged.

WHY THIS IS IN V1, NOT V2

The decision to include the addon primitive in v1 (not retrofit later) is the same as the helper-node decision: a one-time cost now is much less than a retrofit cost later. The addon_object table is one table. The manifest is one struct. The registration is one function. Total: ~1,500-2,000 lines of engine code.

The cost of retrofitting later: every command, every subscription, every state read needs to think about whether it is core-only or addon-callable. Every test needs to cover both paths. Every migration needs to handle "what if an addon needs a new column on a core table." That retrofit is much more expensive than the v1 cost.

============================================================

------------------------------------------------------------
10. THE API SURFACE
------------------------------------------------------------

10.1 RUST API: THE WABISTATE TRAIT

Wabi-server embeds the engine and calls it through a single trait:

    pub trait WabiState: Send + Sync {
        /// Execute a command. Returns the result and any outbox rows that were emitted.
        fn execute(&self, command: &str, caller: CallerIdentity, args: &[u8]) -> Result<ExecResult, CommandError>;
    
        /// Subscribe to topics. Returns an initial snapshot stream and an update stream.
        fn subscribe(&self, filters: &[TopicFilter]) -> SubscriptionStream;
    
        /// Admin operations.
        fn issue_pair_token(&self, node_type: &str, capabilities: &[&str], ttl_secs: u64) -> Result<String, CommandError>;
        fn get_stats(&self) -> EngineStats;
    }

The execute method is the single entry point for all mutations. Wabi-server deserializes the WebSocket command into the command name and BSATN args, calls execute, and sends the result back over the WebSocket.

In embedded mode, this is a direct function call in the same process:

    let engine = WabiDbEngine::open("/var/lib/wabi/wabidb").await?;
    let result = engine.execute("send_message", caller, &bsatn_args)?;

In sibling-process mode, there is a lightweight IPC layer: a Unix domain socket with a simple request/response protocol. The WabiState trait is implemented by a RemoteEngine that sends BSATN-framed messages over the socket and reads responses. This mode exists for operational isolation but is not the primary deployment.

10.2 WEBSOCKET PROTOCOL

The frontend connects to wabi-server (not directly to WabiDB). The Socket.IO connection is preserved for now, but the command path changes: instead of calling ingestwabievent reducers on STDB, the frontend will emit Socket.IO events that wabi-server translates into WabiDB execute() calls.

Connection lifecycle:

1. Client connects to `wss://server/ws?token=<jwt>`.
2. Server authenticates the JWT, extracts the user identity and capabilities.
3. Server creates a `Subscriber` in the `SubscriptionManager` with the client's initial filters.
4. Server sends `{"type":"subscribed","filters":[...], "snapshot":[...]}`.
5. Bidirectional message exchange.
6. On disconnect, server removes the subscriber and cleans up.

Authentication: JWT in the query string. The JWT is issued by wabi-server's auth system and contains at minimum: { sub: user_id, exp: ..., iat: ... }. The engine validates the JWT signature using the shared secret.

Frame format: The first byte of each WebSocket message indicates the encoding: 0x00 = BSATN (binary), 0x01 = JSON (debugging). The remainder is the encoded ClientMessage or ServerMessage (see Section 5.5).

10.3 HTTP ADMIN API

A minimal HTTP API is available on a separate port (default :9812) for admin operations that are inconvenient over WebSocket:

    POST /v1/admin/pair-token          — issue a helper pair token
    POST /v1/admin/helpers/revoke      — revoke a helper route token
    POST /v1/admin/retention           — set retention policy
    GET  /v1/admin/stats               — engine statistics
    POST /v1/admin/backup              — trigger an immediate SQLite backup
    POST /v1/admin/restore             — restore from a backup file

All admin endpoints require a separate admin API key (not the same JWT as user auth) in the Authorization: Bearer <admin_key> header. The admin key is configured in the server config file.

============================================================

------------------------------------------------------------
11. THE STORAGE ENGINE DETAIL
------------------------------------------------------------

11.1 SQLITE CONFIGURATION

* **Journal mode:** WAL (`PRAGMA journal_mode=WAL`). This provides concurrent reads without writer blocking and crash recovery without a separate commitlog.
* **Synchronous mode:** NORMAL (`PRAGMA synchronous=NORMAL`). WAL mode with NORMAL provides durability within the WAL's checkpoint interval while being significantly faster than FULL.
* **Page size:** 4096 bytes (`PRAGMA page_size=4096`). Balanced for mixed row sizes.
* **Wal autocheckpoint:** 1000 pages (`PRAGMA wal_autocheckpoint=1000`).
* **Foreign keys:** ON (`PRAGMA foreign_keys=ON`).
* **Busy timeout:** 5000ms (`PRAGMA busy_timeout=5000`).
* **Temp store:** MEMORY (`PRAGMA temp_store=MEMORY`).

One file per Wabi server at {data_dir}/wabidb/wabidb.sqlite, with wabidb.sqlite-wal and wabidb.sqlite-shm as WAL companion files.

11.2 SCHEMA MIGRATION

Migrations use refinery (embedded Rust migration framework). Migration files are at wabidb/src/storage/migrations/V1initial.sql, V2add_fts5.sql, etc. Migrations are run at engine startup before any commands are accepted. Rollback: each migration must have both an up() and a down() SQL block; the down() is used if a migration fails mid-flight.

11.3 INDICES

Full index list (all tables):

* `idx_users_username_lc` UNIQUE on `users(username_lc)`
* `idx_users_handle_lc` UNIQUE on `users(handle_lc)` WHERE `handle_lc IS NOT NULL`
* `idx_users_status` on `users(status)`
* `idx_sessions_user_id` on `sessions(user_id)`
* `idx_devices_user_id` on `devices(user_id)`
* `idx_identity_keys_fingerprint` UNIQUE on `identity_keys(key_fingerprint)`
* `idx_channels_type` on `channels(channel_type)`
* `idx_channels_parent` on `channels(parent_id)`
* `idx_cm_user_id` on `channel_members(user_id)`
* `idx_messages_channel_created` on `messages(channel_id, created_at)`
* `idx_messages_sender` on `messages(sender_id)`
* `idx_messages_deleted` on `messages(deleted)` WHERE `deleted = 1`
* `idx_reactions_message` on `reactions(message_id)`
* `idx_reactions_user` on `reactions(user_id)`
* `idx_dm_user_a` on `dm_conversations(user_a)`
* `idx_dm_user_b` on `dm_conversations(user_b)`
* `idx_dm_msgs_conversation` on `dm_messages(conversation_id)`
* `idx_call_sessions_channel` on `call_sessions(channel_id)`
* `idx_cp_session` on `call_participants(session_id)`
* `idx_whiteboards_scope` on `whiteboards(scope_type, scope_id)`
* `idx_wb_patches_board_created` on `whiteboard_patches(board_id, created_at)`
* `idx_places_slug` UNIQUE on `places(slug)`
* `idx_notes_owner_scope` on `notes(owner_id, scope_type, scope_id)`
* `idx_nv_note` on `note_versions(note_id)`
* `idx_kt_board_status` on `kanban_tasks(board_id, status)`
* `idx_kt_assigned` on `kanban_tasks(assigned_to)`
* `idx_bge_source` on `business_graph_edges(source_id)`
* `idx_bge_target` on `business_graph_edges(target_id)`
* `idx_outbox_unsent` on `outbox(broadcast_at)` WHERE `broadcast_at IS NULL`

Each index exists because the corresponding query pattern exists in the frontend or in a background command.

11.4 FTS5 FOR MESSAGE SEARCH

    CREATE VIRTUAL TABLE messages_fts USING fts5(
        content,
        content=messages,
        content_rowid=rowid,
        tokenize='unicode61'
    );

The content table is messages(message_id, content). The FTS5 table is kept in sync via triggers (insert, update, delete on messages). Relevance ranking uses BM25. Queries are issued by the search engine (whether internal or forwarded to a helper search node).

11.5 BLAKE3 CONTENT-ADDRESSED BLOB STORE

For file attachments (images, audio, files), WabiDB does not store the file bytes inline. Instead, a content-addressed blob store is used:

* Directory layout: `{data_dir}/wabidb/blobs/{first_two_hex}/{blake3_hash}`
* File naming: `{blake3_hash}.bin`
* GC: a background task scans the `blobs` directory, queries the database for all referenced blob hashes, and deletes unreferenced files. Runs every hour.
* Blobs are stored with a metadata sidecar: `{hash}.meta` (JSON: `{size, mime, created_at}`).

This is conceptually similar to STDB's BlobStore and DirTrie (crates/table/src/blobstore.rs, crates/fs-utils/src/dirtrie.rs), but without the snapshot hardlinking (see Section 8.6).

11.6 BLOB STORE RANGE READS

For large blobs (megabytes to gigabytes — common in CAD assemblies, video attachments, voice notes longer than a few minutes), loading the entire blob into memory before sending it to the client is wasteful and slow. The blob store therefore supports range reads.

Protocol. Range reads are served over a dedicated HTTP endpoint that bypasses the main WebSocket connection:

    GET /v1/blobs/{hash}?offset=1048576&length=4194304
    Authorization: Bearer {token}
    
    Response:
    HTTP/1.1 206 Partial Content
    Content-Range: bytes 1048576-5242879/{total}
    Content-Type: {mime}
    ETag: "{hash}"

The endpoint validates the token, looks up the blob by hash, seeks to offset, and streams length bytes. The client can request multiple ranges in parallel (using HTTP/2 multiplexing or HTTP/1.1 range requests) to assemble large files efficiently.

Memory bounds. The server uses tokio::fs::File::seek and a bounded BufReader (default 256 KB). It never loads the entire blob into memory. A 1 GB CAD mesh streams from disk to client at network speed without ever sitting in the server's RAM.

Caching. The ETag is the BLAKE3 hash. Clients that already have the blob can send If-None-Match: "{hash}" and receive a 304 Not Modified. The server does not need to read the file to respond to a 304.

Lazy-load pattern. For CAD assemblies, the frontend does not request the entire assembly blob on connect. Instead:

1. The frontend requests the assembly metadata (a small JSON blob) via the normal subscription channel: `cad:{session_id}:state`.
2. The metadata contains a list of part references, each with a blob hash and a bounding box.
3. The frontend renders the parts whose bounding boxes are in the current viewport, requesting their blobs via the range-read endpoint.
4. As the user pans/zooms, the frontend requests additional blobs and releases the ones no longer in view.
5. The same pattern works for whiteboard imports (lazy-load image data only when the image is in view) and for albums (lazy-load thumbnails first, full images on click).

What this is not. This is not a CDN. The blob store serves blobs from local disk on the server. For multi-region deployments, a CDN or a regional cache helper would sit in front. The blob store is the source of truth; the CDN is an accelerator.

11.7 BACKUP, RESTORE, AND RECOVERY

Hot backup. The engine runs a periodic backup task (default every 6 hours, configurable) using SQLite's sqlite3 .backup API:

    sqlite3 /var/lib/wabi/wabidb/wabidb.sqlite ".backup /var/backups/wabi/wabidb-{timestamp}.sqlite"

The backup is a complete, consistent copy of the database file at the moment the backup starts. Concurrent reads and writes are not blocked. The backup task also runs PRAGMA integrity_check on the completed file to verify it is not corrupt.

Blob store backup. The blob store directory ({data_dir}/wabidb/blobs/) is backed up separately. A simple rsync or tar is sufficient — the blobs are immutable (content-addressed by hash) so partial backups are safe. The recommended schedule: hourly for the SQLite file, daily for the blob directory. The two are backed up to the same destination but at different cadences.

Restore procedure (with downtime). The simple, safe option:

1. Stop the engine (`wabi-node stop` or systemd).
2. Replace `{data_dir}/wabidb/wabidb.sqlite` with the chosen backup file.
3. Replace the blob directory with the corresponding blob backup.
4. Start the engine.
5. The engine runs migrations on startup (no-op for a backup restore since the backup was made on the same schema version).
6. Engine is live again.

Restore procedure (hot-swap, future). A more advanced option, not v1:

1. Start a second engine instance pointed at the backup file.
2. The new instance begins accepting read traffic but rejects writes (read-only mode).
3. A drain is performed on the old instance: new connections go to the new instance, existing connections complete their work and disconnect.
4. Once the old instance is drained, the new instance is promoted to read-write and the old instance is stopped.
5. The new instance's WAL catches up to the most recent state via the standard recovery pipeline.

This requires a load balancer in front of the engine and a coordination protocol between instances. v1 ships the downtime restore; hot-swap is a future capability.

Point-in-time recovery (PITR). SQLite WAL mode supports PITR via continuous WAL archiving:

1. The engine runs a `wal_archive` task that copies the WAL file to a backup destination every 30 seconds.
2. To restore to a point in time: restore the most recent full backup, then replay the WAL archives up to the desired timestamp.
3. The replay tool is a small Rust binary that uses the same SQLite library to apply WAL frames in order.

This is more complex than the hot backup. It is the right tool for "I accidentally deleted a message at 3:42pm and need to recover it." v1 ships the WAL archive task and the replay tool; full PITR workflows are documented but not automated.

Backup retention policy. Backups older than 30 days are deleted (configurable). The most recent 24 hourly WAL archives are kept. The most recent 12 daily full backups are kept. This gives 30 days of recovery options without unbounded storage growth.

Disaster recovery. For a complete server loss:

1. Provision a new machine.
2. Install the engine binary.
3. Restore the most recent full backup + WAL archives.
4. Restore the blob directory from the most recent blob backup.
5. Update DNS to point to the new machine.
6. Engine is live.

The recovery time is dominated by the size of the blob directory (potentially gigabytes to restore over the network). For a small community server, this is minutes. For a large one with many years of attachments, it is hours.

============================================================

------------------------------------------------------------
12. THE CONNECTION TO EXISTING WABI CODE
------------------------------------------------------------

12.1 ENGINE FITS INTO WABI-SERVER

Wabi-server currently runs as a Rust binary with a Socket.IO server, connecting to SpacetimeDB as an external database. In the end state:

* The `wabidb` crate is added as a dependency to wabi-server's `Cargo.toml`.
* At startup, wabi-server calls `WabiDbEngine::open(config)` which opens the SQLite database, runs migrations, and returns an `Arc<dyn WabiState>`.
* The existing Socket.IO event handlers that currently call `ingest_wabi_event` (via HTTP POST to STDB) are rewritten to call `engine.execute(command_name, caller, args)` directly.
* The existing Socket.IO event handlers that push real-time updates to clients (e.g., `whiteboard:patch`, `channel:message`) are replaced by the subscription engine: the engine pushes updates, and wabi-server's Socket.IO layer translates them to Socket.IO events for backward compatibility with the existing frontend.
* The `stdb_bindings/` directory and the `wabi_state_bridge` and `wabi_call_state_bridge` crates are deleted. They become dead code.

12.2 FRONTEND MIGRATION

The frontend currently uses the spacetimedb SDK via generated bindings (stdb_bindings/index.ts). The migration path:

1. A new `WabiClient` class is introduced in the frontend that speaks the WebSocket protocol described in Section 10.2.
2. The `WabiClient` class replaces the `spacetimedb` SDK's `DbConnection`.
3. Table subscriptions (e.g., `db.state_message! table().subscribe()`) are replaced by topic subscriptions (`wabiClient.subscribe('+channel:general:messages')`).
4. Reducer calls (e.g., `reducers.ingestWabiEvent(...)`) are replaced by command calls (`wabiClient.command('send_message', args)`).
5. The migration can happen incrementally: the `WabiClient` can be introduced alongside the existing STDB client, and channels/features can be migrated one at a time.

12.3 END STATE

* The `stdb_bindings/` directory is deleted.
* The `wabi_state_bridge` and `wabi_call_state_bridge` crates are removed from the workspace.
* The `spacetimedb` dependency is removed from both server and frontend.
* The engine is the single source of truth for all persistent state.
* All frontend features that work today (whiteboard, map, notes, business, calling, channels, DMs, friends, settings, moderation) continue to work because their data model is preserved — it is simply stored in WabiDB tables instead of STDB tables.

12.4 FEATURE PRESERVATION

Each frontend feature and its corresponding WabiDB table:

Frontend Feature  WabiDB Tables                                                                                                                                                                Key Frontend Source Files                             
----------------  ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------  ------------------------------------------------------
Channel messages  `messages`, `reactions`                                                                                                                                                      `channelStore.ts`, `messageStore.ts`                  
Whiteboard        `whiteboards`, `whiteboard_layers`, `whiteboard_elements`, `whiteboard_patches`                                                                                              `whiteboard/` (all 18 files)                          
Map/Places        `places`, `place_layers`, `place_pois`, `place_drafts`                                                                                                                       `mapWorkspace.ts`, `placeRegistry.ts`, `placeDraft.ts`
Notes             `notes`, `note_versions`                                                                                                                                                     `notesStore.ts`, `userNotes.ts`                       
Business Hub      `kanban_boards`, `kanban_tasks`, `kanban_columns`, `projects`, `sprints`, `calendar_events`, `diary_entries`, `business_resources`, `business_tags`, `business_graph_edges`  `business/` (all 13 files)                            
Calling           `call_sessions`, `call_participants`, `call_signals`                                                                                                                         `callingStdb.ts`, `wabi_call_state_bridge/`           
Users/Auth        `users`, `sessions`, `devices`                                                                                                                                               `authStore.ts`, `authSession.ts`                      
Friends           `friends`, `friend_requests`                                                                                                                                                 `peopleTracker.ts`, `friend request UI`               
Moderation        `bans`, `mutes`, `role_definitions`, `rbac_assignments`                                                                                                                      `presenceStore.ts`                                    
Encryption        `encryption_keys`, `dm_messages.ciphertext_*`                                                                                                                                `encryption.ts`                                       

12.5 MIGRATION PATH FROM SPACETIMEDB (BRIEF)

This section is brief because the end-state doc explicitly is not a migration plan. The full migration is its own document (docs/futuresight-wabidb-migration.md, to be written). The high-level shape:

1. **Both engines side-by-side.** The `WabiState` trait (Section 10.1) is the boundary. For the duration of the migration, the trait has two implementations: `StdbBackend` (current) and `WabiDbBackend` (new). wabi-server is configured at startup to use one or the other (or both, with a routing rule).
2. **Per-feature rollout.** Each Wabi feature (channels, DMs, whiteboards, calls, etc.) is migrated independently. A user account can have some state in STDB and some state in WabiDB; the frontend doesn't see the difference.
3. **Routing rule.** A simple convention: "data created before date X is in STDB, data created after date X is in WabiDB." The migration scripts copy existing data to WabiDB. After the cutover date, all new writes go to WabiDB. Old reads from STDB are still served for backward compatibility during a grace period (default 30 days).
4. **Fallback.** If WabiDB fails, the routing rule can be reverted. The server admin re-enables STDB for the failed feature. Because both engines write to the same logical data, this is a configuration change, not a data migration.
5. **Cleanup.** After the grace period, the STDB bridges and `stdb_bindings/` directory are deleted. The engine becomes the only source of truth.

The migration is not free, but it is bounded. Each feature's migration is a one-time script that copies the STDB tables to the WabiDB tables (with a possible transformation for things like the schema-version column). The WabiState trait is the safety net: if WabiDB fails, you switch back to STDB and keep running.

============================================================

------------------------------------------------------------
13. WHAT THIS IS NOT
------------------------------------------------------------

This section reaffirms the design boundary.

Not a SQL database. WabiDB has no SQL parser, no ad-hoc queries, no SELECT *, no JOIN, no GROUP BY, no ORDER BY, no subqueries, no aggregations. Data is read through command return values, not through SQL queries. The subscription system uses a fixed topic grammar, not SQL. If a future feature requires complex read-side queries (e.g., "show me all messages from user X in channel Y from last week"), that query is either written as a specific Rust command or forwarded to a search helper node. The engine does not grow a general query planner.

Not a game engine. No spatial queries. No physics. No game loop. No client-side prediction. No server-authoritative movement. Turn trackers and dice logs are simple CRUD tables, not game engine primitives.

Not a programmable app platform. No WASM. No V8. No module lifecycle. No clientconnected/clientdisconnected reducers. No user-uploaded logic. No syscall ABI. The engine runs exactly the code that is compiled into it. If someone wants to add a custom integration, they write a Rust command, recompile, and deploy.

Not federation. WabiDB runs one instance per Wabi server. Cross-server messaging is handled by wabi-server's Socket.IO relay infrastructure, not by the database engine.

Not multi-tenant SaaS. WabiDB is a single-tenant engine. There is no "database per tenant" abstraction at the engine level. If the product evolves to need per-community databases, that is implemented by running multiple wabi-node processes, each with its own WabiDB instance.

Not user-installed-backend-code. The end state has no equivalent of STDB's #[spacetimedb::reducer] attribute or the module WASM upload flow. All commands are compiled into the engine. If the product needs a plugin system later, that plugin system is a separate layer that calls WabiState::execute(), not a WASM runtime inside the engine.

What happens when the boundary is pushed: If a future requirement genuinely needs ad-hoc queries (unlikely for Wabi's use case), the boundary is revisited. But the response is not "make the engine more general." It is "build a query layer on top of the engine that reads from the same SQLite file." The engine itself stays focused.

============================================================

------------------------------------------------------------
14. THE END STATE IN ONE PARAGRAPH
------------------------------------------------------------

WabiDB is a purpose-built Rust database engine and live-subscription service, embedded in wabi-server, that persists all Wabi state in a single SQLite WAL-mode file, exposes atomic state mutations as typed Rust commands that atomically write both state rows and outbox notification rows, pushes real-time changes to browser clients over a WebSocket topic-subscription protocol, stores only public encryption keys (never private), encrypts DM content per-recipient with client-side AES-GCM keys derived from ECDH shared secrets, supports a bounded set of 16 object types (channels, DMs, whiteboards, kanban boards, places, notes, calls, etc.), enforces per-class retention policies with a periodic TTL reaper, manages helper nodes (media, search, transcode, cache) through a signed-token pairing protocol, and replaces the entirety of SpacetimeDB's role in Wabi — eliminating the BSL license constraint, the WASM module runtime, the SQL query engine, the in-memory-only committed state, the generic database scope, and the lack of privacy/retention primitives — while preserving every feature the existing frontend implements, because the data model is derived directly from the frontend source.

============================================================

------------------------------------------------------------
15. SOURCE MAP AND VERIFICATION
------------------------------------------------------------

15.1 FRONTEND SOURCES JUSTIFYING DESIGN CHOICES

Whiteboard data model. The WhiteboardLayer type at frontend/src/lib/whiteboard/boardTypes.ts:11-21 defines id, name, kind, visible, locked, opacity, order, createdAt, updatedAt. The layer kind is 'content' | 'reference' | 'background' (line 3). The WhiteboardElement shapes at elementTypes.ts:36-76 define the full element schema with all property columns. The layer operation patch types at boardLayerOps.ts:6 (layer:create, layer:update, layer:delete, layer:reorder) and element patch ops at boardElements.ts:8 (create, update, delete, reorder) directly map to command names. These files are the authoritative source for the whiteboard schema — every column in the section 3.6 tables is derived from them.

Map/places data model. PlaceRecord at frontend/src/lib/placeRegistry.ts:17-34 defines the full place schema with aliases, building, floor, lat, lon, modelUrl, mapImageUrl, mapRotation, poiThemePreset, mapLayers, pois, tags. PlaceMapLayerRecord at placeRegistry.ts:40-46 and PlacePoiRecord at placeRegistry.ts:48-60 define the child schemas. PlaceDraft at placeDraft.ts:62-78 adds draft-specific fields. These justify the places schema in section 3.7.

Business data model. shared/businessContracts.ts:27-44 defines Todo (kanban task) with id, title, description, status, priority, estimatedMinutes, dueDate, createdBy, assignedTo, tags, projectId, completedAt. KanbanColumn at business/types.ts:33-38 defines id, label, color, visible. CalendarEvent at businessContracts.ts:52-66 and DiaryEntry at businessContracts.ts:68-80 define the calendar and diary schemas. Project at businessContracts.ts:82-95 and Sprint at businessContracts.ts:97-108 define the project/sprint schema. These justify the business tables in section 3.9.

Call data model. wabicallstatebridge/src/lib.rs:4-43 defines the three call tables. The frontend call reducer calls at frontend/src/lib/callingStdb.ts and the STDB bindings at stdbbindings/callsession* files confirm the exact field set. These justify section 3.5.

Encryption. frontend/src/lib/encryption.ts:10-39 (generateKeyPair using ECDH P-256), lines 44-91 (deriveSharedKey), and lines 164-182 (encryptForUsers with per-recipient keys) define the encryption pattern that section 6 codifies.

15.2 STDB SOURCES THAT INFORMED THE DESIGN

The following STDB sources were studied for conceptual inspiration, not translated:

* **Reducer model** (`crates/core/src/host/module_host.rs`): the command/transaction pattern (open tx, run reducer, commit or rollback) is identical in spirit. WabiDB replaces the WASM bridge with a direct Rust function call.
* **SendWorker** (`crates/core/src/subscription/module_subscription_manager.rs:1714`): the off-main-thread aggregation of per-client updates is directly adapted in WabiDB's subscription manager. The comment at lines 1385-1387 ("get this work off the main thread") is preserved as a design principle.
* **Event table** (`crates/lib/src/db/raw_def/v10.rs:279-284` and `crates/datastore/src/locking_tx_datastore/committed_state.rs:619-622`): the ephemeral (write-to-commitlog, don't-merge-to-state) pattern is adapted as WabiDB's `ephemeral` retention class for call signals. WabiDB avoids the commitlog persistence issue by simply not writing ephemeral rows to SQLite at all.
* **Subscriptions by delta evaluation** (`crates/core/src/subscription/subscription.rs` and `crates/subscription/src/lib.rs`): the concept of evaluating what changed and pushing only deltas is preserved. The implementation is replaced by the explicit outbox pattern.
* **Blob store content addressing** (`crates/table/src/blob_store.rs`): the BLAKE3 content-addressed blob storage concept is adapted for file attachments, minus the cross-snapshot hardlinking.
* **Locking and transactions** (`crates/datastore/src/locking_tx_datastore/datastore.rs:64`): STDB's serialized-writer model (single `MutTxId` at a time) is preserved, but the implementation is delegated to SQLite's own serialized transaction model (one writer, concurrent readers in WAL mode).

15.3 VERIFICATION NOTES

Verified claims: All frontend-derived schema claims are verified against the actual source files listed above. The STDB bridge tables at wabistatebridge/src/lib.rs (30+ tables) and wabicallstatebridge/src/lib.rs (3 tables) have been read in full. The whiteboard frontend code (18 files) has been read in full. The business frontend code (13 files) has been read in full. The map/places code (6 files) has been read in full. The 53 files in stdbbindings/ have been confirmed to match the bridge reducer signatures.

Aspirational claims: The exact WebSocket frame format (BSATN vs JSON, envelope shapes) is aspirational — the format will be finalized during implementation. The FTS5 setup and BM25 ranking parameters are aspirational defaults. The 5-25ms adaptive batching range is a starting point that will be tuned. The TTL reaper interval of 60 seconds is a default. The backup frequency of 6 hours is a default. All of these are configuration parameters, not architecture.

STDB-derived claims: The conceptual derivations from STDB (SendWorker, reducer model, event tables, blob store) are verified against the cited source files. The specific line numbers cited are accurate as of STDB 2.6.0. No STDB source code has been copied into this document. Short excerpts (≤10 lines) have been attributed with file:line citations where load-bearing.

Corrected error from the architecture study: Section 10 of stdb-architecture-study.md claimed that event tables have a private/sender column and are V2-only. The actual STDB source at crates/datastore/src/lockingtxdatastore/committedstate.rs:619-622 and crates/core/src/subscription/subscription.rs:26-28 confirms: event tables skip committed-state merge, are excluded from initial subscribe-all, and are visible to subscribers who explicitly query them. The private column and V2-only claims are not present in the authoritative sources (the isevent field at raw_def/v10.rs:279-284 says nothing about private or V2-only). This document uses the corrected understanding: event tables are a write-only-not-committed tier, with no private/sender/V2-only semantics. WabiDB's ephemeral tier is similar: write to outbox only, never to SQLite.

15.4 TESTING STRATEGY

The engine's correctness depends on three things being true at all times: (1) commands atomically commit state and outbox rows, (2) subscribers receive every committed event exactly once in causal order, and (3) encryption is correct end-to-end. The test strategy targets all three with layered tests.

Layer 1: Per-command unit tests. Every command in the registry has at least one test. The test creates a fresh SQLite file, opens the engine, runs the command with a fake CallerIdentity, asserts the returned value, and asserts the resulting state in the database. Per-command tests catch logic bugs, permission bugs, and validation bugs.

Layer 2: Transaction atomicity tests. A set of tests specifically target the atomicity invariant:

* A test that runs a command which writes state rows and outbox rows, then forces a failure halfway through (e.g., a panic in the command body). The test asserts that no state rows were committed and no outbox rows were committed.
* A test that runs two commands concurrently (in different threads) on the same database. The test asserts that one wins, the other gets a serialization error, and the database is consistent.
* A test that runs a command which enqueues 10 outbox rows and crashes the engine mid-transaction. On restart, the test asserts that no outbox rows were committed and no state mutations were applied.

Layer 3: Subscription delivery tests. A test harness simulates multiple WebSocket clients:

* The harness creates a fresh engine and connects N client stubs.
* Each client subscribes to a topic.
* The harness runs a command that emits an outbox row.
* The harness asserts that all N clients receive the corresponding event within a timeout, in the correct order, with the correct payload.
* The harness runs a command that emits 1,000 outbox rows in quick succession. The harness asserts that all events are delivered (none lost, none duplicated) and that the per-client batch boundaries are respected.
* The harness drops a client connection mid-stream. The harness asserts that the other clients are unaffected and the dropped client does not cause the engine to block.
* The harness forces the outbox consumer to fall behind (artificial sleep in the consumer). The harness asserts that backpressure frames are sent and that no client is starved indefinitely.

Layer 4: End-to-end encryption tests. (See Section 6.7.) Roundtrip, key rotation, multi-device, ciphertext integrity, and retention. Property-based tests (using proptest or similar) generate random keypairs and random plaintext, run the encryption, run the decryption, and assert that the plaintext matches.

Layer 5: Migration tests. Every migration file in wabidb::storage::migrations is tested as follows:

* A test creates a database at the previous schema version (using a snapshot of the database file from before the migration was written).
* The test runs the migration.
* The test asserts that all expected tables, columns, and indices exist at the new schema version.
* The test asserts that no data was lost (count rows before and after).
* The test runs the down-migration (if it exists) and asserts the database returns to the previous state.

Layer 6: Load tests. A separate test suite (run manually or in CI nightly, not on every commit) measures:

* Sustained write throughput: how many `send_message` commands per second can the engine commit?
* Read concurrency: how many concurrent subscribers can read without affecting write throughput?
* Outbox drain rate: when the outbox is at 100,000 rows backlog, how long does it take to drain?
* Memory usage: what is the RSS at idle, at 1,000 subscribers, at 10,000 subscribers?
* Latency percentiles: p50, p95, p99 for command commit latency and for end-to-end subscription delivery latency.

The target numbers, derived from the SQLite ceiling in Section 8.8: 1,000 writes/sec sustained, <100ms p95 commit latency, <200ms p95 end-to-end delivery on LAN, <500ms on internet.

Layer 7: Real-world test. A test deployment on a real server (Ronin or similar) with real users (a small friend group) for at least one week before any production cutover. The test measures the same things as Layer 6 but in the wild. A real-world test catches things lab tests miss: slow client devices, weird network conditions, the timezone that wasn't tested, the Wabi user who runs 17 tabs.

The test harness. Layer 3 requires a multi-client test harness. The harness is a small Rust binary that:

* Spins up the engine in-process.
* Exposes a function `connect_client() -> ClientStub` that returns a struct simulating a WebSocket connection.
* Provides `ClientStub::subscribe`, `ClientStub::receive`, `ClientStub::send_command`.
* Allows assertions on the client's view of the world (received events, last seen snapshot, etc.).

The harness is a #[cfg(test)] module in the same crate as the engine. It is not shipped in production builds. It is the primary tool for catching the kind of bugs that are easy to introduce in subscription delivery: ordering, batching, loss, duplication, and backpressure.

Test isolation. Each test runs against a fresh SQLite file in /tmp/wabidb-test-{uuid}/. Tests do not share state. The StorageEngine::open function takes a path argument, so tests can spin up as many engines as they need. CI runs the full test suite in parallel; each test is independent.

Coverage target. The goal is 80% line coverage for wabidb::storage, wabidb::commands, wabidb::subscription, and wabidb::crypto. These are the four crates where bugs are most likely to corrupt data or break user-visible behavior. The remaining 20% is acceptable for code that is exercised only in unusual edge cases or in test-only modules.

============================================================

============================================================
PART 2: SPACETIMEDB ARCHITECTURE STUDY (REFERENCE)
============================================================

The research foundation. ~8,200 words. 17 sections. One section (10) had a hallucination in an earlier draft; it was corrected on 2026-06-18 with verification against the actual STDB source.

============================================================

============================================================
SPACETIMEDB ARCHITECTURE STUDY
============================================================

  | An independent technical review conducted from the perspective of a self-hosted, privacy-first chat/social platform evaluating whether to keep using SpacetimeDB or build a purpose-built replacement.
  | 
  | Source tree: `/var/home/Ronin/vendor/SpacetimeDB` (tag v2.6.0)
  | Date: 2026-06-18

============================================================

------------------------------------------------------------
1. WHAT SPACETIMEDB ACTUALLY IS
------------------------------------------------------------

SpacetimeDB is a combined database engine and application runtime that compiles user-supplied modules (Rust, C#, TypeScript/JavaScript via V8, or C++) into WASM or V8 bytecode, executes them as "reducers" within database transactions, and pushes live row-level deltas to subscribed WebSocket clients. The canonical deployment model is a single binary (spacetimedb) that acts as both the storage engine and the application server.

The "zen" of SpacetimeDB can be summarized as: in-memory committed state + append-only commitlog for durability. There is no page buffer pool cache in the traditional sense — the entire committed state of every table lives in memory as a collection of BTree-backed pages (crates/table/src/table.rs, the Table struct). The commitlog (crates/commitlog/src/lib.rs) serves as the write-ahead log: every committed transaction is serialized and appended before the outcome is reported to the caller. Snapshots (crates/snapshot/src/lib.rs) exist as a recovery optimization so that restarting a database does not require replaying the entire commitlog from offset 0.

The key architectural claim — "the database is the server" — is realized through this colocation: client connections arrive over WebSocket (or HTTP for ad-hoc queries), the connection handler deserializes the reducer call, the module host invokes the WASM/V8 function inside a transaction, the transaction commits to the in-memory state and the commitlog, and the subscription manager computes the delta between the pre- and post-transaction state and pushes it to every subscribed client. All of this happens in a single process.

The workspace at /var/home/Ronin/vendor/SpacetimeDB/Cargo.toml:118 declares version 2.6.0, edition 2024, rust-version 1.93.0.

The database side is built on a collection of carefully separated Rust crates — roughly 40+ workspace members — that the rest of this document will walk through in detail. From a product perspective, what a user or operator sees is: a single spacetimedb start command boots an HTTP+WebSocket server that accepts connections, module uploads, and SQL queries. The server compiles uploaded modules (Rust via wasm-pack, C# via the spacetimedb-csharp-codegen toolchain, TypeScript via rolldown + V8), runs them, and maintains the database state. There is no separate database process, no ORM, no API gateway to deploy. This colocation is the defining architectural decision.

============================================================

------------------------------------------------------------
2. LICENSE AND LICENSING REALITY
------------------------------------------------------------

The repository is licensed under the Business Source License 1.1 (BSL 1.1), with the full text at /var/home/Ronin/vendor/SpacetimeDB/LICENSE.txt.

KEY LICENSE PARAMETERS (LINES 7-27):

* **Licensor**: Clockwork Laboratories, Inc.
* **Licensed Work**: SpacetimeDB 2.6.0
* **Additional Use Grant** (lines 12-16):
  > You may make use of the Licensed Work provided your application or service uses the Licensed Work with no more than one SpacetimeDB instance in production and provided that you do not use the Licensed Work for a Database Service.
* **"Database Service" definition** (lines 18-22):
  > A "Database Service" is a commercial offering that allows third parties (other than your employees and contractors) to access the functionality of the Licensed Work by creating tables whose schemas are controlled by such third parties.
* **Change Date**: 2031-06-15 (line 24)
* **Change License**: GNU Affero General Public License v3.0 with a linking exception (lines 26-27)

PRACTICAL IMPLICATIONS FOR A SELF-HOSTED CHAT PLATFORM

The "one SpacetimeDB instance in production" restriction means that an app running its own single SpacetimeDB server for its own users is within the Additional Use Grant. However, any scenario that requires multiple logical databases (e.g., per-community isolated databases for privacy compartmentalization) would need either an explicit commercial license or a creative interpretation of "one instance." The Database Service clause targets cloud providers reselling STDB as a service — it does not appear to restrict an app that controls its own tables. But the "one instance" constraint is a meaningful architectural limitation: you cannot shard across multiple STDB processes without a license negotiation.

The change license (AGPLv3 + linking exception) means that after June 2031, the code becomes AGPL with an additional permission that waives the network-interaction source distribution requirement for modified versions that link to STDB. Before that date, the BSL terms apply.

The local clone is definitively identified as SpacetimeDB 2.6.0 at Cargo.toml:118.

============================================================

------------------------------------------------------------
3. CRATE LAYOUT AND RESPONSIBILITIES
------------------------------------------------------------

The workspace members are declared at Cargo.toml:3-79. Below is a crate-by-crate breakdown.

The crate map is not a random decomposition — each line maps to a separable concern that a reimplementation would either adopt or deliberately replace. Below is the catalog.

`CRATES/CORE` (SPACETIMEDB-CORE)
The central orchestrator. Contains the HostController (src/host/hostcontroller.rs), module host actor (src/host/modulehost.rs and src/host/wasmcommon/modulehostactor.rs), subscription manager (src/subscription/modulesubscription_manager.rs), client connection handling, identity/auth, the scheduler (src/host/scheduler/), and the WasmInstanceEnv host function bridge (src/host/mod.rs). Every running module is registered in HostController.hosts as a HostCell — an Arc<AsyncRwLock<Option<Host>>> in an IntMap<u64, HostCell> (lines 67-70). The HostController also owns the shared PagePool (line 114) and the BsatnRowListBuilderPool (line 120).

`CRATES/ENGINE` (SPACETIMEDB-ENGINE)
The RelationalDB struct (src/relationaldb.rs) and persistence wiring (src/persistence.rs). RelationalDB wraps Locking (the MVCC datastore), the durability layer, and the snapshot worker. Provides beginmuttx, committx, rollback, and open (which does snapshot restore + commitlog replay). The SNAPSHOT_FREQUENCY constant at line 126 is 1,000,000 transactions.

`CRATES/DATASTORE` (SPACETIMEDB-DATASTORE)
The transactional storage core. Locking (src/lockingtxdatastore/datastore.rs) holds a CommittedState behind an Arc<RwLock<...>> and a SequencesState behind Arc<Mutex<...>>. Lock ordering is documented at lines 54-61: memory, then committedstate, then sequencestate. Contains TxId (read-only transaction), MutTxId (mutable transaction), TxState (per-tx dirty state), CommittedState (the in-memory committed snapshot), and the replay logic (src/lockingtxdatastore/replay.rs). The IsolationLevel enum (src/traits.rs) defines Serializable and Snapshot variants.

`CRATES/COMMITLOG` (SPACETIMEDB-COMMITLOG)
The write-ahead log. Commitlog<T, R> (src/lib.rs:164) is parameterized by record type T (canonically payload::Txdata) and repository backend R (canonically repo::Fs). The Options struct (lines 42-101) controls segment size (default 1 GiB), offset index interval (default 4 KiB), fsync behavior, write buffer size (default 128 KiB). Records are committed via Commitlog::commit() which writes a batch of Transaction<T> as a single Commit. The payload Txdata is defined in src/payload/txdata.rs and encodes the full set of row inserts and deletes per table.

`CRATES/DURABILITY` (SPACETIMEDB-DURABILITY)
The abstract Durability trait (src/lib.rs:127): appendtx, durabletxoffset, close. Also the History trait (line 185): foldtransactionsfrom, transactionsfrom, txrangehint. The local (commitlog-based) implementation is in src/imp/.

`CRATES/SNAPSHOT` (SPACETIMEDB-SNAPSHOT)
Snapshot create/restore. SnapshotRepository (src/lib.rs:750) holds a directory, database identity, and replica id. createsnapshot (line 781) captures pages and blobs at a given TxOffset into an UnflushedSnapshot, which is later made durable by syncall. Snapshots are content-addressed: pages and blobs are stored as files named by their BLAKE3 hash, enabling hardlink-based deduplication across snapshots via prevsnapshot. readsnapshot (line 959) reconstructs a ReconstructedSnapshot containing a HashMapBlobStore and a BTreeMap<TableId, Vec<Box<Page>>>.

`CRATES/TABLE` (SPACETIMEDB-TABLE)
The PagePool (src/pagepool.rs), BlobStore trait and HashMapBlobStore (src/blobstore.rs), Table struct with indexes and BTree-backed pages, RowPointer, and the page layout (src/page.rs, src/pages.rs). The page pool is an object pool of Box<Page> that is shared across all databases on a host (see HostController.pagepool at crates/core/src/host/hostcontroller.rs:114). This is not per-database — it is a single pool that all CommittedStates on the same process draw from.

`CRATES/SUBSCRIPTION` (SPACETIMEDB-SUBSCRIPTION)
The subscription plan and evaluation compiler. SubscriptionPlan (src/lib.rs) holds compiled Fragments (insert and delete plan fragments built from PipelinedIxScan, PipelinedIxJoin, PipelinedProject). compilefromplan produces these from a PhysicalPlan. The subscription SQL is compiled via compile_subscription from crates/query/.

`CRATES/LIB` (SPACETIMEDB-LIB)
Schema definitions, the SATS typespace, AlgebraicType / AlgebraicValue, ProductType / ProductValue, Identity, ConnectionId, the RawModuleDefV9/V10 builders, and the RawTableDefV10 with its isevent field (src/db/rawdef/v10.rs:279-284).

`CRATES/SATS` (SPACETIMEDB-SATS)
The Spacetime Algebraic Type System (SATS) and BSATN (binary SATS) encoding/decoding. All types used in tables, subscriptions, and the commitlog are expressed as AlgebraicTypes and serialized as AlgebraicValues through BSATN.

`CRATES/BINDINGS`, `CRATES/BINDINGS-MACRO`, `CRATES/BINDINGS-SYS`
The Rust module author SDK. spacetimedb::table, spacetimedb::reducer, spacetimedb::procedure, etc. These macros generate the describemodule, callreducer, etc. exports and (de)serialize the C ABI between the module and host.

`CRATES/STANDALONE` (SPACETIMEDB-STANDALONE)
The server binary. main.rs and src/lib.rs wire together: a ControlDb (sled-backed metadata store, src/control_db.rs), DiskStorage for module bytes, HostController, ClientActorIndex, Prometheus metrics, the JWT auth provider, WebSocket options. StandaloneEnv::init (line 63) orchestrates all of this.

`CRATES/CLIENT-API` AND `CRATES/CLIENT-API-MESSAGES`
HTTP and WebSocket route handlers. WebSocket upgrade, subscription messages, the v1 and v2 protocol message types (wsv1::, wsv2::). The v2 protocol is the typed-query-builder protocol; v1 is the legacy JSON/BSATN protocol.

`CRATES/CLIENT` (IN CORE)
Client connection actor loop, ClientConnectionSender, ClientActorIndex, message serialization.

`CRATES/SQL-PARSER` AND `CRATES/QUERY`
SQL parsing (via sqlparser crate) and query compilation into physical plans. crates/query/src/lib.rs has compilesubscription for subscription compilation and compilequery for ad-hoc query compilation.

`CRATES/PHYSICAL-PLAN`, `CRATES/EXECUTION`, `CRATES/EXPR`, `CRATES/QUERY-BUILDER`
The query plan layer. PhysicalPlan types (IxScan, TableScan, ProjectPlan, JoinPlan), PipelinedExecutor (crates/execution/src/pipelined/), CollectViews, ParamResolver. The query-builder crate provides the V2 typed-query-builder protocol used by newer SDKs.

`CRATES/V8` (IN CORE AT `SRC/HOST/V8/`)
V8 module runtime. JsMainInstance for the main lane (reducers, subscriptions, one-off queries) and JsProcedureInstance for the procedure pool. Described in the module-level doc comment at src/host/v8/mod.rs:1-55. Worker threads hold their own V8 isolates.

`CRATES/WASMTIME` (IN CORE AT `SRC/HOST/WASMTIME/`)
WASM module runtime via Wasmtime. ModuleInstance, WasmtimeModule, fuel-based energy budgeting.

`CRATES/WASM_COMMON` (IN CORE AT `SRC/HOST/WASM_COMMON/`)
The WasmModuleHostActor at src/host/wasmcommon/modulehostactor.rs and the WasmInstance trait (line 79): callreducer, callprocedure, callview, callviewanon, callhttphandler. Shared by both WASM and V8 backends.

`CRATES/FS-UTILS`, `CRATES/PATHS`, `CRATES/RUNTIME`, `CRATES/DATA-STRUCTURES`
Utility crates. fs-utils has DirTrie (content-addressed directory tree), CompressReader/Writer, lockfile. paths provides strongly-typed path wrappers (ServerDataDir, ReplicaDir, CommitLogDir, SnapshotsPath, etc.). runtime provides Handle for tokio runtime handles. data-structures provides custom HashMap, HashSet, IntMap, Pool, SmallHashMap, ErrorStream.

`CRATES/SCHEMA` (SPACETIMEDB-SCHEMA)
The canonical TableSchema, ColumnSchema, IndexSchema, ViewDef, ModuleDef, ReducerDef, ProcedureDef, migration planning (auto_migrate). This crate is where the type-level representation of "what a table is" lives. If Wabi defines its own schema system, this is the crate to study for the feature surface.

============================================================

------------------------------------------------------------
4. STORAGE MODEL: PAGES, TABLES, AND BLOBS
------------------------------------------------------------

PAGE POOL

The PagePool at crates/table/src/pagepool.rs:26 wraps a generic Pool<Box<Page>> — an object pool of Page boxes. It is shared across all databases on a host. The HostController holds one pub pagepool: PagePool at crates/core/src/host/host_controller.rs:114. Every Locking datastore and its CommittedState hold a handle to this same pool.

PagePool::new (line 44) accepts an optional max size in bytes (default 128 × sizeof::<Page>()). takewithfixedrow_size retrieves or creates a page configured for a given fixed row size. put returns a page to the pool.

PAGES

Each Page (crates/table/src/page.rs) is a fixed-size block that can hold multiple rows of a single table. The page tracks which slots are occupied via a bitset (present_rows). Pages are BTree leaves: a Table is a collection of pages organized in a BTree keyed by row pointer.

TABLES

Table (crates/table/src/table.rs) wraps a BTreeMap<RowPointer, Box<Page>> plus indexes. When rows are inserted, they fill slots in existing pages or allocate new ones from the PagePool. The BlobStore is threaded through all table operations so that large variable-length columns are stored as content-addressed blobs rather than inline in the page BTree.

BLOB STORE

BlobStore trait at crates/table/src/blob_store.rs:67:
* `clone_blob` (reference count increment)
* `insert_blob` (content-address + store)
* `retrieve_blob`
* `free_blob` (refcount decrement)
* `iter_blobs` (for snapshot creation)
* `insert_with_uses` (for snapshot restore)

HashMapBlobStore (line 164) is the default in-memory implementation — a HashMap<BlobHash, BlobObject> where BlobObject holds a uses: usize refcount and blob: Box<[u8]>. Blobs are content-addressed via BLAKE3 of their bytes (BlobHash::hashfrombytes, line 36). This store is used directly in CommittedState (crates/datastore/src/lockingtxdatastore/committedstate.rs:66: pub(crate) blobstore: HashMapBlobStore).

PAGE POOL SHARING

The page pool is explicitly shared across databases. From CommittedState at crates/datastore/src/lockingtxdatastore/committed_state.rs:74-77:
  | "Between transactions, this is untouched. During transactions, the [`MutTxId`] can steal pages from the committed state. This is a handle on a shared structure. Pages are shared between all modules running on a particular host, not allocated per-module."

============================================================

------------------------------------------------------------
5. COMMITLOG AND DURABILITY
------------------------------------------------------------

COMMITLOG STRUCTURE

The Commitlog at crates/commitlog/src/lib.rs:164 is the canonical WAL. The Options struct (lines 42-101):
* `max_segment_size`: default 1 GiB (line 110)
* `offset_index_interval_bytes`: default 4 KiB (line 111)
* `offset_index_require_segment_fsync`: default true (line 112)
* `write_buffer_size`: default 128 KiB (line 114)
* `preallocate_segments`: default false (line 113)

Records are appended via Commitlog::commit() (line 475), which takes an IntoIterator<Item = Transaction<T>>. The method acquires a write lock on inner, writes the commit, and returns a Committed struct containing the offset range and checksum. Data is buffered; callers must call flush() or flushandsync() to persist to storage.

The commitlog segment is the on-disk file. Each segment has a log format version, a header, and a sequence of commits. The repo::Fs backend creates files in the directory given by CommitLogDir. Segments are named by their starting transaction offset.

DURABILITY TRAIT

crates/durability/src/lib.rs:127 defines the Durability trait: appendtx (non-blocking), durabletxoffset (returns a DurableOffset handle), and close (async shutdown). The History trait (line 185) provides replay via foldtransactionsfrom and transactionsfrom.

The local implementation (in crates/durability/src/imp/) wraps a Commitlog and a background flush/sync worker. appendtx submits a PreparedTx<Txdata> to an internal channel; a worker drains the channel and writes to the commitlog, calling flushand_sync periodically.

RELATIONALDB PERSISTENCE WIRING

crates/engine/src/persistence.rs exposes DurabilityConfig and CommitlogConfig that map user-facing config to spacetimedbdurability::local::Options and spacetimedbcommitlog::Options. LocalPersistenceProvider creates per-replica commitlog directories and snapshot directories.

SNAPSHOTS

crates/snapshot/src/lib.rs:3-7:
  | "A snapshot is an on-disk view of the committed state of a database at a particular transaction offset. Snapshots exist as an optimization over replaying the commitlog; when restoring to the most recent transaction, rather than replaying the commitlog from 0, we can reload the most recent snapshot, then replay only the suffix of the commitlog."

SnapshotRepository::createsnapshot (line 781) captures all tables (pages) and blobs into a content-addressed object repository using DirTrie. Objects are stored as files named by their BLAKE3 hash. If a prevsnapshot is supplied, objects are hardlinked to avoid re-writing unchanged pages/blobs.

read_snapshot (line 959) reads a ReconstructedSnapshot containing a HashMapBlobStore and a BTreeMap<TableId, Vec<Box<Page>>>. The caller (typically RelationalDB::open) installs these into a new Locking / CommittedState.

REPLAY

crates/datastore/src/lockingtxdatastore/replay.rs:35 starts applyhistory, which creates a Replay struct and calls history.foldtransactionsfrom(starttxoffset, &mut replay). The Replay struct implements Decoder — each decoderecord call processes one transaction by iterating over its table entries and calling replayinsert or replaydelete.

FULL DURABILITY PIPELINE

1. A reducer runs inside a `MutTxId`. On commit (`RelationalDB::commit_tx`), the `TxState` is merged into `CommittedState` and the `TxData` is serialized.
2. The `TxData` is sent to the durability layer via `append_tx`.
3. The durability worker writes the `TxData` to the commitlog buffer and periodically calls `flush_and_sync`.
4. When the durable offset advances by `SNAPSHOT_FREQUENCY` (1,000,000), a snapshot is triggered.
5. On restart, `RelationalDB::open` calls `restore_from_snapshot_or_bootstrap` to load the latest snapshot, then `apply_history` to replay the commitlog suffix after the snapshot's offset.

============================================================

------------------------------------------------------------
6. TRANSACTION MODEL: MVCC, LOCKING, AND ISOLATION
------------------------------------------------------------

LOCKING ARCHITECTURE

The top-level datastore is Locking at crates/datastore/src/lockingtxdatastore/datastore.rs:64:

    pub struct Locking {
        pub committed_state: Arc<RwLock<CommittedState>>,
        pub(super) sequence_state: Arc<Mutex<SequencesState>>,
        pub(crate) database_identity: Identity,
    }

The lock acquisition order is documented at lines 54-61: memory → committedstate → sequencestate. This ordering prevents deadlocks.

READ TRANSACTIONS (`TXID`)

TxId (crates/datastore/src/lockingtxdatastore/tx.rs:26) holds a SharedReadGuard<CommittedState> — a shared (read) lock on the committed state. Multiple readers can proceed concurrently. The TxId implements StateView for scans and index lookups.

WRITE TRANSACTIONS (`MUTTXID`)

MutTxId (crates/datastore/src/lockingtxdatastore/muttx.rs) acquires an exclusive (write) lock on CommittedState. While held, it also holds a TxState that accumulates dirty data: inserttables, deletetables, pendingschema_changes. Writers serialize — only one mutable transaction can be in-flight at a time per database.

TXSTATE

TxState at crates/datastore/src/lockingtxdatastore/tx_state.rs:21-47:
* `insert_tables: BTreeMap<TableId, Table>` — rows inserted during this transaction.
* `delete_tables: BTreeMap<TableId, DeleteTable>` — rows deleted during this transaction.
* `blob_store: HashMapBlobStore` — blobs inserted in this transaction.
* `pending_schema_changes: Vec<PendingSchemaChange>` — schema mutations (create table, drop table, create index, drop index).

COMMIT PATH

When RelationalDB::commit_tx is called, the MutTxId is consumed:
1. The in-memory state in `CommittedState` is updated with the inserted/deleted rows from `TxState`.
2. The committed `TxData` (the delta) is serialized for the commitlog.
3. The write lock is released.
4. The `TxData` is sent to the durability layer.
5. The subscription manager receives the delta and evaluates subscription queries.

ROLLBACK

Rollback reverses schema changes in reverse order (CommittedState::rollback, line 679). Inserted pages are returned to the page pool. Deleted rows are unmarked.

ISOLATION LEVEL

IsolationLevel at crates/datastore/src/traits.rs:23-100 defines Serializable and Snapshot. The documentation explains that the datastore must treat unsupported isolation levels as the strongest supported level. In practice, the serialized-writer model plus snapshot reads means the effective isolation is serializable for writers (only one writer at a time) and snapshot for readers (readers see a consistent committed-state snapshot taken at their start).

VIEW READ SETS

CommittedState also tracks viewreadsets: IntMap<ViewId, ViewReadSets> (at committed_state.rs:81+). These track which tables each view reads from. When a reducer writes to a table, the system checks whether any view reads that table; if so, the view is re-evaluated. This is the core of the incremental view maintenance system.

============================================================

------------------------------------------------------------
7. MODULE HOST: WASM AND V8
------------------------------------------------------------

HOSTCONTROLLER

crates/core/src/host/host_controller.rs:98 is the central registry:
* `hosts: Hosts` (i.e., `Arc<Mutex<IntMap<u64, HostCell>>>`)
* `page_pool: PagePool` — shared across all databases
* `runtimes: Arc<HostRuntimes>` — holds `WasmtimeRuntime` and `V8Runtime`
* `energy_monitor: Arc<dyn EnergyMonitor>`
* `persistence: Arc<dyn PersistenceProvider>`

Each HostCell is an Arc<AsyncRwLock<Option<Host>>>. A Host wraps a ModuleHost (behind a watch channel for live-update subscribers) and a DatabaseLogger.

WASM RUNTIME

The Wasmtime runtime at crates/core/src/host/wasmtime/ uses fuel metering for energy budgeting. WasmtimeModule compiles the WASM bytecode. ModuleInstance (a WasmtimeModule plus an instantiated Instance) holds the store and linker. Fuel is set via FunctionBudget::DEFAULT_BUDGET when an instance is created; each WASM instruction consumes fuel, and when fuel runs out the reducer is terminated with OutOfEnergy.

The async variant is used for procedures; the sync variant is used for the main reducer/query lane.

V8 RUNTIME

crates/core/src/host/v8/mod.rs:1-55 describes the topology:
* A single `SharedJsMainInstanceManager` thread (one V8 isolate, mpsc-queued) handles reducers, subscriptions, and one-off queries.
* A separate bounded `ModuleInstanceManager<JsModule>` pool handles procedures. Each procedure instance is an independent thread with its own isolate.
* After a trap or heap retirement, the isolate is replaced inline.

THE WASMINSTANCE TRAIT

crates/core/src/host/wasmcommon/modulehost_actor.rs:79:

    pub trait WasmInstance {
        fn call_reducer(&mut self, op: ReducerOp<'_>, budget: FunctionBudget) -> ReducerExecuteResult;
        fn call_view(&mut self, op: ViewOp<'_>, budget: FunctionBudget) -> ViewExecuteResult;
        fn call_view_anon(&mut self, op: AnonymousViewOp<'_>, budget: FunctionBudget) -> ViewExecuteResult;
        fn call_procedure(&mut self, op: ProcedureOp, budget: FunctionBudget) -> impl Future<...>;
        fn call_http_handler(&mut self, ...);
        fn extract_descriptions(&mut self) -> Result<RawModuleDef, DescribeError>;
    }

This trait is shared by both WASM and V8 backends, providing a uniform interface to the module host actor.

HOST FUNCTIONS (MODULE ABI)

The host functions (tagged exports from the module to the host runtime) are declared and dispatched through WasmInstanceEnv at crates/core/src/host/mod.rs. The C ABI that modules use includes: consolelog, bufferalloc, schedulereducer, cancelreducer, insert, delete, iter, datastoretablerowcount, etc. Modules export: describemodule, callreducer, callprocedure, callview, callviewanon, callhttp_handler.

ENERGY BUDGET

FunctionBudget from spacetimedbclientapimessages::energy::FunctionBudget limits how many "instructions" a single reducer/procedure can execute. ReducerOutcome::BudgetExceeded (hostcontroller.rs:175) is returned when the budget is exhausted.

============================================================

------------------------------------------------------------
8. REDUCERS, PROCEDURES, VIEWS, HTTP HANDLERS
------------------------------------------------------------

REDUCERS

Reducers are transactional, atomic functions. The module host calls callreducer on the WASM/V8 instance, passing the reducer name and arguments encoded as BSATN. The reducer runs inside a MutTxId (exclusive write lock). If it succeeds, the transaction is committed: EventStatus::Committed(DatabaseUpdate). If it fails (panic, trap, budget exceeded), the transaction is rolled back and the error is reported: EventStatus::FailedUser or EventStatus::OutOfEnergy. The reducer result is serialized in EventStatus at crates/core/src/host/modulehost.rs.

PROCEDURES

Procedures (call_procedure) can span multiple transactions. They hold an exclusive instance (for V8) or an async WASM instance. The procedure can explicitly commit intermediate state and continue. Unlike reducers, procedure results return values and sub-steps are not automatically emitted as Event entries.

VIEWS

Views (callview) are read-only. They do not open a transaction; they operate on a TxId (shared lock). Views can be parameterized (with argument restrictions). Parameterized views are interpreted via ViewDef and have result size limits. Anonymous views (callview_anon) return raw SQL results without a pre-defined schema.

HTTP HANDLERS

callhttphandler receives HttpRequest and returns HttpResponse. Routes are registered by the module and dispatched by the host.

SIGNATURES (FROM MODULE_HOST.RS)

The call signatures in ModuleHost (the higher-level struct, not the WasmInstance trait) include:
* `call_reducer` — name, args, auth context, caller connection, energy budget → `ReducerCallResult`
* `call_procedure` — procedure id, args → `ProcedureCallResult`
* `call_view` — view id, args → `ViewCallResult`
* `call_view_anon` — SQL string → `ViewCallResult`
* `call_http_handler` — request → `HttpHandlerCallResult`

============================================================

------------------------------------------------------------
9. SUBSCRIPTIONS: SQL → PLAN → EVALUATION → BROADCAST
------------------------------------------------------------

SUBSCRIPTION MANAGER

The central actor is SubscriptionManager at crates/core/src/subscription/modulesubscriptionmanager.rs. It tracks:
* `clients: HashMap<ClientId, ClientInfo>` — each with `v1_subscriptions`, `v2_subscriptions`, `legacy_subscriptions`, and a `dropped` flag.
* `queries: HashMap<QueryHash, QueryState>` — cached subscription plans.
* `search_args: SearchArguments` — `(TableId, ColId, AlgebraicValue) → HashSet<QueryHash>`: an index mapping "which queries are interested in a given column value being inserted/deleted."
* `join_edges: SearchArguments` — similar index for join conditions.
* `tables: IntMap<TableId, HashSet<QueryHash>>` — "which queries are interested in any change to this table."
* `indexes: QueriedTableIndexIds` — refcounted index usage tracking.

QUERY COMPILATION

When a client subscribes (crates/core/src/subscription/subscription.rs), the SQL string is compiled via SubscriptionPlan::compileplans. The subscription SQL subset is intentionally restricted: SELECT * FROM table (no arbitrary projections), optional WHERE clause with indexed columns, optional JOIN with one other table on indexed columns, no aggregates, no subqueries, no ORDER BY, no GROUP BY. The compiler produces one or more SubscriptionPlan objects (at crates/subscription/src/lib.rs), each containing Fragments with insertplans and delete_plans as PipelinedProject vectors.

DELTA EVALUATION

When a transaction commits, evalupdatessequential (line 1346) processes the delta:

1. The `DeltaTx` wraps a `TxId` (for committed state reads) plus `DeltaTableIndexes` (small BTree indexes built from the changed rows for fast lookups).
2. For each changed row, `queries_for_row` (line 1321) looks up the `QueryHash` set from `search_args` and `join_edges`.
3. Only the queries whose hash appears in the lookup results are re-evaluated — not all subscriptions.
4. `eval_delta` at `crates/core/src/subscription/delta.rs:19` runs the `SubscriptionPlan` over the `DeltaTx`, collecting `inserts` and `deletes` as `UpdatesRelValue`. The function explicitly documents bag semantics (line 12-17): duplicate rows from joins are NOT removed.
5. The result (`ComputedQueries`) is sent over a channel to the `SendWorker`.

SENDWORKER

The SendWorker (line 1714) is an off-main-thread async task. It receives SendWorkerMessage::Broadcast messages and aggregates per-client updates. Key optimization (from the comment at line 1385-1387):
  | "We've now finished all of the work which needs to read from the datastore, so get this work off the main thread and over to the send_worker, then return ASAP in order to unlock the datastore and start running the next transaction."

The worker (at sendonecomputed_queries, line 1881+):
1. Groups `ClientUpdate`s by `(ClientId, TableId)`.
2. For each client, stitches all table updates into a single `DatabaseUpdate`.
3. Sends the update to the client's `ClientConnectionSender`.

This design ensures per-client update cost scales with O(updates touched) rather than O(subscriptions × transactions).

WEBSOCKET FRAMING

Two protocols exist:
* **v1** (`ws_v1`): JSON or BSATN. Each subscription is a SQL string sent via `Subscribe` message. Updates are sent as `DatabaseUpdate` messages with `table_updates` per table.
* **v2** (`ws_v2`): The typed-query-builder protocol. Subscriptions use `QuerySetId`-keyed query sets. Introduces `TableUpdateRows` with per-row data. Two-phase subscribe: apply the initial snapshot, then subscribe to incremental updates.

RLS INTEGRATION

Row-Level Security filters are compiled into the subscription plan as additional WHERE clauses. The RLS expression is a RowLevelExpr from crates/engine/src/sql/rls.rs. Module owners bypass RLS; subscriber clients do not.

============================================================

------------------------------------------------------------
10. EVENT TABLES: THE SPECIAL "EPHEMERAL" TIER
------------------------------------------------------------

Event tables are a SpacetimeDB feature for data that should be broadcast to subscribers in the transaction that inserts it, but not merged into the committed in-memory state for future transactions.

DECLARATION

Tables are declared as event tables via the #[table(..., event)] attribute in Rust, or the equivalent in other languages. The isevent: bool field lives in RawTableDefV10 at crates/lib/src/db/rawdef/v10.rs:279-284:

    /// Whether this is an event table.
    ///
    /// Event tables are write-only: their rows are persisted to the commitlog
    /// but are NOT merged into committed state. They are only visible to V2
    /// subscribers in the transaction that inserted them.
    pub is_event: bool,

BEHAVIOR IN COMMITTEDSTATE

In CommittedState::mergeapplyinserts at crates/datastore/src/lockingtxdatastore/committed_state.rs:619-622:

    if schema.is_event {
        // For event tables, we don't want to insert into the committed state,
        // we just want to include them in subscriptions and the commitlog.
        Self::collect_inserts(page_pool, truncates, tx_data, &tx_bs, table_id, tx_table, |_| {});
    }

The rows are collected into TxData (for the commitlog and subscriptions) but the commit-table insert is skipped.

BEHAVIOR IN REPLAY

In Replay::replayinsert at crates/datastore/src/lockingtx_datastore/replay.rs:754-758:

    // Event table rows in the commitlog are preserved for future replay features
    // but don't rebuild state — event tables have no committed state.
    if schema.is_event {
        return Ok(());
    }

The comment explicitly states: event table rows are serialized into TxData and written to the commitlog. They are just not re-inserted into the committed state on replay.

BEHAVIOR IN SUBSCRIPTIONS

In get_all at crates/core/src/subscription/subscription.rs:26:

    .filter(|t| t.table_type == StTableType::User && auth.has_read_access(t.table_access) && !t.is_event)

Event tables are excluded from the initial "subscribe all" query. They are only visible to queries that explicitly select from them, and only see rows inserted in the current transaction (since no committed rows exist).

SUBSCRIPTION BEHAVIOR (VERIFIED)

Event tables are excluded from the initial "subscribe all" query. They are only visible to queries that explicitly select from them, and only see rows inserted in the current transaction (since no committed rows exist).

A subscription that includes an event table evaluates rows as part of the current transaction's delta only. The returnseventtable() method on the subscription Plan (at crates/core/src/subscription/modulesubscriptionmanager.rs:146) signals that the delta for this subscription should include event-table rows for the current transaction.

**CORRECTION (VERIFIED AGAINST SOURCE 2026-06-18)**

A previous version of this section claimed that event tables have a private flag and a sender column, and that rows are visible only to V2 (typed-query-builder) subscribers. These claims are not supported by the actual STDB 2.6.0 source. Verified against crates/datastore/src/system_tables.rs:1783-1785:

    pub struct StEventTableRow {
        pub(crate) table_id: TableId,
    }

StEventTableRow has exactly one field: tableid. The StEventTableFields enum at line 435 also has only one field: tableid. There is no private column, no sender column, and no V2-only gating in the current STDB source. Any per-recipient privacy model must be built at the application layer; STDB does not provide it via event tables.

WabiDB's ephemeral tier does not depend on these features. WabiDB simply does not write ephemeral rows to SQLite at all, which sidesteps the entire class of "how do we ensure per-recipient privacy on a public commitlog" concerns.

CRITICAL PRIVACY IMPLICATION

The fact that event table rows are written to the commitlog is a critical consideration for a privacy-first platform. Even though event tables appear ephemeral (rows vanish after the transaction), the commitlog on disk retains the serialized TxData. An attacker with disk access or an operator who retains commitlog files can reconstruct all event table rows. There is no built-in mechanism to purge event rows from the commitlog after a retention period. The comment at replay.rs:754-758 says "Event table rows in the commitlog are preserved for future replay features" — this is intentional, not an oversight.

============================================================

------------------------------------------------------------
11. FILE / BINARY STORAGE IN COLUMNS
------------------------------------------------------------

SpacetimeDB supports Vec<u8> (Rust), List<byte> (C#), std::vector<uint8_t> (C++), and t.array(t.u8()) (TypeScript) as column types. Binary data is stored inline in the row when it fits, and as a blob-store entry (content-addressed via BLAKE3) when it exceeds a threshold.

The 2.0 documentation (docs/docs/00200-core-concepts/00300-tables/00210-file-storage.md) describes binary column storage:
* Files up to ~100 MiB are supported inline.
* Rows (including binary columns) are held in memory during reducer execution.
* Binary columns participate in transactions and subscriptions just like any other column.
* If binary data exceeds available memory during reducer execution, the system will be under memory pressure.

For a chat platform, this means voice notes, image attachments, and audio frames can be stored directly in tables with transactional guarantees. The subscription system will push new binary rows to subscribed clients automatically. However, the in-memory requirement means large binary storage is expensive — every binary value in a row is present in the committed state's in-memory blob store.

============================================================

------------------------------------------------------------
12. SQL: SHAPE, RESTRICTIONS, AND THE "NO FULL SQL" REALITY
------------------------------------------------------------

SQL PARSER

The parser lives at crates/sql-parser/ and wraps the sqlparser crate. Two subsets exist:

1. **Query SQL** (CLI/HTTP): A broader subset allowing multi-table joins, `WHERE`, indexing hints. Used via the HTTP API (`/sql` endpoint).
2. **Subscription SQL**: Stricter subset. Used in WebSocket subscription messages.

SUBSCRIPTION SQL RESTRICTIONS

The subscription SQL at crates/subscription/src/lib.rs and crates/core/src/subscription/subscription.rs is intentionally restricted:
* `SELECT * FROM table` — no arbitrary column projections; always full rows.
* One or two tables joined on indexed columns.
* `WHERE` predicates on indexed columns.
* No `ORDER BY`, `GROUP BY`, `HAVING`, `LIMIT`, `OFFSET`.
* No aggregates (`COUNT`, `SUM`, etc.).
* No subqueries, no `UNION`, no `EXCEPT`, no `INTERSECT`.
* No `DISTINCT`.
* No `JOIN` types other than inner join on equality.
* No `UPDATE`, `DELETE`, or `INSERT` — state changes are exclusively through reducers.

QUERY SQL

The query SQL (HTTP API) is a strict superset of the subscription SQL. More-than-two-table joins are allowed. It is still not a full SQL dialect (no UPDATE/DELETE/INSERT).

VIEW SQL

Views have their own SQL subset: the query is analyzed and incrementally maintained. View definitions compile into the ViewDef schema stored in system tables. When a table a view depends on changes, the view is re-evaluated.

RLS

Row-Level Security can be enabled with a preprocessor directive (@rls). The module defines a filter function; the system compiles it into a RowLevelExpr that is injected into subscription plans. Module owners bypass RLS. This is documented in the v2 docs but the RLS doc still references v1.12 patterns in docs/versioned_docs/version-1.12.0/.

============================================================

------------------------------------------------------------
13. SECURITY AND ISOLATION
------------------------------------------------------------

WASM ISOLATION

WASM modules run in Wasmtime instances with fuel metering. This provides genuine defense in depth: even if the module's WASM bytecode is malicious, it cannot escape the Wasmtime sandbox. The host's memory is not directly accessible to the WASM instance — all communication is through the C ABI host function bridge.

V8 ISOLATION

V8 modules run in separate isolates per main-lane/work-lane. Each isolate has its own heap, GC, and JIT. Communication with the host is through explicit syscall functions defined in crates/core/src/host/v8/syscall.rs.

ENERGY BUDGET

FunctionBudget caps how much CPU work a single reducer/procedure can consume. This is enforced via Wasmtime fuel for WASM modules and approximate instruction counting for V8. The ReducerOutcome::BudgetExceeded variant signals that the budget was hit.

IDENTITY AND AUTH

Authentication is identity-based: 32-byte public keys, JWTs, AuthCtx carrying the caller's Identity and ConnectionId. ConnectionAuthCtx is threaded through every reducer call. The clientconnected and clientdisconnected lifecycle reducers can reject connections by throwing in client_connected.

DATABASE ISOLATION

Each database runs in its own HostCell (module host + RelationalDB + Locking). Modules cannot read other databases' tables. The ControlDb at crates/standalone/src/control_db.rs holds server-level metadata (databases, replicas, hosts, nodes) in a sled-backed store, separate from user data.

CONNECTION LIFECYCLE

Connections open via WebSocket upgrade. The server authenticates the client (JWT or identity key), creates a ClientConnectionSender, and optionally calls the clientconnected reducer. On disconnect, clientdisconnected is called. If client_connected throws, the connection is rejected.

============================================================

------------------------------------------------------------
14. OPERATIONAL REALITY: HOW SOMEONE ACTUALLY RUNS THIS
------------------------------------------------------------

SERVER LAYOUT

The standalone binary (crates/standalone/src/main.rs) wires:
1. `ControlDb` — sled-backed metadata store at `data_dir/control_db/`.
2. `DiskStorage` — program bytes at `data_dir/program_bytes/`.
3. `LocalPersistenceProvider` — creates per-replica directories.
4. `HostController` — manages all databases/modules.
5. `ClientActorIndex` — WebSocket connection registry.
6. Prometheus metrics registry.
7. JWT auth provider.

DATA DIRECTORY LAYOUT

    data_dir/
      control_db/           # sled database of databases/replicas/nodes
      program_bytes/        # compiled WASM/V8 bytecode files
      <database_identity>/  # per-database
        <replica_id>/
          commitlog/        # segments: <offset>.segment
          snapshots/        # snapshot directories: <tx_offset>.snapshot_dir/
          lockfile          # advisory database lock

SNAPSHOTS

Every SNAPSHOTFREQUENCY (1,000,000) transactions, a snapshot is taken. The snapshot worker runs on spawnblocking to avoid blocking the main async runtime. Snapshots are created as UnflushedSnapshot objects, then asynchronously synced via sync_all.

COMPRESSION

Snapshots and commitlog segments can be compressed with zstd. compresssnapshots and compresssegments are explicit operations. The CompressionStats types track compression metrics.

MONITORING

Prometheus metrics are exposed: ENGINEMETRICS, DBMETRICS, WORKER_METRICS. Jemalloc profiling is compiled in (line 44-60 of standalone/src/main.rs) but disabled by default.

ENCRYPTION AT REST

There is no built-in encryption at rest. The commitlog segments and snapshot files are plaintext (or plaintext-compressed). OS-level encryption (dm-crypt, LUKS) would be required for disk-level protection.

RESOURCE CONSTRAINTS

* Page pool shared across all databases on a host.
* In-memory committed state: the entire database lives in RAM.
* Single-writer lock per database: only one reducer at a time per database.
* Energy budgets bound reducer CPU usage.

============================================================

------------------------------------------------------------
15. WHAT THIS MEANS FOR A SELF-HOSTED PRIVACY-FIRST CHAT PLATFORM
------------------------------------------------------------

LESSONS THAT TRANSLATE DIRECTLY

Reducer model with transactional state mutations. The idea of encapsulating state changes in named, typed functions that run atomically is elegant and directly applicable. A chat platform's operations (send message, join room, upload attachment) map naturally to reducers. The reducer model gives a clear mental model: the database never transitions between invalid states because each reducer is an atomic "patch." Notably, STDB's reducer model is not a stored procedure or a database trigger — it is application code that happens to run inside the storage engine's write lock. For Wabi, the same pattern works: define commands that read state, compute a result, and produce row mutations, all in one shot.

Live subscriptions to row changes via push. The subscription system — compile a query, evaluate it on each transaction delta, push only the changed rows — is the correct model for real-time UI updates. The architecture of deduplicating by query hash, indexing by (table, column, value), and offloading serialization to a background worker are all directly reusable patterns. The key insight from the STDB implementation is that the SendWorker (crates/core/src/subscription/modulesubscriptionmanager.rs:1714) is the right place to aggregate and format per-client messages, because it moves CPU-intensive serialization off the critical transaction path. The evalupdatessequential design (line 1346) — which removed rayon parallelization in favor of single-threaded evaluation for small deltas — is also instructive: small updates are the common case for chat, and thread-switching overhead dominates evaluation time.

Colocated state and logic, single binary deploy. Having the application logic and the database in the same process eliminates network round trips and simplifies deployment. For a single-tenant server, this is ideal. STDB achieves this through the module WASM sandbox, but the same benefit can be had without WASM by co-compiling the app logic with the database engine. The colocation eliminates the replication lag, connection pooling, and serialization overhead that traditional three-tier architectures impose.

In-memory hot state + commitlog durability. The performance profile (fast reads/writes because everything is in memory, safe persistence because the WAL is append-only) is well-suited to a chat workload where most data is hot. The recovery pipeline (snapshot → commitlog replay) is well-proven in STDB. For a smaller engine, a simpler approach is possible: open a SQLite database for the durable state and keep a small in-memory cache of hot topics. But the STDB pattern of "completely in-memory, backed by an append-only log" is viable if the dataset fits in RAM.

Energy quotas to bound reducer work. A necessary operational control for multi-tenant scenarios and DoS protection. The FunctionBudget mechanism (spacetimedbclientapi_messages::energy::FunctionBudget) limits how much CPU a single reducer can consume. In a chat platform, this bounds the harm from a buggy or malicious room event handler.

Event tables as a tier for "transient" data. The concept of write-commitlog-only, don't-merge-to-state is exactly right for short-lived signals (typing indicators, audio frames, ephemeral presence). The commitlog caveat (rows persist in the WAL) must be understood and addressed; a Wabi implementation could add a memory_only tier that does not write to the commitlog at all, accepting the risk of loss on restart for truly transient data.

Pages + page pool as a shared resource model. Object pooling for pages with content-addressed snapshot deduplication is a smart resource management pattern. The page pool (crates/table/src/pagepool.rs) reduces allocation pressure by recycling Box<Page> objects. The snapshot hardlinking (crates/snapshot/src/lib.rs:335: hardlinkor_write) avoids re-writing unchanged pages across consecutive snapshots. These are engineering patterns that a smaller engine can adopt directly.

Query hash indexing for delta evaluation. The SearchArguments data structure (a mapping from (TableId, ColId, AlgebraicValue) to HashSet<QueryHash>) at modulesubscriptionmanager.rs is an elegant index for "given this changed row, which subscriptions need re-evaluation?" This avoids re-evaluating every subscription on every transaction. A chat topic system could use an analogous index: (topic, channel_id) -> set of subscribers.

LESSONS THAT DO NOT TRANSLATE

BSL 1.1 license. The "one SpacetimeDB instance in production" restriction is a meaningful constraint for any architecture that wants to run multiple isolated database processes. For a platform that envisions per-community databases, data sovereignty, or tenant-isolated storage, the BSL's "one instance" provision is a liability. A Wabi-native engine has no such restriction.

The full subscription query planner and SQL subset. STDB ships a SQL compiler, a physical plan optimizer (crates/physical-plan/), a pipelined executor (crates/execution/src/pipelined/), and a subscription plan compiler (crates/subscription/src/lib.rs) — approximately 15,000 lines of nontrivial query processing code. A chat platform does not need SELECT * FROM table JOIN table2 ON ... WHERE col = ?. A topic-based or channel-based subscription model (subscribe("room/123/messages")) is simpler, more intuitive, and easier to secure. The entire SQL layer is overhead that a purpose-built engine does not need.

The full multi-language module runtime (WASM + V8). STDB invests heavily in running untrusted user code: Wasmtime fuel metering, V8 isolate pools, the C ABI host function bridge, the WasmInstanceEnv binding layer, compile-time codegen (crates/bindings-macro). This is tens of thousands of lines of code across crates/{bindings,bindings-macro,bindings-sys,core/src/host/{wasmtime,v8,wasm_common}}. Wabi does not need to run arbitrary user code — it only needs to run its own logic. The module runtime is the single largest cost in the STDB codebase.

BSL-imposed "one instance per app." As noted above, this restriction complicates privacy-split architectures that need multiple logical databases for data compartmentalization. If the design goal is "each community gets its own encrypted database," running N STDB instances is not allowed under the default grant.

Generic database engine scope. The SQL interface, ad-hoc query support, schema migrations, system tables (sttable, stcolumn, stindex, stsequence, stview, steventtable, stscheduled, stvar, stclient, stconnectioncredentials, strowlevelsecurity, sttableaccessor, stcolumnaccessor, stindexaccessor — at least 15 system tables), the RawModuleDefV9/V10 builders, the migration planner (crates/schema/src/automigrate/), all exist to make STDB a general-purpose database. For a focused chat platform, these are liabilities: they add complexity, attack surface, and maintenance burden.

Client authentication and energy accounting infrastructure. STDB has a full JWT-based identity system, energy balance tracking, and a NullEnergyMonitor trait that is extensible for paid-tier energy metering. A self-hosted chat platform does not need micro-transaction-level energy accounting. Simple rate limiting per-connection is sufficient.

PRIVACY / RETENTION OBSERVATIONS

Event tables' commitlog persistence is a gotcha. If a chat platform uses event tables for "transient" features (voice frames, ephemeral messages), the commitlog on disk retains a full history of those events. There is no built-in retention or garbage collection for the commitlog. An operator (or attacker with disk access) can replay the commitlog and reconstruct all event rows. The source at replay.rs:754-758 is explicit: "Event table rows in the commitlog are preserved for future replay features." If Wabi adopts event tables, it must either (a) add a memory_only flag that skips commitlog writing for truly transient data, losing it on restart, or (b) encrypt event-table commitlog entries with per-session keys.

WAL replay reconstructs everything. Because the commitlog is the source of truth for recovery, anyone who can read the commitlog files can reconstruct the entire database state at any point in history (bounded by snapshot pruning). For a privacy-first platform, this means disk-level encryption is mandatory, and a secure-erase policy for old commitlog segments is necessary. The STDB codebase does not provide any of this — it is operator responsibility.

No built-in TTL or retention. There is no database primitive for per-table or per-row time-to-live. Retention must be implemented as application logic (scheduled reducers that delete old rows). The commitlog, however, will retain those deletions forever unless the operator prunes old segments. For a chat platform with legal retention requirements, a first-class retention policy engine (per-topic, per-user, durable-only vs. soft-delete) is essential.

Snapshot compression does not imply privacy. Snapshots can be zstd-compressed (crates/snapshot/src/lib.rs:1229-1261), but compression is not encryption. The same BLAKE3 content hashes are used for deduplication across snapshots, which means content-equivalent pages are linkable by hash value. A privacy-focused design might want to use per-snapshot encryption keys to prevent cross-snapshot linkage.

WHAT WABI COULD KEEP

* **Reducer model + transactional state.** The core abstraction of named, typed, atomic state transitions is the single most important idea to retain. It provides an audit trail (every state change is a named event with caller identity), simplifies concurrency (single writer lock), and maps cleanly to the UX of a chat application.
* **Topic-based subscriptions with row-level deltas.** The concept of per-subscription deltas computed as the symmetric difference between a query's old and new results is the correct foundation for real-time UI. But the query language should be `subscribe("room/{id}/messages")` not `SELECT * FROM messages WHERE room_id = ?`. The delta computation can be simpler: insert new rows matching the topic, delete rows that were removed or no longer match (e.g., deleted messages).
* **Two-tier data lifecycle.** Borrow the durable vs. ephemeral (event table) distinction, but add a third tier for encrypted-at-rest blobs. STDB's event tables demonstrate that not all data needs committed-state persistence. A third "crypto" tier would hold messages that can only be decrypted by the intended recipient.
* **In-memory hot state + append-only log.** The performance and simplicity arguments are compelling for a single-tenant server with moderate data volume.
* **Energy budgeting for any future addon system.** If Wabi ever supports plugins, fuel metering via Wasmtime is the right approach. The ABI lesson from STDB (`_insert`, `_delete`, `_iter` as host functions) is a clean, testable contract between the runtime and user code.

WHAT WABI SHOULD REINVENT CLEANLY

* **An embedded storage layer.** Use SQLite or `redb` for committed state, instead of the custom page pool + BTree index implementation. This eliminates the "all data must fit in RAM" requirement and provides battle-tested ACID compliance, concurrent readers, and on-disk durability out of the box. The in-memory cache can be a separate layer (e.g., a row cache for hot topics), not the primary storage. This also eliminates ~15,000 lines of custom storage engine code (`crates/table/`, `crates/datastore/src/locking_tx_datastore/`, and large parts of `crates/engine/`).
* **A purpose-built live-update broadcaster.** Not a SQL subscription planner. A channel-based pub/sub with topic routing (e.g., `"room/{room_id}"`, `"user/{user_id}/dm"`), per-subscriber filters (e.g., only messages from certain senders), and minimal overhead. The SendWorker pattern (off-main-thread aggregation + serialization) is worth keeping.
* **A bounded object-type system.** Instead of arbitrary `AlgebraicType` schemas reducible to SATS/BSATN, define a fixed set of message types (`TextMessage`, `VoiceFrame`, `Attachment`, `UserPresence`, `Reaction`, etc.) that map to database rows. This eliminates the entire dynamic type system, schema compilation, and the `RawModuleDef` pipeline, and simplifies the subscription compiler enormously because every subscription is just "rows of type X with filter Y."
* **A retention engine as a first-class component.** Per-topic retention policies (TTL for ephemeral messages, logical deletion markers, encrypted blob garbage collection), secure deletion (file-level shredding for commitlog segments), and encryption-at-rest integration. This is a non-negotiable requirement for a privacy-first platform, and STDB does not provide it.
* **Replica isolation.** STDB's `Locking` struct uses a single `RwLock<CommittedState>` per database, serializing all writers. A chat platform with many rooms can use per-room or per-shard write locks for concurrency, as long as cross-room transactions are not needed. This is a significant scalability win that STDB's architecture does not support.

============================================================

------------------------------------------------------------
16. SOURCE MAP APPENDIX
------------------------------------------------------------

A recommended reading order for a future implementer who wants to study the codebase for design inspiration:

#   File                                                                            Lesson                                                                                                                          
--  ------------------------------------------------------------------------------  --------------------------------------------------------------------------------------------------------------------------------
1   `crates/engine/src/relational_db.rs:98-126`                                     The `RelationalDB` struct: how durability, snapshotting, and MVCC are composed. Read `open` (line 259) for the startup pipeline.
2   `crates/datastore/src/locking_tx_datastore/datastore.rs:54-100`                 `Locking` struct, lock ordering, and the `new`/`commit` flow.                                                                   
3   `crates/datastore/src/locking_tx_datastore/committed_state.rs:52-80`            `CommittedState` — the entire in-memory database state in one struct.                                                           
4   `crates/datastore/src/locking_tx_datastore/tx_state.rs:21-60`                   `TxState` — how a transaction tracks dirty data.                                                                                
5   `crates/commitlog/src/lib.rs:40-101,156-480`                                    `Options` and `Commitlog` — the WAL interface.                                                                                  
6   `crates/durability/src/lib.rs:118-174`                                          `Durability` trait — the abstract persistence layer.                                                                            
7   `crates/snapshot/src/lib.rs:1-22,750-853`                                       Snapshot philosophy and `SnapshotRepository::create_snapshot`.                                                                  
8   `crates/datastore/src/locking_tx_datastore/replay.rs:35-80,754-758`             Replay logic and event-table skip.                                                                                              
9   `crates/table/src/page_pool.rs:24-51`                                           `PagePool` — shared page object pool.                                                                                           
10  `crates/table/src/blob_store.rs:63-128`                                         `BlobStore` trait — content-addressed blob storage.                                                                             
11  `crates/core/src/host/host_controller.rs:66-121`                                `HostController` — the host registry and shared resources.                                                                      
12  `crates/core/src/host/module_host.rs:80-120`                                    `DatabaseTableUpdate` and `EventStatus` — the transaction delta format.                                                         
13  `crates/core/src/host/wasm_common/module_host_actor.rs:79-100`                  `WasmInstance` trait — the unified module call interface.                                                                       
14  `crates/core/src/subscription/module_subscription_manager.rs:1300-1395`         `eval_updates_sequential` — the subscription evaluation entry point.                                                            
15  `crates/core/src/subscription/module_subscription_manager.rs:1710-1842`         `SendWorker` — off-main-thread broadcast aggregation.                                                                           
16  `crates/core/src/subscription/delta.rs:1-50`                                    `eval_delta` — running a subscription plan over a delta.                                                                        
17  `crates/core/src/subscription/tx.rs:1-60`                                       `DeltaTx` and `DeltaTableIndexes` — fast delta lookup structures.                                                               
18  `crates/core/src/subscription/subscription.rs:14-39`                            Subscription SQL compilation and event-table filter.                                                                            
19  `crates/subscription/src/lib.rs:1-100`                                          `Fragments` and `SubscriptionPlan` — compiled query plan for subs.                                                              
20  `crates/lib/src/db/raw_def/v10.rs:270-284`                                      `is_event` field definition.                                                                                                    
21  `crates/datastore/src/locking_tx_datastore/committed_state.rs:600-633`          Event-table skip in `merge_apply_inserts`.                                                                                      
22  `crates/datastore/src/locking_tx_datastore/replay.rs:754-758`                   Event-table skip in replay.                                                                                                     
23  `crates/datastore/src/traits.rs:23-100`                                         `IsolationLevel` and the isolation model.                                                                                       
24  `crates/core/src/host/v8/mod.rs:1-55`                                           V8 module host topology diagram and description.                                                                                
25  `crates/standalone/src/lib.rs:42-100`                                           `StandaloneOptions` and `StandaloneEnv::init` — how the server wires together.                                                  
26  `crates/standalone/src/control_db.rs`                                           The sled-backed ControlDb for server metadata.                                                                                  
27  `crates/commitlog/src/payload.rs` and `crates/commitlog/src/payload/txdata.rs`  The `Txdata` encoding format — what is actually written to the WAL per transaction.                                             
28  `crates/core/src/host/mod.rs:34-77`                                             `FunctionArgs` — how reducer arguments are encoded/decoded (JSON or BSATN).                                                     
29  `crates/engine/src/persistence.rs:22-73`                                        `DurabilityConfig` and `CommitlogConfig` — how operator-facing config maps to commitlog `Options`.                              
30  `crates/core/src/host/host_controller.rs:619-650`                               `acquire_read_lock` and `acquire_write_lock` — the lock acquisition for concurrent access to a host cell.                       
31  `crates/subscription/src/lib.rs:1-100`                                          `SubscriptionPlan` and `Fragments` — the compiled subscription plan structure.                                                  
32  `crates/physical-plan/src/plan.rs`                                              `PhysicalPlan` types (`IxScan`, `TableScan`, `JoinPlan`, `ProjectPlan`).                                                        
33  `crates/execution/src/pipelined/`                                               `PipelinedIxScan`, `PipelinedIxJoin`, `PipelinedProject` — the pipelined execution engine.                                      
34  `crates/core/src/subscription/module_subscription_manager.rs:150-199`           `ClientInfo` — how the subscription manager tracks per-client state (v1, v2, legacy subs, ref counts).                          
35  `crates/datastore/src/locking_tx_datastore/tx.rs:1-80`                          `TxId` — a read-only transaction with shared lock on committed state.                                                           
36  `crates/datastore/src/locking_tx_datastore/mut_tx.rs:1-60`                      `MutTxId` — the writer transaction and its view of inserts/deletes.                                                             
37  `crates/core/src/host/wasmtime/mod.rs`                                          `WasmtimeModule` compilation, `ModuleInstance` creation, fuel setup.                                                            
38  `crates/core/src/host/v8/syscall.rs:62-65`                                      The V8 syscall bridge: `call_call_reducer`, `call_call_view`, etc.                                                              
39  `crates/bindings/src/lib.rs`                                                    The `spacetimedb` crate API surface that module authors use (`#[table]`, `#[reducer]`, etc.).                                   
40  `crates/bindings-macro/src/lib.rs`                                              The proc macros that generate `__describe_module__` and the C ABI exports.                                                      

============================================================

------------------------------------------------------------
17. OPEN QUESTIONS AND UNKNOWNS
------------------------------------------------------------

DOC DRIFT

The file storage documentation exists only in the 2.0 docs (docs/docs/00200-core-concepts/00300-tables/00210-file-storage.md). The RLS documentation at docs/versioned_docs/version-1.12.0/00300-resources/00100-how-to/00400-row-level-security.md references patterns from the v1.12 era. The gap suggests that some features (file storage) were introduced or documented only in 2.0, while others (RLS) have documentation that has not been updated to reflect 2.0 changes.

CONTRADICTIONS BETWEEN DOCS AND SOURCE

The source code's comment at committedstate.rs:74-77 says the page pool handle is "shared between all modules running on a particular host." The HostController::pagepool field indeed holds a single PagePool. However, it is unclear if this means a malicious module could exhaust the shared page pool and affect other databases. Each database's CommittedState has the same PagePool handle, so a module that allocates many large pages could pressure the pool. The pool's max_size is the only guard.

BSL ADDITIONAL USE GRANT INTERPRETATION

The BSL Additional Use Grant language ("no more than one SpacetimeDB instance in production") has not been legally tested. Whether "instance" means one operating system process or one logical database is ambiguous. The "Database Service" carveout is also ambiguous for apps that provide multi-tenant chat rooms (where each room operator controls their own schema). This study does not constitute legal advice.

EVENT TABLE COMMITLOG RETENTION

While the source and documentation are clear that event table rows are preserved in the commitlog (replay.rs:754-758), the long-term implications are not addressed: there is no documented mechanism to purge event rows from old commitlog segments, and no discussion of the ethical/privacy implications of ephemeral data persisting in the WAL indefinitely.

ENCRYPTION AT REST

The commitlog, snapshots, and program bytes are all stored as plaintext files. The server documentation does not mention encryption at rest. An operator must rely on OS-level disk encryption, which is unsuitable for environments where the disk is not fully controlled (e.g., cloud block storage with snapshot features).

============================================================

This document was produced as an independent technical study of the SpacetimeDB source tree at /var/home/Ronin/vendor/SpacetimeDB (v2.6.0). It is not legal advice. It does not reproduce significant portions of the STDB source code. All claims are backed by file:line citations to specific source files read during the study.

============================================================

============================================================
END OF PACKAGE
============================================================

Total: 1 file, ~26,000 words. Generated 2026-06-19 by Hermes for Ronin.
