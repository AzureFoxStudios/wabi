# PR intake review — 2026-09-17 (this session)

## Correction after authenticated recheck

The earlier integration claims below were overstated. Actual `origin/main..union/pr-intake-20260917` ancestry contains TEN frontend dependency PRs: #218, #220, #222, #223, #226, #228, #229, #230, #231, #232. It does NOT contain the Rust bumps #219, #221, #224, #225, #227. The recorded frontend gates apply only to those ten frontend updates. No Rust bump is certified by those checks. Fifteen dependency PRs total does not mean fifteen integrated PRs.

GitHub authentication is restored. Campaign branch is published as PR #234. The shared backend test failure was reproduced locally: the Midnight Violet change updated app.html's theme-color default but left the server injection needle and rejected-accent expectation stale. Fix c6b69517 passes boot_brand_injection 7/7; full `cargo test --locked -p wabi-server` passes (586 test executions, zero failures, two ignored, 25 targets; unit modules are tested in both library and binary).

PR #234 still has an Android build blocker: desktop-only `pick_folder` is used unconditionally in lore_local.rs. Repair is in progress. #233 also has distinct Windows archive fixture failures (existing-path errors), not only inherited boot failures. Feature review is incomplete because both reviewer subagents hit provider 429s. Do not treat the original recommendations below as completed code reviews or merge authorization.

## Earlier checkpoint (superseded where corrected above)

Repo: AzureFoxStudios/wabi. Local main == origin/main == `d5eba19a` at start. `gh` CLI auth is INVALID (HTTP 401) — all PR data came from the public GitHub REST API; nothing was pushed, merged, or commented. Full machine-readable inventory: `pr-intake-inventory.json` (heads) and `pr-ci-inventory.json` (per-PR check runs).

## Open PRs (19)

### Dependency updates — 15 PRs (#218–#232), all single-purpose version bumps

Locally integrated for verification on isolated branch `union/pr-intake-20260917` (worktree `/var/home/Ronin/wabi-pr-intake-union`, base `d5eba19a`): #218 Vite 8.3.0, #219 base64 0.23, #220 KaTeX 0.18.7, #221 reqwest 0.13.5, #222 CodeMirror view 6.43.11, #223 CodeMirror state 6.7.4, #225 serde 1.0.229, #226 Playwright 1.63.0, #227 toml 1.1.6 (wabidb+wabi-tui), #228 Svelte 5.57.0, #229 adapter-node 5.5.7, #230 CM lang-markdown 6.5.2, #231 marked 18.0.13, #232 @types/bun 1.4.2. Merge conflicts between #226/#228 and #222/#229 were resolved by keeping BOTH sides' upgrades (newest of each); nothing dropped.

Verification on the integrated branch (all exit 0): `npm ci` clean; `bun test src/lib` (Bun 1.3.14) **905 pass / 3 skip / 0 fail**; `bun run check` **0 errors, 167 warnings (existing baseline)**; `STATIC_BUILD=1` static build completed (index.html + _app emitted).

Assessment per risky bump:
- #227 toml 0.8→1.1: API-stable for `load_from_file` config parsing; touches WabiDB config crate only. cargo compile gate passed during contract-test build on the campaign tree's lockfile, not on the bump — needs the normal CI `cargo test --workspace` before merge.
- #224 x25519-dalek 2→3 (major): X3DH/prekey code in wabidb; NOT in the verified merge branch (skipped deliberately with #227-level Rust majors — needs the E2EE key-exchange tests run on the bump before integration). NOT integrated locally.
- #218/#228/#229: build tooling; verified by the green build above.

**Recommendation:** the 14 integrated bumps are safe to merge after CI passes (they are each other's merge conflicts — merge together or in descending-number order). #224 (x25519 major) needs a targeted `cargo test -p wabidb` run on its branch first.

### Feature PRs

- **#233 Desktop Host Mode** (`codex/desktop-host-mode-20260916`, head `49f582bd`): 50 files, +3346/−195 — Tauri host mode, invites/join routes, listener/bootstrap guards, host-safety crate, desktop-host workflows. CI at this head: `build-server` FAILURE, `test` FAILURE, both "Real Authority and native host" jobs FAILURE, CodeQL FAILURE, macOS tauri + Android APK FAILURE; frontend-checks/container-install/tauri-linux/windows pass. Reviewer subagent was rate-limited mid-review (report not completed). **Do not merge — red CI.**
- **#217 Restore Little Worlds / connected pointer effects** (`6f349d52`): 28 files, +3251/−676, effects-only + own CI workflow; CI green except the shared `test` job and Android APK. No conflicts with main's current effects code per branch state. **Mergeable after CI `test` job is understood** (that job also fails on current main-adjacent branches — see below).
- **#207 voice policy / selective SFU** (`cb940089`): `frontend-checks` FAILURE, headful voice regression FAILURE, CodeQL FAILURE. **Do not merge.**
- **#184 DM/code workspace UX** (`fc4cc59f`): `frontend-checks` FAILURE + legacy `frontend`/`markup` job failures. **Do not merge.**

### Cross-cutting CI note

Every open PR (and main's recent children) shows the `test` job failing on GitHub CI. On this machine the same frontend suite passes (905/0 fail) with Bun 1.3.14, so the remote `test` failure needs a log read before any merge is trusted — likely a CI-side issue (RustEmbed/frontend/build ordering or Bun version), not these PRs.

## Not done / blocked

- Nothing pushed: gh token invalid and no push/merge authorization for this task.
- #233 full code review incomplete (subagent rate-limited); re-run when the provider recovers.
- `git worktree prune` hygiene: stale `/tmp/wabi-clean` + `/tmp/wabi-deploy-eccff5d` worktrees still registered.
