Work only at /var/home/Ronin/wabi-production-finish-union. Bounded GF06 repair, not broad campaign. Read AGENTS.md frontend/AGENTS.md, then allowed files. Existing changes uncommitted and must be preserved.
Allowed edits ONLY frontend/src/lib/galleryFeedbackStore.ts, frontend/src/lib/galleryFeedbackSession.test.ts, frontend/src/lib/components/GalleryLightbox.svelte, docs/reviews/gallery-feedback-retirement.md. Allowed reads also groupAccess.ts/groupMembership.ts, authSession.ts, api/channelAccess.ts, apiRequest.ts, existing session draft helpers if needed. No dependency edits, build stubs, reset/stash/clean/checkout/push. Other workers/builds are separate.
Concrete source findings: session currently only subscribes to onAuthSessionCleared. Already-loaded feedback does not clear on groupMembership context change or matching revocation. Component draft Map is channel::work only, no server/account/session fence, never clears on auth retirement. Pending send completion could clear another account's equal-text draft; add() lacks post-json ownership check before follow-up. dispose leaves loaded items. Fix bounded retirement lifecycle using existing groupMembership events/leases and explicit retirement revision or callback consumed by lightbox. Clear drafts on account/server/session/matching revocation, NOT same-account token refresh. Ensure component receives clear synchronously; preserve per-work same-session draft navigation. No promise of durable remount recovery in this slice; report it open.
FIRST add regression proving already-loaded items remain after context change and/or revocation, run RED, then fix. Tests actual session implementation (existing subprocess wrapper retained), injectable events okay but production wired to existing membership source. Add late response after retirement/A-B-A fencing, unrelated revocation no clearing, actual changed same-sub token refresh nonce rather than identical JWT, dispose clears, post-json change no followup, draft revision/old completion guard. Do not weaken tests or add mocks to hide production problems. Do not add request bypass. One scope, no new global store architecture.
Run npm exec --yes --package=bun@1.3.14 -- bun test src/lib/galleryFeedbackSession.test.ts then full bun test src/lib and check. Logs and report ONLY inside docs/reviews (repo root, use ../docs/reviews from frontend); do NOT use /tmp, caused prior worker auto-rejection. Avoid pipelines masking exit status. Report exact counts and honest remaining browser/remount limits. Scoped commit these exact allowed files only; no -A.