Work at /var/home/Ronin/wabi-production-finish-union. Resume HANDOFF GF01-GF06 TEST ISOLATION ONLY. Existing partial product changes are preserved and being independently reviewed. Read AGENTS.md, frontend/AGENTS.md, and the three Gallery tests. Do not change product code.
Allowed writes: frontend/src/lib/galleryLifecycle.test.ts, frontend/src/lib/galleryFeedbackSession.test.ts, frontend/scripts/gallery-lifecycle-regression.ts (or .mjs), frontend/scripts/gallery-feedback-regression.ts (or .mjs), docs/reviews/gallery-test-isolation.md. NO OTHER WRITES except test output logs in docs/reviews. No reset/stash/checkout/clean, no dependency updates, no build/stubs, no push.
Reproduced under npm exec --yes --package=bun@1.3.14 -- bun test src/lib/galleryFilter.test.ts src/lib/galleryLifecycle.test.ts src/lib/galleryFeedbackSession.test.ts: 17 pass then feedback import fails setAuthToken export missing. Feedback alone passes 14 tests /45 assertions. GalleryLifecycle.test.ts globally mock.modules authSession etc and replaces global fetch without cleanup, poisoning other tests. Repair isolation robustly using subprocess fixtures or established isolated harness pattern; do NOT add fake exports, weaken assertions, skip tests, or merely change test order. Keep exercising actual stores. Avoid new stack/deps. Review existing subprocess patterns if needed, bounded scope.
Run combined focused tests and full npm exec --yes --package=bun@1.3.14 -- bun test src/lib. Capture exact counts/exit. Write concise report and a scoped commit of ONLY your allowed paths (explicit git add file list, never -A). Do not commit other existing Gallery changes. No broad investigation; implement isolation after bounded reads. Report any product defect rather than editing product files. Tests and browser runtime are separate acceptance.