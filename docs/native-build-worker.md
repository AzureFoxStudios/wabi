Work in /var/home/Ronin/wabi-production-finish-union. Fix one actual Android build blocker, not a rewrite. Parent verified PR234 Android CI error E0599 at src-tauri/src/lore_local.rs:182: pick_folder unavailable on Android. Read AGENTS.md, src-tauri/src/lore_local.rs and src-tauri/src/lib.rs and Cargo.toml only initially. Allowed edits lore_local.rs plus narrowly needed frontend capability UI ONLY after verifying consumer, and tests/docs/reviews/native-folder-build.md. Parent/other worker own FilesWorkspace.svelte, filesWorkspaceSession*, Gallery, boot, dependency files; don't touch them. Desktop manual local-folder staging must remain fully functional. On mobile, preserve command signature but report clear unsupported local-folder staging boundary without compiling desktop pick_folder. Match Tauri v2 supported target cfg (verify installed plugin source if necessary). Don't replace folder picker with file picker, remove functionality globally or weaken any path/grant checks. Add regression guard/test for target split. Run cargo check --manifest-path src-tauri/Cargo.toml --locked (real results only). Inspect local Android SDK/NDK availability without secrets and if available run actual Android target check/build; otherwise state not run and CI required. No installs/deploy/push. Write report. Commit ONLY exact scoped files with message fix(native): gate desktop folder picker on supported targets. No git add -A, stash/reset/checkout. Start narrow edit after confirming API rather than recon spiral.