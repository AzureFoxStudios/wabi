Implement GF06 Gallery feedback lifecycle and album-derived identity correctness in this isolated review worktree. Read AGENTS.md, docs/wabi-production-finish-agent-prompt.md GF06, continuation GF06, frontend/AGENTS.md. Baseline d5eba19a. Own ONLY frontend/src/lib/galleryFeedbackStore.ts, frontend/src/lib/components/GalleryLightbox.svelte, new feedback lifecycle tests/scripts and backend gallery.rs plus narrowly scoped helper/tests if needed for album-item resolution. Other worker owns galleryStore.ts, GalleryChannel.svelte and gallery filters; do NOT edit them. Coordinator owns docs except your report docs/gallery-feedback-worker-report.md. No auth bypass, no record schema changes, no lockfile changes, no live tests/data, no deployment/push/merge/reset/stash. Source finding: Gallery list derives ids album-{albumId}-item-{id}, but POST gallery feedback requires existing GalleryWork by that ID, likely mismatch. Verify exact album API/domain IDs and resolve safely against actual album ownership/channel (do NOT weaken resource authorization or manufacture duplicate works on view). Preserve real gallery-work support. Need per-lightbox instance cache/request lifetime, captured server/account/channel/work/session, guard stale loads/save followups on A-B-A/logout/relogin/revocation/disposal; token refresh shouldn't destroy legitimate drafts. Errors truthful, drafts independent during navigation/save; do not clear new draft from old completion. Unit behavior regressions RED then GREEN for identical ids two servers, independent instances, stale load/mutation, scope retirement, token refresh. Add backend legitimate album-derived feedback route tests plus denied/missing resources using existing local test conventions; no exploitation scripts. Run actual targeted tests (and typecheck when deps ready), review diff, scoped commit exact owned files only. Report evidence and remaining browser/full-suite boundaries. External ZIP unavailable, don't invent its contents.