import {expect,test} from 'bun:test';
import {cleanDesign,cleanSceneObject,chartRows,tableRows,type SceneObject} from './scene';
const object:SceneObject={id:'aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa',kind:'text',x:.1,y:.1,w:.8,h:.2,z:0,text:'ไทย',image:null,fill:'transparent',color:'#242836',fontSize:24,align:'left',fit:'contain',cropX:.5,cropY:.5};
test('native audience primitives reject private metadata, active resources and oversized coordinates',()=>{for(const patch of [{notes:'SECRET'},{image:'https://private.invalid'},{fill:'url(javascript:alert(1))'},{x:2},{w:Number.NaN},{text:'x'.repeat(6001)}])expect(()=>cleanSceneObject({...object,...patch})).toThrow();});
test('canvas ordering is deterministic and identities cannot collide',()=>{const other={...object,id:'bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb',z:-1};expect(cleanDesign({theme:'night',objects:[object,other]}).objects.map(o=>o.id)).toEqual([other.id,object.id]);expect(()=>cleanDesign({theme:'paper',objects:[object,object]})).toThrow('Duplicate');});
test('chart/table data stays a bounded pinned value snapshot, never evaluated formulas',()=>{expect(chartRows('First\t12\nไทย\t-4')).toEqual([{label:'First',value:12},{label:'ไทย',value:-4}]);expect(chartRows('Secret\t=WEBSERVICE("remote")')).toEqual([]);expect(chartRows('Empty\t')).toEqual([]);expect(tableRows('Name\tValue\nไทย\t00123')[1][1]).toBe('00123');expect(tableRows(Array.from({length:21},()=> 'row').join('\n'))).toEqual([]);});
