/** livekitSfu.ts — STUB */
export let livekitRoom: any = null;
export let livekitChannelId: string | null = null;

export async function connectLivekitSfu(_channelId: string, _displayName: string): Promise<void> {
	/* STUB — replace with real LiveKit SFU connection logic */
}

export async function disconnectLivekitSfu(_options?: { preserveCallState?: boolean }): Promise<void> {
	/* STUB */
}
