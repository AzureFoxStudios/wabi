/**
 * storageEncryption.ts — legacy barrel.
 * Canonical implementation lives in storage/encryption.ts (finding 11).
 */
export {
	enableStorageEncryption,
	disableStorageEncryption,
	initializeStorageEncryption,
	isStorageEncryptionEnabled,
	isEncryptionEnabled
} from './storage/encryption';
