Plan

Verify which service is failing and which image is running (frontend vs backend) by checking the service names, Dockerfile context paths, and any start commands referenced in docker-compose.
Lock the frontend to static hosting: keep @sveltejs/adapter-static, remove or ignore any node build start usage, and ensure the frontend build output is treated as static assets.
Update the frontend container/runtime to serve static files (e.g., nginx) from frontend/build, and remove any node-based start command from the frontend service.
Fix backend build output naming and entrypoint: make esbuild emit a clear file like server.mjs, and set the backend start command to server.mjs.
Resolve backend ESM dynamic require errors (e.g., buffer) by adjusting esbuild config (format/target/platform) and/or marking problematic dependencies as external so Node resolves them at runtime.
Remove production bind-mounts that overwrite /app so built artifacts aren’t hidden at runtime.
Rebuild images and re-run containers, then verify: frontend serves static assets and backend starts cleanly without runtime module errors.
